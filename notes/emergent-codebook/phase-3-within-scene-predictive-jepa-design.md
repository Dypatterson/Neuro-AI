# Within-Scene Predictive (JEPA-style) Codebook-Shaping — design spec

> Status: **DRAFT — SECONDARY/EXPLORATORY GENERALIZATION TRACK** (opened 2026-05-31). Not a graduation experiment. Companion to [phase-3-consolidation-write-design.md]. This file is the self-contained spec; build is gated on the OPEN DECISIONS + the cheapest-kill gate below. Grounding: the JEPA-transfer + axiom + citation-repair pass (domain-expert, 2026-05-31) and a 5-lens plan-and-grill (0 fatals).

## 0. TL;DR and the honest prior
A batch-offline InfoNCE representation-shaping pressure uses an FHRR-native within-scene unbind-then-rebind self-target to write low-rank role-discriminative structure into the FIXED Phase-3 VALUE codebook (filler atoms; roles are fixed position vectors, encoding.py:18-26), so the graduated heteroassociative memory H (Reports 055/056; hetero_write.py:138-174 + decorrelator.py:44-63) reads from a more role-separable codebook. A COMPOSE, never a replacement write. THE HONEST PRIOR: PAM is itself memory-not-learner (held-out CBR@20≈0.000, primary-read, uncarded); AAR + 2509.01987 + Report 056 (H real-text held-out ≈ chance) all agree. The analytically predicted Stage-1 outcome on real text is the held-out-roles NULL — a memory. Built to DETECT that, not assume it away. A held-out-roles PASS is the ONLY way this earns its keep over plain H.

## 1. Scope, naming, within-scene commitment
- NAMING (grill fix): NOT "Γ1". path-gamma-mechanism-family-survey.md:32,44-52 binds Γ1 to "context-residual consolidation" — an ASYMMETRIC ADDITIVE atom-space residual update, explicitly distinguished (line 52) from the contrastive pull/push this mechanism IS. Renamed "WS-InfoNCE" (or a new Γ6 entry). Report 114 hyp-(b) carried as a non-binding prior; 114's controls were gauge-vacuous so its closure is interpretation-only; use gauge-safe role-pairing-derangement controls (natural_source_protocol.py:93,189), NEVER 114's shuffled-token.
- SCOPE: secondary/exploratory generalization track; memorization (055/056) is the FLOOR, not relitigated.
- WITHIN-SCENE/INWARD (binding): cue = rest of CURRENT scene with one position masked; self-target t* = true masked filler of THIS scene only. Across-scene target = BANNED Phase-6 E_world, excluded by construction.

## 2. Mechanism — FHRR-native unbind-then-rebind self-target
S_rest = Σ_{p≠m} bind(positions[p], codebook[tok_p]); slot_query = unbind(S_rest, positions[m]) = S_rest·conj(positions[m]) (FREE, EXACT, torch_fhrr.py:79-80); v_hat = g_phi(slot_query) (Stage-1: identity); y_target = codebook[t*] (FIXED fn of data); loss = InfoNCE(v_hat, y_target; precommitted negs, tau). Rebind closure: bind(codebook[t*], positions[m]); unbound-frame preferred (MHN cleanup = direct value-codebook lookup, the bundle-first path 066 validated tix=3072, NOT the killed key-only read 065/066 tix=0). PORTS WHERE DORRELL DIED: only inversion is the FREE conjugate; g_phi denoises 1/√D crosstalk, never inverts the bind. Write = InfoNCE pull/push on value codebook; sims.argmax thermostat (error_driven_learner.py:106 / reconstruction_learner.py:106) DELETED. Negatives precommitted (precommit_swap_negatives, hetero_write.py:77-94).

## 2.5 LEARNABLE-THING DECISION (staged): CODEBOOK primary, predictor subordinate
- STAGE 1 (DEFAULT, only thing built before a basin): learnable = VALUE-codebook atom embeddings via InfoNCE, g_phi=IDENTITY, no predictor MLP. EMA/stop-grad UNNECESSARY (fixed codebook IS the target). ANALYTIC CAVEAT (grill attack 2): with g_phi=identity, slot_query = cb[t*]+crosstalk → InfoNCE writes (filler,co-filler) co-occurrence = a memory by construction; held-out NULL on real text is PREDICTED, not surprise.
- STAGE 2 (ABLATION, only if Stage-1 basin + generalization pursued): learned g_phi MLP; if codebook+g_phi both learnable, stop-grad+EMA reinstated — AH-PASS only because target INDEX stays fixed t*. g_phi DISCARDED after offline pass.
- NEVER: g_phi as runtime error-driven updater (BANNED STATUS:43) or runtime exemplar-miner.

## 3. Composes with H — sequential offline, no arbitration
3 stages over SAME frozen buffer: (1) WS-InfoNCE → C′; (2) H + decorrelator UNCHANGED, reading value_codebook=C′; (3) recall_top_index read. No supervisor; data-shaping handoff. COUPLING IS NOT ORTHOGONAL (grill attack 4): value codebook is the delta-rule REGRESSION TARGET (hetero_write.py:162,169-171) AND feeds the cue covariance the decorrelator fits — 3 coupled objects. "H unaffected if WS-InfoNCE inert" is FALSE (Report 114 negative-Δ precedent). HARD FLOOR GUARD: write+L2 on C′ ≥ Report-056 ceiling at EVERY cue richness, as precondition before any margin read; in-sample regression = immediate ABORT.

## 4. Headline + controls + held-out-roles + falsifiers (fence down)
- HEADLINE: value-codebook top_index_hits Selectivity-Δ, TWO-FLOOR Wilson (true-role Wilson-lower > chance=1/N toy or 1/|codebook| corpus; Δ-CI>0; sub-chance shuffled arm = anti-overlap REJECTED). LOAD-BEARING = composition-margin Δ(C′)−Δ(raw C), Newcombe CI. WHERE READ (grill attack 3): in-sample is the FLOOR GUARD only (H tracks Bayes ceiling, no headroom); margin go/no-go = held-out-roles REAL TEXT (H≈chance, headroom exists); topic toy = sanity arm only (store-as-is already generalizes, null uninformative, NOT a kill).
- CONTROLS (same test set, spec:118-137 + coverage-matched): random-codebook (leak detector), no-negatives, role-pairing-derangement, content-matched non-positional shuffle, perfect-cue upper bound (re-aimed §5), lower-D/N (G-C), + coverage-matched store-as-is (Dorrell FATAL-3).
- MANDATORY HELD-OUT-ROLES: partition (role,filler) COMBINATIONS; fit on train, test on held-out combinations never co-occurring; read Δ-vs-frac_seen; substrate = topic-toy + REAL TEXT, NOT the literal 4-role rule-toy (§G-D Forks 1/2 retired it). Learner = flat Δ as frac_seen→0; memory = Δ decays to chance. In-sample PASS + held-out NULL = MEMORY (sanctioned), not failure.
- 3 FALSIFIERS: F-REPRO (cheapest-kill multi-term fails → port DEAD); F-KILL (held-out ≈ chance at low frac_seen → memory-not-learner, kills generalization claim); F-COMPOSE (no held-out margin lift over Report-056 → inert on top of H). Supporting: F-NEG, F-RANDOM, F-CONTENT, F-COVERAGE, F-ASYMMETRY.

## 5. CHEAPEST-KILL FIRST (RE-AIMED, grill attack 1) — **DISCHARGED by Report 067, no new run needed**
Candidate's single-term perfect-cue limit gated NOTHING (trivially re-confirms "MHN cleans an exact atom", proven 3×). RE-AIMED: run on MULTI-TERM scenes (obs≥2), report tix at realistic crosstalk, sweep N to the Plate break-point. PASS = matches Report 065/066 bundle-first curve at matched (D,N,k). FAIL below curve = port DEAD; FAIL tracking Plate curve = capacity wall (separate via lower-D/N arm).

> **DISCHARGED 2026-05-31 (grep-before-build, CLAUDE.md):** [Report 067](../../reports/067_mqar_multirole_bundle_first.md) (multi-role bundle-first, 27,648 trials @ D=4096, K_roles∈{2,4,8}, N∈{16..512}) **already PASSES this curve**: top-1 **>88% up to N=256**, and F-2 shows *"cleanup is essentially deterministic given correct scene-MHN identification; the bottleneck is scene-MHN identification, NOT content cleanup or K_roles."* So the readout machinery the self-target relies on is **de-risked on existing evidence — the port survives Tier-1 without a new run.** Read literally as §2's direct-cleanup on the masked-EXCLUDED bundle, the gate is **chance-by-construction untrained** (the masked filler is absent), so it cannot serve as a *pre-InfoNCE* kill — InfoNCE is precisely what would make it non-chance. **LOAD-BEARING CAVEAT for F-COMPOSE:** because 067's bottleneck is *scene-identification*, not cleanup, **shaping the value (cleanup) codebook may not move the bottleneck.** F-COMPOSE must therefore measure the composition-margin in the **sparse-cue regime where store-as-is fails** (Report 056's held-out-≈-chance regime), NOT where scene-ID dominates — else a null is uninformative. The genuinely un-de-risked gates are **F-COMPOSE** and **F-KILL**, both requiring the Stage-1 InfoNCE build.

DON'T-SCALE-A-NULL: no wikitext until Stage-1 held-out-roles real-text composition-margin > 0.

## 6. Anti-homunculus — PASS by construction
Self-target fixed fn of data (opposite of deleted sims.argmax:106); negatives precommitted; objective Stage-1 InfoNCE over fixed atoms (EMA unnecessary); write batch-offline over frozen buffer (freeze() raises, hetero_write.py:113-114,156-159); composition = sequential offline statistics, no arbitration; read terminates in argmax basin count (hetero_write.py:65). Residual drift risks (binding preconditions): Stage-2 EMA must assert target index == precommitted t*; learned g_phi gated behind random-codebook falsifier; Stage-1 g_phi=identity is the only built default.

## 7. Fence bright-line — verified
Read terminates in top_index_hits count via recall_top_index→batched_hopfield_topindex. MUST NOT touch _energy_from_scores/mhn_energy/cue_conditioned_scene_energy/delta_e_content_minus_role/raw_scene_energy_v0 (all verified off-path). Shared _scores matmul permitted as measurement. Internal convergence energy (hetero_write.py:54) never escapes. INFONCE BRIGHT-LINE (grill fence fix): the softmax partition is a training loss, computes its own log-softmax over _scores, must NOT call _energy_from_scores; tau ≠ read-time beta.

## 8. Repaired citation base (HARD RULE 3)
(a) DROP 2501.14174 (Dreamweaver, generative/no-EMA/NOT-JEPA, absent from manifest verified count 0); title/authors flagged agent-knowledge/uncarded. (b) DEMOTE 2502.05164 to MHN/energy cluster (In-context denoising one-layer transformer = Dense-AM, NOT JEPA). (c) tPC 2305.11982 asymmetry NOT imported (temporal-order-only, contradicts symmetric MHN → F-ASYMMETRY); import whitening intuition only (decorrelator already implements it). (d) pam-2026 sole anchor, MECHANISM SHAPE ONLY, "writes a basin" = analogy; PAM is memory-not-learner. (e) Metadata fixes: 2402.10814 true title "Associative Memories in the Feature Space" (Salvatori et al.) but link_only/no-PDF → card-from-primary BEFORE overwriting; 2502.05164 + 2305.11982 manifest titles/authors synced; 2509.01987 + 2604.20850 NOT re-opened (HARD RULE 5). AFTER REPAIR: predictive/JEPA cluster rests on pam-2026 ALONE (memory-not-learner) → ZERO paper-backed generalization evidence; track justified by substrate-faithful argument only; held-out-roles PASS is the sole possible source of a generalization claim.

## 9. LOC/integration
Cheapest-kill ~30; slot_query builder ~15; Stage-1 InfoNCE ~60; H+decorrelator+read UNCHANGED (0); Selectivity-Δ+Wilson+Newcombe ~40; held-out-roles harness ~50; Stage-2 g_phi+EMA (gated) ~80. No edits to hetero_write.py/decorrelator.py; downstream is existing graduated path with value_codebook=C′.

## 10. Experiment preamble (FIRST run = cheapest-kill) — DRILL-DOWN label
Active phase Phase 3 secondary/exploratory generalization track. Headline per phase-3-consolidation-write-design.md:55-64; load-bearing = composition-margin Δ(C′)−Δ(raw C) Newcombe CI on held-out-roles real-text (:97-99,:112-116). Controls per :118-137 + coverage-matched + held-out-roles. Last verified: Report 056 (memory-not-learner), Report 066 (bundle-first tix=3072). DRILL-DOWN not graduation until secondary-track re-point signed (:70-71; STATUS signs memorization track only).

## 11. Done-gates
Headline with Newcombe+Wilson CIs on held-out-roles real-text; all controls + held-out-roles on same test set, in-sample floor guard passes BEFORE margin read; drill-downs explain headline; report labeled DRILL-DOWN; STATUS.md + checklist updated SAME session walk-back-first, terminal outcome recorded as resolved finding not open blocker.

## 12. De-risked vs speculative
DE-RISKED: read path + AH/fence (cleared graduated path 055/056/058); FHRR unbind free/exact (Dorrell-killing inversion never enters predictor); bundle-first cleanup recovers stored fillers at ceiling (066 D-3). SPECULATIVE: multi-term within-scene residual recoverable near Plate bound (F-REPRO); InfoNCE adds over binding selectivity (F-COMPOSE); no floor regression (§3.2 coupling, 114 precedent); generalizes held-out (F-KILL, ZERO paper backing). DEFAULT OUTCOME = a THIRD MEMORY (in-sample pass, held-out null); built to detect via held-out-Δ-vs-frac_seen + bag-ablation, not assume away. Memory = sanctioned result, learner = the surprise.