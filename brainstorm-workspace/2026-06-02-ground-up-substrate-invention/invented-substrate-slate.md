# Invented energy-based substrate slate (WF4, 2026-06-02)

Verbatim distillation of the ground-up invention workflow (`wm1sug62u`, 36 agents).
Walls = full null record; biology = drift-guardrail only; literal new substrate; **the brain isn't flat** baked in.
Light filter: kill ONLY the inadmissible or already-nulled; keep speculative-but-new. **10 survivors / 1 kill.**

> These are BETS, not results. Zero have been run. Every headline metric is forced onto a CLASS-level differential (B-KILL is verified hostile to a correct paradigmatic class).

## Forge summary

WHAT KIND OF SUBSTRATE THE SLATE CONVERGES TOWARD. Every survivor obeys the same negative-space shape forced by the wall-set: per-occurrence / instance-resolved codes (NEVER the type-averaged centroid verified at experiments/61:254 C += P.t()@P and at the substrate's only superposition primitive bundle()=normalize(sum), torch_fhrr.py:99-101), sparse + nonnegative / amplitude-bearing activity (escaping the 1/sqrt(D) unit-modulus-phase-torus Family-2 floor by construction), separation INTRINSIC to one Lyapunov (keep-together and keep-apart are the same settle — no bolted-on O(V^2) repulsion), a GENERATIVE energy (minima are syntheses, not a retrieve-only convex blend of stored items), and the load-bearing partition/factorization reached by LOCAL settling + grown capacity rather than the banned global SVD/k-means/backprop route. Within that envelope the slate splits into three families: (A) RELATION-AS-PRIMITIVE (N2, N1) — reconceive the stored OBJECT as a transform/tangent-operator so paradigmatic class membership is a DIRECT energy property, not a subdominant mode to excavate; (B) PHASE/OSCILLATOR (N4, N5, N3) — represent memory as a phase-configuration/resonance so occurrences are held distinctly in time; (C) GENUINE HIERARCHY (N8, N11) — multi-timescale levels each holding a residual by joint settling/tiling, NOT a deflation-peel — plus the structural-plasticity specialist N10 (atom-splitting as a fission bifurcation) and the cleanest-E-I-balance per-occurrence writer N6. THE MOST FERTILE INVENTION-SEED was 'make the RELATION the learned, grown, energy-bearing primitive instead of the items' — it produced the top two ranked candidates (N2 TENSOR-BOND and N1 RAFT), is the only seed aimed at the actual target thesis (structural/analogical retrieval = graduation behavior 2) rather than the king/queen type-cosine micro-probe the brief diagnoses as a likely over-fixated local minimum, and is the furthest from every banked wall because nothing in the 121-132 arc ever stored a transform. THE HONEST STATE: these are BETS, not results — zero have been run. The ranking encodes a clear risk gradient verified against the codebase: the relation-as-primitive family (N2/N1) is furthest from any banked null; the phase family (N4/N5/N3) sits under the explicit Report-130 shadow (the single-projection bound is channel-invariant INCLUDING the phase channel; toy-positive-collapses-on-real is their most likely death); the hierarchy family (N8/N11) hangs on the hardest-to-adjudicate admissibility line (the Millidge-2020 PC-equals-backprop trap that already KILLED the LPR sibling); and N6/N10 carry verified reduces-to-a-closed-family risks (N6's write rule is the already-banked-as-inverse-frequency Report-051/128 decorrelator; N10's detector and replay-priority are the already-built C.2.2 and C.2.4 mechanisms). Every candidate's headline metric was checked to use a CLASS-LEVEL differential control rather than the verified-hostile bare-pair-specificity B-KILL arbiter. The discipline that caught >=5 false positives in the prior arc (planted-toy-collapse, low-dim cosine inflation, gauge-vacuous controls, single-seed false-PASS) must gate every oracle. ALL 10 SURVIVED admissibility; the one entry that violated an invariant (LPR, downward-W^T-error = backprop in disguise) was correctly killed.

## Recommended first oracle

Run N2's (TENSOR-BOND) planted-FIRST relational-operator oracle — the single highest-information, cheapest decisive test on the slate. Concretely (~100-150 lines numpy/torch, ~1-2 GPU-hours, no FHRR/Hopfield/codebook): (1) GENERATE a synthetic relational corpus with KNOWN ground-truth relations: emit (agent, verb, patient) tuples where a planted paradigmatic class agent∈{king,queen} NEVER co-occur but always fill the same role-slot under the same relations, plus planted DISTRACTOR co-occurrences (collocational noise that conflicts with the role structure — the subdominant-mode wall, planted recoverable). (2) Initialize a small bank of K random rank-r nonneg bilinear relation-operators {R_k} and per-occurrence sparse head/tail sub-population codes; for each occurrence run the local relational-Hebbian/delta-rule update ΔR_k = η·g_k·(h·t^T − R_k·h·h^T) (the anti-Hebbian second term is the collapse-preventer) with soft operator-assignment g_k = softmax over which R_k best fits the pair, over a few replay passes. (3) HEADLINE = CLASS-LEVEL differential (NOT pair-specific B-KILL): spectrally cluster the learned {R_k} and test whether distinct learned operators align with distinct ground-truth relations AND whether the planted king/queen class shares an operator-membership ABOVE a within-relation label-shuffle control, multi-seed. (4) HARD PRE-COMMITTED KILL: compute effective-number-of-operators (participation ratio of operator usage); if it collapses to ≈1 (all operators degenerate to the identity/dominant mode) on the PLANTED-RECOVERABLE toy, TENSOR-BOND certainly fails on real text and is dead before any parser/real-WikiText run. WHY THIS ONE FIRST: it tests the slate's most fertile and most novel seed (the stored object is a RELATION/transform, not an item) at its single load-bearing claim (locally-learned operators do not collapse and DO separate a planted paradigmatic class), it uses the verification discipline the brief demands (class-level differential, multi-seed, planted-first-then-real, pre-committed collapse kill), and a PASS is the strongest possible signal that the relation-as-primitive family (which also underwrites the #2 candidate N1/RAFT) is worth a real build — while a FAIL cheaply kills the top two ranked candidates at once. It deliberately AVOIDS the king/queen type-cosine micro-probe on real co-occurrence as the first test (the Report-130 toy-positive-collapse trap), measuring structural-operator recovery on a planted-recoverable substrate instead.

## Survivors (full records)

### RAFT — Relational Atlas of Free Tangents (a curvature/tangent-operator energy whose minima are roles, judged by tangent-match not content-proximity)

- **One-line:** Store each occurrence as a (point, local-LINEAR-MAP) pair on a manifold; the energy is minimized when a query's local relational transform MATCHES a stored tangent operator, so king→queen and man→woman land in the same basin because they share the same gender-swap TANGENT, even though the points never co-occur; the generative readout is a closed-form-inverse synthesis that can produce a point never stored.
- **Energy function:** State z∈R^D (current content point) + sparse nonneg activation a∈R_{>=0}^M over M learned 'tangent cells', each cell m owning a local anchor c_m∈R^D and a LOW-RANK linear operator T_m=I+U_mV_m^T (rank r<<D). E(z,a)=Σ_m a_m·||T_m(z−c_m)−(z'−c_m)||^2 + λ_sp Σ_m φ(a_m) + λ_lat a^T(W_lat⊖I)a, where (z,z') is the per-occurrence cue/target pair (NEVER averaged into a centroid). First term is small exactly when cell m's tangent map carries the cue's local displacement onto the target's — i.e. when the RELATION matches, not the content. φ(a)=a−log(1+a) is a concave log-barrier sparsity floor; W_lat is a learned lateral-inhibition matrix so keep-together (cells sharing a tangent co-activate) and keep-apart (conflicting tangents inhibit) are the SAME quadratic form. Settling first relaxes a (which tangents explain this displacement), then z toward the basin those tangents predict; readout is the GENERATIVE completion z*=c_{m*}+T_{m*}^{-1}(z'−c_{m*}) via the closed-form Woodbury inverse of I+UV^T — a synthesized point.
- **Representational unit:** A per-occurrence relational triple (c_m anchor, T_m=I+U_mV_m^T low-rank tangent operator, a_m sparse nonneg activation). NOT a type-averaged centroid — each occurrence contributes its own displacement (z−c_m) to whichever tangent cells already represent that direction, or recruits a new cell. A word's concept is distributed across the tangent cells its occurrences traverse; the relational ROLE (gender-of, plural-of) is the first-class stored object.
- **Local dynamics:** Two-timescale gradient flow on E, both pure descent. (1) a-dynamics (fast inhibitory pool): da_m/dt=−[||T_m(z−c_m)−(z'−c_m)||^2 + λ_sp φ'(a_m) + λ_lat Σ_n(W_lat⊖I)_{mn}a_n], rectified at 0 — a recurrent nonnegative competition settling to a sparse set of winning tangents; each term is local to cell m (its own residual, its own activation, lateral input via W_lat). (2) z-dynamics (slower): dz/dt=−Σ_m a_m T_m^T(T_m(z−c_m)−(z'−c_m)) — pulls z to the point the active tangents jointly predict. Output is the fixed point z* (generative). No module picks the winning tangent; the a-pool's lateral inhibition settles to it as energy descends.
- **Learning rule:** Local three-factor, offline-replay-gated. For an active cell (a_m>0) on a replayed occurrence: ΔU_m=η a_m e_m s_m^T with e_m=(z'−c_m)−T_m(z−c_m) (THIS cell's OWN postsynaptic prediction error) and s_m=V_m^T(z−c_m) (presynaptic drive into the cell's own bottleneck); ΔV_m symmetric; anchor drift Δc_m=η_c a_m(z−c_m) (slow, sparse). Hebbian-with-local-error: every factor computable at the cell from its own pre/post/weight + a broadcast replay/ACh gate. NO global error, NO transpose-of-forward-weights downward path (e_m is the cell's OWN low-rank residual, not a backprop signal threaded through other cells). Structural plasticity: if max_m a_m<θ_recruit on a replayed occurrence (fixed exogenous threshold, not a runtime outcome-reader), seed a new cell at c=z, T=identity (novelty-driven neurogenesis).
- **Gap it fills:** 
- **Why not a closed family:** Not single-projection (Family-1): not a fixed linear operator iterated to its dominant eigenvector — a settling over a POPULATION of low-rank operators with lateral competition, generative output via inverse-map synthesis. Not nonlinear-partition=k-means (Report 127): tangents are overlapping continuous relational maps with lateral structure, not a hard one-hot cluster; the headline is shared-RELATION not co-membership. Not type-averaging (the root wall): displacements per-occurrence, never summed. Not frozen-random-slot (Report 129): T_m and c_m are LEARNED locally; slot-assignment is the settling a-pool, not a fixed random projection. Not inverse-frequency (Report 128): no global count reweight. Not depth-deflation-peel (Report 132): single level, no peel.
- **Anti-homunculus check:** Who picks the firing tangent? Nobody — the a-pool settles by gradient descent on a^T(W_lat⊖I)a + sparsity; the winner is the energy minimum. Who recruits a cell? A fixed exogenous θ_recruit on whether ANY cell explained the datum (BTSP-plateau class), not a controller inspecting per-item loss. Keep-together vs keep-apart = the single sign-structure of W_lat inside ONE energy term (no opposed hand-tuned forces, no O(V^2) bolted repulsion). Replay/ACh gate is a broadcast scalar. Watched corner: T_m^{-1} in the readout must be the closed-form Woodbury inverse (cheap at rank r), NOT an inner optimization — otherwise a solver becomes a hidden controller.
- **Brain-principle grounding:** Energy/attractor settling (explicit Lyapunov, settling=computation); locality (three-factor cell-local error + own bottleneck drive); pervasive inhibition (W_lat lateral competition = keep-together AND keep-apart in one term); structural plasticity (novelty-gated tangent-cell neurogenesis); neuromodulatory gain (broadcast replay/ACh scalar); generative completion (closed-form-inverse synthesis). Drift self-check: target is the cell's own displacement residual (cue-derived), no downward generative-weight chain, no global error.
- **Honest risk:** Most likely failure: tangent cells trained by local Hebbian error on per-occurrence displacements converge to COLLOCATIONAL direction-fields (toward the syntactic-neighbor centroid) rather than paradigmatic ones — the same dominant-mode capture that killed the single-projection family, now inside each low-rank T_m. The bottleneck V_m^T(z−c_m) is itself a projection; if every cell's V_m aligns to the dominant displacement direction, RAFT degenerates into a mixture-of-local-projections still dominant-mode-bound. W_lat's lateral inhibition is the only thing forcing cells to specialize to DIFFERENT (subdominant) directions; too weak → redundant dominant-direction cells (atom-collapse in a new coordinate system).
- **Cheapest oracle:** Substrate-free, ~50 lines, no FHRR. Take per-occurrence GloVe-style context vectors for a curated analogy set (king/queen/man/woman + 20 distractor pairs), form per-occurrence displacements d=z'−z, run JUST the a-pool competition: seed K random rank-1 tangent cells, settle a by the lateral-inhibition dynamics, apply the local ΔU/ΔV update over a few replay passes, then test whether the SAME cell wins for king→queen and man→woman (class-level differential: shuffled-pair displacements must NOT share a cell). If non-co-occurring same-relation pairs do not converge to a shared tangent cell above a shuffled-displacement control, RAFT is dead before any integration (~1 GPU-hour). Isolates the one load-bearing claim (shared-tangent-by-energy).
- **Novelty:** The stored object is a RELATION (a low-rank tangent operator), not a point or a centroid; the energy measures relational MATCH not content proximity, so paradigmatic class membership is a DIRECT consequence of the energy's form rather than a subdominant mode to be excavated — and the readout is a closed-form-INVERSE tangent synthesis (z*=c+T^{-1}(...)) no power-iteration produces.

### TENSOR-BOND — relation-as-primitive substrate (factored per-occurrence head-rel-tail units + a learned bilinear relation-operator bank; analogy is the native fixed point)

- **One-line:** Make THE RELATION the learned, grown, energy-bearing primitive — not the items: each occurrence is a factored (head, relation, tail) micro-binding held with relation, head, tail in separate addressable sub-populations; the energy is a structural-consistency Lyapunov whose minima are cliques of structurally-analogous experiences, so 'X did Y to ?' completes by surfacing same-SHAPE different-CONTENT stored experiences (and can synthesize a tail never stored — generative), and bind-vs-bundle is discovered from the VARIANCE of the local satisfaction signal.
- **Energy function:** State = sparse nonneg activity a over a population of binding-cells, each cell c holding a per-occurrence micro-triple (h_c, r_c, t_c) as THREE distinct sparse sub-populations (relation addressable independently of content; per-occurrence, never averaged), PLUS a small bank of learned RELATION-OPERATORS {R_k} (sparse nonneg bilinear forms / transforms, grown by neurogenesis) and per-pair bond-activities. Two equivalent energy variants both kept: (a) LEARNED-OPERATOR form E=−Σ_edges sat(e) + α·budget-tension + β·Cmplx(state), sat(e)=h_e^T R_{r(e)} t_e (high when head/tail fit the relation's learned geometry), budget-tension=Σ_node(Σ_edges-at-node − budget)^2 (bounded participation → over-budget nodes push their edges apart, metabolic compression = INTRINSIC separation), Cmplx = a description-length/operator-reuse term (Dorrell geometry-not-λ modularity selection); (b) STATIC-KERNEL degenerate form E(a)=−½Σ_{c,c'}a_c a_{c'}K(c,c') + λΣ_c φ(a_c) + μ(Σa−A)^2 with a structural kernel K large when two cells share the SAME relation and a role-consistent content-swap but differ in content — the fixed-operator special case. Minima are CLIQUES of mutually structure-consistent micro-triples = a completed relational pattern; settling synthesizes the unclamped slot (generative). Lyapunov via symmetric construction + rectified coordinate descent.
- **Representational unit:** A per-occurrence factored relational EDGE / binding-cell: (sparse head code, relation-operator id, sparse tail code) held as a distinct object in a growing store — NEVER summed into a scene/window vector (the explicit break from bundle()=normalize(sum); binding is structural/factored, separately addressable, exact unbind, occurrences never collide). An entity 'type' is its participation pattern across edges (a node in the lattice), not a centroid; a 'role' is an EMERGENT relation-operator that recurs across scenes.
- **Local dynamics:** Edge/cell-activation relaxation: act_e ← ReLU(sat(e) − 2α·Σ_{nodes∈e}(over-budget) − β·reuse-cost), or a_c ← [a_c + η(Σ_{c'}K(c,c')a_{c'} − λ(1+log a_c) − 2μ(Σa−A))]_+ — an edge/cell stays active iff well-satisfied AND its nodes are under budget (local competition for participation; the μ term is ONE recurrent inhibitory scalar every cell reads = lateral inhibition as a settling variable, not all-pairs repulsion). Entity-code relaxation (generative): h ← Σ_{edges with h as head} act_e R_{r(e)} t_e − inhib(h) — each entity SYNTHESIZED by the relations it must satisfy, not looked up. Operator-assignment is a softmax over which R_k best fits a pair (competition living in a (Σ_k g_k −1)^2 energy term). All terms read only an edge's own nodes/operator + one shared scalar.
- **Learning rule:** Local relational-Hebbian (delta-rule analogue) on operators + codes: ΔR_k=η g_k (h t^T − R_k h h^T) — make R_k map the head closer to the tail, MINUS the operator's own prediction (anti-Hebbian, prevents collapse to identity/rank-1); entity-code Δh=η(observed neighbours − predicted neighbours) (self-prediction subtraction = local decorrelating inhibition). Fully local (pre h, post t, own-output R_k h, soft assignment g). OPERATOR NEUROGENESIS: an edge whose satisfaction under ALL existing operators is below a fixed floor while its items recur (a slow local eligibility trace per candidate displacement) seeds a new operator at the residual transform — a fixed stochastic rule, not a global scan. BIND-VS-BUNDLE FALLS OUT: a pair with a consistent recurring displacement sharpens an operator (BOUND, energy drops); random co-occurrence never finds a stable operator (BUNDLED, no drop) — discovered from the satisfaction VARIANCE, never declared.
- **Gap it fills:** 
- **Why not a closed family:** NOT single-projection over a flat code (readout is bilinear h^T R t with a LEARNED nonneg operator bank; structure lives in WHICH operator fits, not in a projection of a flat code). NOT k-means partition (operators are transforms applied to items, not centroids; same item participates in many bonds, graded multi-membership). NOT retrieve-only (minima can be NOVEL triples = generative, the explicit break from the Hopfield convex-blend wall). NOT type-averaging / not bundle-by-sum (edges are factored and separately addressable — the precise primitive the substrate lacked). NOT bolted-on repulsion (separation = the budget tensegrity term). NOT backprop (operators learned by local outer-product Hebbian + self-prediction subtraction, no downward error). NOT inverse-frequency (the signal is transform-consistency across occurrences, orthogonal to marginal counts — the oracle must confirm operators are not secretly frequency-sorted).
- **Anti-homunculus check:** Who decides a pair is bound vs bundled? Nobody — operator-confidence is the integral of a local satisfaction-variance dynamic; high-confidence operators emerge and low ones dissolve by the SAME learning rule (no if-stable-then-bind branch). Who decides the analogy? The settling basin under the shared-operator energy. Who allocates a new operator? A fixed local novelty rule (satisfaction below floor + recurrence eligibility, neurogenesis-class), not a metric-reading allocator. The relation/role decomposition (three sub-populations, fixed G-cap, fixed softmax temperature) is a FIXED problem-generic scaffold (evolution-style, §4-legal), NOT hand-shaped to king/queen. Separation = the budget tensegrity term, intrinsic to E.
- **Brain-principle grounding:** Energy/generative settling (minima are syntheses, not a stored bank — the project's first generative vs retrieve-only energy); pervasive inhibition (budget term = local competition, keep-together=keep-apart); metabolic constraints (Dorrell geometry-not-λ: the description-length/reuse term selects modularity structurally); prediction-without-backprop (operator delta-rule subtracts the operator's OWN prediction, target = a same-scene item); structural plasticity (operator neurogenesis); sparse+distributed nonneg codes. The relation-operator-as-recurring-displacement grounds bind-vs-bundle in 'a stable relative role'.
- **Honest risk:** Most likely failure: it smuggles in MORE structural prior than the data supports — assuming experience arrives as typed (head,rel,tail) triples is a design-time homunculus (real WikiText windows are bags, not triples). If edges come from a fixed parser, the parser does the structural work; if edges are DISCOVERED, edge-discovery may BE the unsolved partition problem in disguise — AND defining the structural kernel K / 'role-consistent content-swap' WITHOUT a global alignment may be impossible locally (the exact trap that killed every prior global route). Second: bilinear operators R_k learned by local Hebbian may collapse to a single dominant operator (the collocational mode as 'one big relation'); generative completions may be incoherent blends (the Hopfield convex-blend failure relocated to relation space). Effective-number-of-operators≈1 on real text = collapse = dead.
- **Cheapest oracle:** Substrate-free, planted-FIRST then real, on a SYNTHETIC relational corpus with KNOWN ground-truth relations (generated (agent,verb,patient) tuples + a planted paradigmatic class agent∈{king,queen} that NEVER co-occur + planted distractor co-occurrences): (1) do distinct operators emerge for distinct ground-truth relations (learned-R_k clustering matches planted relations / spectral clustering of K separates the planted class ABOVE a within-relation label-shuffle, class-level differential)? (2) does a held-out partial 'A did R to ?' settle to surface same-R different-content scenes ABOVE a frequency-matched non-R control? KILL CRITERION: if on the planted-recoverable toy the local operator-learning STILL collapses all operators to one (identity/dominant mode), it cannot do relational structure even when it exists → certainly fails on real text; only a pass earns a real windowed-text run (where extracting r locally without a parser is the real test).
- **Novelty:** The genuinely new commitment is making the RELATION (the bond/transform between items) the learned, grown, energy-bearing primitive — not the items. Every prior experiment learned item-codes (codebook atoms) and read pairwise SIMILARITY; this learns and stores TRANSFORMS and reads structural FIT, making 'the structural part does real work' an energy property. The only candidate attacking the structural/analogical thesis (behavior 2) head-on rather than the king/queen micro-probe.

### EMBER — Eligibility-Marked Burst Ensembles with Resonant binding (per-occurrence sparse-spike substrate; binding is a HIGHER-ORDER 3-body phase-resonance, not an elementwise product)

- **One-line:** Replace the dense phase torus and sum-superposition with a SPIKE-TIMING substrate where each occurrence lays down a sparse set of timed event-traces; the energy is a resonance/phase-locking Lyapunov whose distinctive term is a genuine TRIPLE (3-body) coupling, so BIND = a stable 3-phase resonant clique (a distinct energy basin) and BUNDLE = transient pairwise co-firing that does NOT phase-lock — the bind-vs-bundle discrimination the flat sum-substrate structurally cannot make.
- **Energy function:** Per active unit i: phase θ_i∈S^1 (oscillator) + nonneg amplitude r_i>=0 (rate/eligibility). Kuramoto-style resonance Lyapunov made GENERATIVE by a learned coupling tensor: E(θ,r)=−Σ_{i<j}K_ij r_i r_j cos(θ_i−θ_j−ψ_ij) − Σ_{i<j<k}J_ijk r_i r_j r_k cos(θ_i+θ_j−θ_k) + Σ_i[−μ r_i + ½r_i² + β·r_i·(local-inhib pool)]. The pairwise K-term is the associative core (learned phase-locked coupling with a stored phase OFFSET ψ_ij = the RELATIONAL ROLE — 'subject'/'object' are phase relationships, not slots). The DISTINCTIVE TRIPLE J-term makes BINDING first-class: three units phase-lock into a bound triple (agent+relation+patient) only when the J-coupling supports a stable 3-phase resonance — a DIFFERENT energy basin from three pairwise-bundled units. The amplitude terms (−μr+½r²) give graded confidence and, with the local-inhibition pool, force sparsity. Minima are coherent, sparse, phase-structured assemblies; settling = the field synchronizing.
- **Representational unit:** A per-occurrence sparse spike-ensemble: an event activates a small set of units with specific phases and amplitudes (NOT a single dense D-vector, NOT a sum). Two occurrences of the same word are DISTINCT ensembles sharing unit-identity but differing in phase-context — so the discriminative residual lives in phase relationships, never low-pass-averaged. A 'role' is a stored phase-offset ψ_ij; a 'bound structure' is a stable resonant clique sustained by the J-tensor; the amplitude channel restores the rate/graded-confidence the unit-modulus torus threw away.
- **Local dynamics:** Gradient flow on E. Phase: dθ_i/dt=Σ_j K_ij r_i r_j sin(θ_j−θ_i+ψ_ij) + Σ_{j,k}J_ijk r_i r_j r_k sin(...) — each unit adjusts phase toward its locally-coupled neighbours (sees only coupled partners' phases). Amplitude: dr_i/dt=Σ_j K_ij r_j cos(θ_i−θ_j−ψ_ij) + (triple terms) + μ − r_i − β·(inhib pool), rectified at 0 — units whose phase agrees with their assembly GAIN amplitude (resonance reinforces), discordant/weakly-coupled units LOSE amplitude (inhibition + decay) → the sparse winning assembly emerges from the energy, not a thresholder. Theta/gamma multiplexing is native: a single broadcast slow rhythm gates encode-vs-retrieve gain (does not pick winners); gamma sub-cycles packet distinct bound items into distinct phase slots that EMERGE from the J-resonance, not an index handed in by control code.
- **Learning rule:** Spike-timing-dependent three-factor, local. Pairwise: ΔK_ij=η r_i r_j cos(θ_i−θ_j−ψ_ij)·g, and the role-offset learns: Δψ_ij=η_ψ r_i r_j sin(θ_i−θ_j−ψ_ij)·g — products of LOCAL pre/post amplitudes and the LOCAL phase difference, gated by a broadcast eligibility/dopamine scalar g (phase-resolved Hebbian = local STDP: fire-together-in-phase wire-together). Triple: ΔJ_ijk=η_J r_i r_j r_k cos(θ_i+θ_j−θ_k)·g — local to the three units (a dendritic coincidence detector / nonlinear dendritic branch). NO backprop, NO downward generative weights, NO global error: every update is a function of the participating units' own phases/amplitudes + a diffuse gate. Consolidation: offline, the slow rhythm replays high-eligibility ensembles; a multi-timescale K (fast K^f decays, slow K^s accumulates from K^f, Benna-Fusi cascade) transfers stable resonances to long-term coupling.
- **Gap it fills:** 
- **Why not a closed family:** Not single-projection-in-phase (Report 130 was a fixed LINEAR read of the idle phase channel; EMBER is a recurrent MULTIPLICATIVE 2nd+3rd-order resonance with learned phase offsets and an amplitude-competition channel — no fixed operator, no power-iteration). Not type-averaging (per-occurrence sparse ensembles, never summed). Not sum-superposition (no bundle() at all; superposition is phase-multiplexed coexistence that HOLDS occurrences distinctly). Not nonlinear-partition=k-means (overlapping graded resonances, not hard one-hot clusters). Not frozen-random-slot (phases and couplings learned, slots emerge from resonance). Not deflation-stacking (single recurrent field). Not PC=backprop (STDP-local, no downward generative weight, no global error).
- **Anti-homunculus check:** Who wins amplitude? The amplitude gradient + inhibition pool settling — resonance is self-reinforcing, no thresholder. Bind vs bundle? The energy: a query either falls into a J-stabilized clique basin or it doesn't; nothing reads a 'boundness metric' and branches. Phase-slot assignment (gamma packets)? The J-resonance produces distinct stable phase offsets as energy minima — NOT a loop counter assigning slot indices. The slow theta rhythm is one broadcast oscillator setting gain/encode-window (legal neuromodulatory gain). The eligibility gate g is a diffuse scalar. Watched corner: ΔJ_ijk must be a genuine LOCAL dendritic-coincidence rule (the three units' own state), not a global tensor factorization computed offline and relabelled 'learning'.
- **Brain-principle grounding:** Oscillatory multiplexing (theta-gamma binding-by-timing, phase coding); sparse+distributed (few units win amplitude); pervasive inhibition (local-inhib pool doing the sparsification/competition); locality (phase-resolved STDP + dendritic-coincidence triple rule); multi-timescale consolidation (Benna-Fusi K^f→K^s cascade); neuromodulatory gain (broadcast theta + eligibility g). Drift self-check: phase evolves under its OWN dynamics; targets self-derived; no global error.
- **Honest risk:** Most likely failure: the resonance dynamics are CHAOTIC/metastable rather than settling — higher-order Kuramoto systems can have limit cycles and no clean fixed point, so 'settling IS the computation' fails (the energy is a Lyapunov function only under conditions on K,J that real learned couplings may violate). Second: phase coding has notoriously low capacity (1-D phase per unit); the discriminative residual paradigmatic structure needs may not fit at MacBook-class unit counts, recapitulating Report 130's collapse (toy-positive, real-null) because sparse-phase capacity is too small to separate king/queen from dominant collocational partners. Third: the J-tensor is O(M^3) dense if not kept extremely sparse — the metabolic-budget invariant could be violated, making it un-runnable.
- **Cheapest oracle:** Substrate-free bind-vs-bundle test, ~80 lines, no FHRR. Tiny world with KNOWN bound pairs (consistent relative role across contexts) and bundled pairs (co-occur with random relative roles). Encode each occurrence as a sparse phase-ensemble, run ONLY the phase+amplitude settling + the local STDP K/J updates over replay, then test: does the J-resonance basin depth (settled E gap between bound-clique and shuffled-clique) separate true-bound from true-bundled pairs ABOVE a label-shuffle control? If a learned J cannot make bound triples sit in a deeper, more stable basin than bundled triples, the central claim is dead. Directly also checks the chaos risk (does it settle?) — ~1 GPU-hour before any substrate commitment.
- **Novelty:** Binding is a HIGHER-ORDER RESONANCE (a stable 3-phase-locked clique), not an elementwise product (FHRR bind) or a stored slot — so 'is this composed or merely co-occurring?' is an INTRINSIC energy distinction (the abstraction behavior the project most wants and the flat substrate most lacks). The genuine TRIPLE J-coupling making bind-vs-bundle a distinct basin is unique among all candidates. Substrate is spikes/phases/amplitudes with no dense vector and no sum-superposition.

### RESONANT-OFFSET FIELD — signed-Kuramoto per-occurrence oscillator substrate (memory is a phase-configuration, not a point; abstraction is a settled phase-OFFSET motif learned by local phase-plasticity)

- **One-line:** Question the deepest assumption (memory = a POINT in R^D): each occurrence is a population of coupled phase-oscillators (graded amplitude, sparse) carried on a slow theta rhythm; co-active items occupy DISTINCT emergent phases (occurrences held distinctly by timing, never summed); the energy is a SIGNED Kuramoto potential where in-phase coupling (keep-together) and anti-phase coupling / learned phase-offset (keep-apart, the relation) are ONE Lyapunov, and a paradigmatic class is a stable phase-relationship the coupling settles into across many different-content occurrences.
- **Energy function:** Population of N phase-oscillators θ_i∈[0,2π) carried on theta, with graded amplitude r_i>=0 (sparse, most r_i≈0). Generalized Kuramoto potential with SIGNED coupling AND learned offset: E(θ,r)=−Σ_{ij}K⁺_ij r_i r_j cos(θ_i−θ_j) + Σ_{ij}K⁻_ij r_i r_j cos(θ_i−θ_j−π) + Σ_{ij}K_ij r_i r_j cos(θ_i−θ_j−φ_ij) + Σ_i[V(r_i) + λ(Σr−k)₊]. K⁺ pulls oscillators into the SAME phase (binding); K⁻ pushes ANTI-phase (separation — lateral inhibition AS anti-phase coupling); the learned per-pair OFFSET φ_ij stores the RELATION as a phase relationship (subject vs object = a phase offset, not a slot). V(r) is a bistable/metabolic amplitude well; λ(Σr−k)₊ is the hard sparsity budget. The signed Kuramoto potential is a genuine Lyapunov function for symmetric coupling. KEY: occurrences are NOT summed — successive occurrences write at different theta-phases (phase precession / sequential gamma-packets), held distinctly; the discriminative residual is the PHASE-RELATIONSHIP, which sum cannot destroy. GENERATIVE: a phase-offset motif is a minimum instantiable with NEW content units snapped into the same offsets — synthesizing a completion.
- **Representational unit:** A per-occurrence phase-configuration: WHICH units are active (sparse graded amplitude) AND their relative theta-phases. A relation is a stable phase-OFFSET motif (e.g. agent at phase 0, action +2π/3, patient +4π/3) that is content-independent — the structural frame is in phase geometry, not a content vector. Information is in relative timing, so there is no vector to average and no common-mode to keep; a word-type is never a representational object, recovered post-hoc as a recurring phase-locked subset across recall episodes.
- **Local dynamics:** Gradient flow dθ_i/dt=−∂E/∂θ_i=Σ_j K_ij r_j sin(θ_j−θ_i+φ_ij) + signed in/anti-phase terms (each oscillator reads only its coupled neighbours' phase-differences — a local oscillator update), and dr_i/dt=−∂E/∂r_i with the bistable well + budget. Encoding: context units activate (amplitude up) and their phases settle under K — co-bound units phase-lock, competing items repel to different phases (lateral inhibition = anti-phase coupling, settling locally). Completion: clamp a partial phase-configuration, settle, read which units snapped into the motif. Analogical retrieval: present a partial phase-MOTIF (relational shape with some content); settling recruits whichever stored content units best fit the OFFSETS — same-shape-different-content by construction (match metric is phase-offset, not content-cosine). Polysemy split: a unit driven to incompatible phases by two contexts grows K⁻ with both context-sets, cannot phase-lock to both, bifurcates into two phase-distinct usages (emergent split). The phase-clock multiplexes distinct roles to distinct phases — several role-classes coexist in one settled field.
- **Learning rule:** Local phase-dependent plasticity (the brain's STDP/phase-coding analog, grounded in the cosine-kernel form, NOT imported as a named model): ΔK⁺_ij=η m r_i r_j[cos(θ_i−θ_j) − K⁺_ij decay] (coupling grows between units that reliably co-phase, headroom-bounded f_P=1+cosθ), ΔK⁻_ij=η r_i r_j[−cos(θ_i−θ_j)]₊ (anti-phase pairs grow repulsion, f_D=1−cosθ), and the OFFSET relaxes toward the OBSERVED relative phase of co-firing Δφ_ij=η_φ r_i r_j sin(θ_i−θ_j−φ_ij) (a local timing-Hebbian rule). All are f(pre amplitude/phase, post amplitude/phase, weight, neuromod) — strictly local, BTSP-style headroom (1−w) gating. Consolidation: replay re-runs theta cycles offline, stabilizing motifs that recur (slow K⁺ / slow φ) — multi-timescale via fast amplitude / slow coupling. A class forms because many similar-meaning items get written into OVERLAPPING phase-offset relations to shared context assemblies, so they phase-lock to the SAME gamma group at recall even though they never co-fired — paradigmatic structure as shared phase geometry, not shared co-occurrence count.
- **Gap it fills:** 
- **Why not a closed family:** NOT single-projection IF the per-occurrence phase-multiplexing + signed multi-item Kuramoto + learned offsets is load-bearing (the oracle's second kill gate enforces this — if it reduces to dominant-eigenvector-of-one-operator it IS Wall-130 and dies, honestly stated; K_ij/φ are recomputed/learned, not a fixed iterated operator). NOT type-averaging (occurrences at distinct phases, no sum primitive — the most literal escape). NOT k-means/nonlinear-partition (phases emerge from continuous Kuramoto settling, multiple groups co-exist, graded membership, never a hard one-hot partition of type-vectors). NOT frozen-random-slot (K⁺/K⁻/φ learned by local timing rules; phases settle, not assigned). NOT inverse-frequency (coupling reads relative spike PHASE, per-occurrence, frequency enters only via saturating BTSP headroom (1−w), rare-pair-friendly). NOT PC=backprop (no downward generative weights). The honest caveat: closest of all candidates to a banked wall (130); included as the bold-but-admissible bet whose cheapest oracle is built to kill it within an afternoon.
- **Anti-homunculus check:** Who assigns which item goes to which phase? The K⁻ anti-phase coupling SETTLING — emergent phase-separation, NOT a loop counter handing out slot indices (the explicit homunculus the oscillation principle warns against). Who decides a motif is a 'relation'? Nobody — it's a stable phase-offset minimum of E. The theta/gamma rhythm is part of the dynamics (the oscillators' own evolution), not an external schedule; neuromod m is broadcast gain. Keep-together (in-phase) and keep-apart (anti-phase) are ONE Kuramoto energy. The fixed scaffold (N units, gamma-bin count, theta clock, inhibition wiring) is problem-generic structure (evolution-style, legal).
- **Brain-principle grounding:** Oscillatory/rhythmic multiplexing (theta-gamma phase coding = native binding-by-timing, phase evolves under its OWN dynamics not a slot-counter); phase-dependent plasticity grounded in the cosine kernels f_P=1+cosθ / f_D=1−cosθ (primary-literature-grounded, not an imported model); E-I balance as in/anti-phase coupling; sparse nonneg amplitude; energy-settling (Kuramoto Lyapunov); multi-timescale (fast amplitude / slow coupling-prior, deposits a class WITHOUT averaging). Drift self-check: write is f(pre,post,w,m) with phase; no backprop; explicit Lyapunov; phase emerges from dynamics not a slot-index; the Report-130 caution is the headline risk and the oracle's first kill gate.
- **Honest risk:** MOST LIKELY KILL: this is the closest to a banked wall — Report-130's frustrated-phase oscillator collapsed from a toy positive to ~0 on real data because a single-projection dynamic in the phase channel still converges to the dominant collocational mode. If the coupling settling reduces to iterating a single (complex/phase) operator toward its dominant eigenmode, OR if the field has only two generic attractors (global synchrony = collapse, or incoherence), it inherits Wall-130 exactly and dies on real text the same way. The defense (per-occurrence phase-multiplexed + signed multi-item Kuramoto + learned offsets + multistable, not single-projection-of-a-type-code) MUST be the load-bearing difference; if local φ-Hebbian relaxation converges to a single global offset, it IS single projection again. Second: phase-oscillator settling is finicky (limit cycles, frequency-locking failures) and may need hand-tuned coupling ratios that smuggle in a controller. Third: M (active set per cue) may need to be large to contain a king/queen pair that rarely co-occurs.
- **Cheapest oracle:** Substrate-free, DESIGNED to confront the Report-130 wall immediately on REAL data (the toy-positive-collapse is the known trap). N=512 phase-oscillators, per-occurrence encoding of WikiText windows (each window's content words get amplitude; phases settle under signed Kuramoto with locally-learned K⁺/K⁻/φ), then the CLASS-LEVEL differential: do paradigmatic-class words develop a SHARED stable phase-relationship to their shared-context units across occurrences, ABOVE a shuffled-token control AND surviving a within-class label-shuffle (so it's not set-hubness)? KILL GATE 1 (baked in from the start): run on REAL WikiText co-occurrence, not a planted toy — if the signal is toy-only (as in 130) it's dead. KILL GATE 2: check whether the converged phase-config is just the dominant eigenvector of a single complex operator (if so = Wall-130, single-projection-in-phase). Also test for global synchrony collapse (effective-number-of-locked-groups≈1 = the dominant collocational mode in phase clothing). ~200 lines, minutes on CPU.
- **Novelty:** Questions the deepest shared assumption (memory = point in R^D) — representation is a TRAJECTORY/phase-configuration. The relation is a LEARNED PHASE-OFFSET φ_ij and class membership is graded multi-group phase-locking, both reached by a local timing-plasticity rule — precisely the generalizing piece prior work got only via backprop, here gotten locally. Distinct from Report-130's frustrated-phase oscillator, which lived ON the FHRR torus as a single global phase projection that CONVERGED; this REPLACES the substrate (oscillators ARE the units, per-occurrence, signed coupling, learned offsets) and is a multistable segregating field, the opposite of a single converging projection.

### TEPID — Two-timescale Episodic Phase-Interference Drift (verbatim per-occurrence traces + recall-time Kuramoto coupling from cue-complements; abstraction is a transient resonance, never stored)

- **One-line:** Store per-occurrence episodic traces VERBATIM (never averaged); at recall, run a local phase-coupling energy descent in which same-role traces fall into the SAME oscillatory phase and co-resonate — the coupling K_ij is COMPUTED AT RECALL from each trace's cue-uncovered complement (not a learned synapse), so abstraction is the transient phase-cluster the settling builds over the small cue-engaged active set, and a slow consolidation deposits the coupling-prior that crystallizes a class without ever summing the traces.
- **Energy function:** Recall-time state is a complex per-trace amplitude/phase field z_i=a_i·exp(iθ_i) over the M episodic traces currently engaged by the cue (M small, the active set, NOT the whole bank). E(z)=−½Σ_ij K_ij a_i a_j cos(θ_i−θ_j−φ_ij) + λ_a Σ_i (a_i²−a_i)² g_i + (μ/2)(Σ_i a_i² − A*)². Term 1 = generalized Kuramoto/XY coupling: K_ij>=0 is the (local, pairwise, CUE-TIME-COMPUTED) feature-overlap between traces i and j on the part NOT pinned by the cue (the complement/role slot); φ_ij is their stored relative-position phase offset (order carried by data via each trace's own bound position vector, not a counter). Traces sharing role-complement structure (high K_ij) phase-lock; conflicting traces de-phase. Term 2 = a local bistability well per trace (active→1 vs silent→0), gated by g_i=cue-match drive (only cue-relevant traces can switch on). Term 3 = a soft global metabolic budget A* (ONE scalar, broadcast, neuromodulatory-legal). Settling is provably non-increasing in E (XY/Hopfield-Lyapunov). The answer is the phase-cluster the field settles into — a transiently-synthesized ROLE CLASS that was never stored.
- **Representational unit:** A per-occurrence episodic trace stored VERBATIM: each token instance as its own FHRR-style composite t_i=bind(pos_i, content_i)⊕(cue-context binding), tagged with an exogenous slow-clock phase stamp from when it was written. NOTHING is averaged into a type — the bank is a growing list of instances (capacity managed by multi-timescale consolidation, not centroid-collapse). The recall-time unit is the complex field z over the cue-engaged active subset; a 'concept' has no stored vector, existing only as a recurring phase-locked subset across recall episodes.
- **Local dynamics:** Recall: (1) the cue gates on an active set by cue-overlap (drive g_i, local match). (2) Iterate the field: θ_i ← θ_i + η Σ_j K_ij a_j sin(θ_j−θ_i−φ_ij); a_i ← a_i − η ∂E/∂a_i (bistable well + budget projection). K_ij computed ONLY between currently-active traces from their cue-uncovered complements — O(M²) over the small active set, NEVER O(V²) over the vocab, and never a projection of a fixed corpus operator. (3) Read out the dominant phase cluster as continued settling to the amplitude-bistable fixed point, then report the surviving active set (NOT an argmax-with-threshold the controller applies). The role-completion is the bundle of contents of the traces in the largest-amplitude phase-locked group — synthesized at recall, can be a content NEVER co-occurring with the cue (generative). Distinct roles settle to distinct phases (theta-style); several role-classes coexist in one settled field.
- **Learning rule:** WRITE is one-shot episodic, BTSP-style local plateau-gated: on a token instance, Δw=P(1−w)f_P − D w f_D with the plateau f_P an exogenous/stochastic clock event (NOT a metric read) — the trace is laid down with its phase stamp and a local potentiation; no global statistic touches the write. CONSOLIDATION (offline replay): coupled two-timescale variables per trace (fast a, slow s, communicating-vessel coupling ds/dt=(a−s)/τ_slow) so frequently-co-resonating subsets deposit a SLOW shared phase-bias φ that makes future co-phase-locking easier — this is how a class crystallizes WITHOUT averaging the traces: the slowly-learned thing is the COUPLING/φ-prior, the traces stay distinct. Replay priority = which subsets co-resonated (a local resonance-energy signal), not a metric-reading scheduler.
- **Gap it fills:** 
- **Why not a closed family:** Not single-projection (Reports 121-129): the readout is a nonlinear RECURRENT phase-settling whose FIXED-POINT structure is the answer — no fixed linear operator is power-iterated, K_ij is recomputed per recall from the cue-active complements, so there is no dominant eigenvector to converge to. Not nonlinear-partition=k-means (Report 127): the partition is over PER-OCCURRENCE traces in PHASE space by continuous coupling, not a hard one-hot assignment of type-vectors to k centroids — online/transient per cue, never a global Lloyd pass. Not frozen-random-slot (Report 129): no fixed slot basis; phase is a continuous emergent coordinate. Not inverse-frequency (Report 128): coupling is feature-overlap on cue-complements; frequency enters only through the SATURATING BTSP headroom (1−w), rare-pair-friendly. Not type-collapse (the central diagnosis): NOTHING is averaged. NOT Report-130's single-projection phase-oracle (named explicitly in the oracle as the honest-risk to beat).
- **Anti-homunculus check:** WHO decides which traces share a role? The phase-coupling energy's gradient — same-role traces phase-lock because K_ij (local cue-complement overlap) is high, a fixed local dynamic. WHO decides the active set? The cue-match drive g_i (local), not a router. WHO decides the budget winner? The soft budget term μ(Σa²−A*)² is ONE scalar broadcast (neuromodulatory-gain-legal); it scales all traces uniformly, does not inspect outcomes or pick a winner. The readout is reading a fixed point of E, not arbitrating. RISK SPOT flagged: 'read the dominant phase cluster' must be a fixed-point property (continued settling to the amplitude-bistable fixed point, then report the surviving active set), NOT an argmax-with-threshold — so no if-metric-then-act survives.
- **Brain-principle grounding:** Oscillatory multiplexing (theta/gamma phase coding); energy/attractor settling (XY/Kuramoto Lyapunov, settling IS the computation); sparse+distributed per-occurrence (the bistable amplitude well forces a sparse active set, traces stay distinct = no type-collapse); multi-timescale consolidation (fast a / slow s communicating-vessels deposits the slow coupling-prior = a class without averaging); locality (K_ij and the BTSP write are pairwise/local); neuromodulatory budget as the only global scalar.
- **Honest risk:** Most likely failure: the phase-coupling collapses to GLOBAL synchrony (one giant phase cluster = everything is one class = the collocational dominant mode in the phase channel) — exactly the wall Report-130 hit (toy +0.498 collapsed to ~0 on real WikiText). If K_ij is dominated by high-frequency collocational overlap, the Kuramoto fixed point is all-in-phase and no paradigmatic sub-cluster survives; frustration/φ-offset and the budget are supposed to prevent this but may not bite on real co-occurrence. Second: M (active set per cue) may have to be large to contain a king/queen pair at all (they rarely co-occur), reintroducing an O(large) settle and possibly re-needing a global gather to assemble the set.
- **Cheapest oracle:** Substrate-free, no codebook: take ~50 king-context and ~50 queen-context PER-OCCURRENCE windows from WikiText (raw token windows, NO type-averaging). Build K_ij ONLY from each window's cue-complement bag-of-features (the non-king/non-queen words), assign random initial phases, run plain Kuramoto descent dθ_i=Σ_j K_ij sin(θ_j−θ_i) to a fixed point. HEADLINE = CLASS-LEVEL differential control (NOT B-KILL pair-specificity): does the settled phase-partition put king-occurrences and queen-occurrences into the SAME phase band MORE than a shuffled-token control's bands (royalty-class coherence > content-word-class coherence), multi-seed, with the shuffled-token negative control? If real-data Kuramoto collapses to one phase band (Report-130 redux) the substrate is dead for ~$0 of compute before any FHRR/Hopfield is built.
- **Novelty:** The first substrate that holds occurrences DISTINCTLY (verbatim episodic) and computes the coupling AT RECALL from cue-complements rather than learning it as a synapse — so the slot-ASSIGNMENT partition (the load-bearing step per Report 129) is produced as a settled PHASE-PARTITION over per-occurrence traces by a local Kuramoto descent, while the slowly-learned thing is only the coupling-PRIOR. Distinct from the learned-coupling oscillator substrate (N4) precisely in that the coupling is recall-time-computed-from-overlap, not a learned weight field.

### SPARTA — Sparse Per-occurrence Attractor with Recurrent-inhibition (single shared inhibitory SCALAR + surprise-gated anti-Hebbian DECORRELATION-while-writing)

- **One-line:** A sparse nonnegative energy substrate that never averages: each word OCCURRENCE writes its own sparse trace, a single recurrent-inhibition Lyapunov makes pull-together and push-apart the SAME settle, and the WRITE rule actively SUBTRACTS the assembly's own predicted (collocational) co-firing — so only SURPRISING (shared-context, paradigmatic) overlap accumulates, with inhibition implemented as ONE shared scalar (E-I-balance-as-one-interneuron, not a learned lateral matrix).
- **Energy function:** State is a nonneg graded activity vector a∈R_{>=0}^N over N feature units (N grows by neurogenesis; sparse). E(a)=−½ a^T W a + ½λ(1^T a)^2 + Σ_i φ(a_i), where W is the symmetric excitatory recurrent weight (Hebbian-learned, W_ii=0, W>=0), the term ½λ(1^T a)^2 is a GLOBAL-but-CONTENT-FREE inhibitory pool implemented by a single shared inhibitory variable h=λ(1^T a) that every unit subtracts identically (E-I balance, ONE scalar settling alongside a — carries no per-unit content), and φ(a_i)=½a_i²+βa_i (per-unit leak + activation cost giving the nonneg-rectified fixed point). Settling: a_i ← ReLU((Wa)_i − h − β), iterated to a sparse fixed point — a genuine Lyapunov function (symmetric W, convex inhibitory + leak) monotonically decreasing E. GENERATIVE: a partial cue settles to an assembly that is a NOVEL union of feature units never co-written. The single shared scalar makes separation intrinsic — two over-correlated assemblies cannot both be fully active under one shared budget, so the minimum already holds them apart; no second repulsion pass.
- **Representational unit:** A PER-OCCURRENCE sparse nonneg assembly: each token instance instantiates a transient sparse activity pattern a^(t) (k-active of N, k<<N) seeded by its context window's input drive b^(t). The representation of a word is NOT one averaged vector but the EMPIRICAL DISTRIBUTION of the sparse assemblies its occurrences settle into; a 'word type' is read out only as a diagnostic. The substrate stores W (occurrence-to-occurrence learned overlap) and a grown unit population, never a per-type centroid.
- **Local dynamics:** Two nested loops. (1) FAST inference settle (the computation): given context drive b^(t)=sum of context-word feature projections, iterate a_i ← ReLU((Wa)_i + b_i^(t) − h − β) with h=λ Σ_j a_j until the sparse fixed point; the settled assembly IS the occurrence's representation. Each step is local: unit i needs only its incoming W row (its own synapses), the single shared scalar h, and its own drive. (2) STRUCTURAL plasticity: if the settled energy stays ABOVE a fixed threshold (cue unexplained by existing units = high residual drive), a fixed exogenous/stochastic neurogenesis rule recruits a fresh unit capturing the unexplained residual (k-WTA-of-residual onto a new unit) — trigger is residual MAGNITUDE local to settling + a stochastic gate (BTSP-plateau-style), NOT a metric-reading allocator.
- **Learning rule:** A THREE-FACTOR, per-occurrence, DECORRELATING local rule applied AFTER each occurrence settles: ΔW_ij = η m^(t)[a_i a_j − a_i (Wa)_j] for i≠j, where a is the settled sparse assembly, (Wa)_j is unit j's OWN recurrent prediction of its drive (local — already computed during settling), and m^(t) is a SLOW DIFFUSE neuromodulator = surprise = the residual energy at convergence (one scalar broadcast, content-free, sets write gain). The first term is Hebbian co-firing; the SECOND term a_i(Wa)_j is ANTI-HEBBIAN — it subtracts the assembly's own predicted correlation, so synapses between units that ALREADY co-fire for collocational reasons stop strengthening (prediction already explained) while units that co-fire SURPRISINGLY (shared context across non-co-occurring words — the king/queen residual) keep strengthening. This descends E_write=Σ_t||a^(t)−Wa^(t)||² by LOCAL gradient (each synapse sees only pre a_i, post a_j, the post's own prediction (Wa)_j, current W_ij, scalar m). W nonnegative by rectification; W_ii=0 structural.
- **Gap it fills:** 
- **Why not a closed family:** NOT first-order Hebbian (the anti-Hebbian subtraction term changes the fixed point — second-order in W). NOT power-iteration over a fixed operator (W co-evolves with its own predictions, not a FIXED linear map iterated, breaking the eigenvector-ordering argument that makes subdominant modes unreachable). NOT k-means/nonlinear-partition (no hard assignment; soft sparse settling, headline is assembly-OVERLAP not partition co-membership). NOT type-averaging (per-occurrence by construction). NOT backprop/PC (the prediction (Wa)_j is the unit's OWN recurrent input, no downward generative weight, no transpose, no chain). NOT inverse-frequency — THE LIVE RISK the cheapest_oracle is built to kill; survives that oracle ⇒ genuinely new, fails ⇒ dead. Distinct from the matrix-inhibition signed-assembly node (N7) in that keep-apart is a single shared scalar, not a learned pairwise W⁻/M — a different and more falsifiable separation bet.
- **Anti-homunculus check:** Who decides which units fire? The settling + the single shared inhibitory scalar h — no controller picks winners; competition lives in the (1^T a)^2 energy term whose gradient is −h applied identically to all units. Who grows a unit? A fixed residual-magnitude threshold gated stochastically (BTSP-plateau-style), not a metric-reading allocator. Who decides write gain? The diffuse scalar surprise m (content-free, one number). The anti-Hebbian term a_i(Wa)_j is computed at the synapse from the post-unit's OWN settled prediction. RISK FLAG held honest: h=λΣa_j is a sum over all units — is this 'global normalization in the WRITE path'? NO: it is in the INFERENCE/settling path not the write rule, carries zero per-unit content (one scalar), and is exactly the brain's single shared inhibitory interneuron pool (the canonical legal form of competition). The write rule itself f(a_i,a_j,(Wa)_j,W_ij,m) sees no global quantity.
- **Brain-principle grounding:** Sparse+distributed coding (per-occurrence sparse assemblies, k<<N); pervasive inhibition / E-I balance (the single shared inhibitory scalar h IS lateral inhibition as a local settling variable); locality of learning (three-factor rule sees only pre/post/own-prediction/neuromodulator); energy-as-computation (explicit Lyapunov); prediction-without-backprop-teacher (the anti-Hebbian term subtracts the network's OWN prediction, Spisak-Friston shape, single-layer so no chain); neuromodulatory gain (scalar surprise m); structural plasticity (residual-gated stochastic neurogenesis); metabolic constraint (the inhibitory budget DOES the sparsification/decorrelation work).
- **Honest risk:** Most likely failure: the anti-Hebbian term a_i(Wa)_j is a LOCAL whitening/decorrelation, and the project has already shown (Reports 122,128,132) that local decorrelation/deflation either deflates to inverse-frequency reweighting or is inert — i.e. SPARTA's surprise-gated rule may converge to exactly the inverse-co-occurrence-frequency reweight Report 128 proved closes the gap WITHOUT being paradigmatic. Second: per-occurrence settling may just reconstruct the type-average in aggregate (king's occurrence-assemblies all collapse to one mode if context drive is dominated by high-frequency function words), re-creating the common-mode. Third: neurogenesis may runaway-recruit on every rare context (capacity explosion) without the surprise gate self-limiting.
- **Cheapest oracle:** Substrate-free, no FHRR, ~80 lines, built to separate 'genuinely new surprise-decorrelation' from 'inverse-freq in disguise' (the exact deflation that killed 5 prior mechanisms): take WikiText-2 windows, build per-occurrence sparse context-drive vectors b^(t) (bag of context-word one-hots, NO type-averaging). Run ONLY the linear-algebra core of the write rule offline as a fixed-point: does the matrix W* solving W=decorrelate(Σ_t a^(t)a^(t)^T − a^(t)(Wa^(t))^T) place king/queen overlap ABOVE collocational pairs, with a CLASS-LEVEL differential control (king/queen/prince/duke as a class vs a frequency-matched random class), multi-seed? KILL CRITERION: if W* reduces to (or correlates >0.9 with) the inverse-frequency-reweighted SPPMI that Report 128 already banked, SPARTA is dead before any settling code is written.
- **Novelty:** The genuinely new commitment is PER-OCCURRENCE writes with a surprise-gated anti-Hebbian DECORRELATION-WHILE-WRITING: never form the type-centroid, and make the write rule actively suppress already-predicted (collocational) co-firing so only surprising (shared-context, paradigmatic) overlap accumulates — with inhibition as a single shared SCALAR (not a learned lateral matrix), the cleanest E-I-balance form. No prior experiment touched per-occurrence codes; all 7 banked operators wrote type-averaged co-occurrence.

### SIGNED-ASSEMBLY GLASS — per-occurrence sparse signed-synapse substrate (keep-together excitatory and keep-apart inhibitory/anti-Hebbian are ONE Lyapunov; the partition is an emergent ground state of a locally-learned frustrated energy)

- **One-line:** Drop the per-word vector entirely: each occurrence is a sparse (binary or graded-nonneg) assembly; a SINGLE signed-synapse Lyapunov (excitatory W⁺ keep-together + inhibitory/anti-Hebbian W⁻ keep-apart, equivalently one mixed-sign frustrated matrix) makes separation intrinsic, and the partition global k-means/SVD reach is here the ATTRACTOR of a frustrated energy LEARNED by a local sign-balanced Hebbian/anti-Hebbian rule and reached by per-occurrence settling — no global assignment step, no single dominant eigenvector (frustration → many basins).
- **Energy function:** Per-occurrence sparse code (binary s∈{0,1}^N at ~k-of-N, or graded nonneg y∈R_{>=0}^H). One signed-synapse Lyapunov, three equivalent renderings kept as the strongest combined form: (PCAN) E=−½ s^T W⁺ s + ½ s^T W⁻ s + (1/τ)Σ_i[s_i log s_i+(1−s_i)log(1−s_i)] − Σ_i b_i s_i with a hard metabolic budget λ(Σs−k)₊; (CRUCIBLE, graded) E(y)=−Σ_h y_h(Wx)_h + ½Σ_{hk}M_hk y_h y_k + Σ_h Φ(y_h), M_hh self-saturation, M_hk lateral INHIBITION between factors competing for overlapping support, Φ a nonneg-activation barrier (the metabolic floor forcing sparsity, Dorrell geometry-not-λ); (FAI, frustrated glass) E=−½Σ_ij J_ij s_i s_j + Σ_i θ_i s_i + (γ/2)(Σ_i s_i−k)², J mixed-sign (excitatory for tight-window co-fire, inhibitory for diffuse/long-range co-fire = FRUSTRATION). The −drive pulls co-supported units ON (keep-together); the +inhibitory/anti-phase term pushes competitors OFF (keep-apart) — the SAME E, separation INTRINSIC (no O(V²) bolt-on). FRUSTRATION is the key: a plain all-positive Hebbian J gives ONE dominant basin (the collocational mode = the wall); MIXED-sign J creates MANY shallow competing basins = a partition. The local ground state the spins/factors settle into IS the cluster assignment. GENERATIVE: a never-stored sparse pattern is a local minimum if its units mutually support via W⁺ without mutual inhibition (compositions emerge as new basins); readback through Wᵀ synthesizes a filler that never co-occurred.
- **Representational unit:** A per-occurrence sparse assembly (binary k-of-N or graded-nonneg) — NOT a vector per word-type; there is no per-type vector and no sum primitive. The SAME word in different contexts ignites OVERLAPPING-BUT-DISTINCT assembly sets (per-occurrence, never summed). A category/class is a SET of factors/assemblies that recur across occurrences (a latent factor h is a population direction engaged by MANY occurrences of MANY similar words — distributed, each word lights several factors, no grandmother cell). A word-type is recovered post-hoc as intersection/union statistics — the direct attack on the type-collapse root (no sum-into-one-vector step exists).
- **Local dynamics:** Asynchronous rectified spin/factor update = local gradient descent: s_i ← 1 if (Σ_j J_ij s_j − θ_i − γ(Σs−k)) > 0 else 0 (binary, the γ term a single global inhibitory scalar = k-budget settling variable = lateral inhibition as ONE recurrent variable), or the multiplicative Cohen-Grossberg/Lotka-Volterra settle y ← y∘(positive drive)/(saturation+inhibition+barrier) to a sparse fixed point. Provably non-increasing on E. Settling from a per-occurrence cue lands in the frustrated ground state = the occurrence's category assignment (un-mixing the cue into latent factors). Completion: clamp a partial pattern, settle, read the basin; generative readback Wᵀy*. Polysemy split (target behavior 3): if a unit is recruited by two context-incompatible assemblies, the W⁻ it accumulates makes the joint state high-energy, so settling bifurcates into two basins — the unit effectively splits with no splitter module. NOT power-iteration: a frustrated glass does not converge to a dominant eigenvector — multiple basins, the data picks which.
- **Learning rule:** Local sign-balanced Hebbian/anti-Hebbian (the competition term), two rules both f(pre,post,w,neuromod): W⁺/excitatory ΔW⁺_ij=η m[s_i s_j(1−W⁺_ij) − decay] (BTSP-style headroom-gated potentiation); W⁻/inhibitory/anti-Hebbian ΔW⁻_ij ∝ [⟨s_i⟩⟨s_j⟩ − s_i s_j]₊ or Δm_hk ∝ y_h y_k − target — factors/units that co-fire grow mutual inhibition (DECORRELATION; subtract the network's OWN predicted correlation, Spisak-Friston). The anti-Hebbian term is the intrinsic separator AND what MANUFACTURES the inhibitory frustration locally — keep-together and keep-apart are the SAME update class, no bolted-on repulsion. Also a feedforward Hebbian-minus-own-reconstruction Δw_hj ∝ y_h(x_j−(Wᵀy)_j) (a LOCAL prediction error using the SAME W transposed as a within-layer reconstruction — single-layer, NOT the Millidge backprop trap). NEUROGENESIS: when the cue's drive is poorly explained (large local residual x−Wᵀy, an exogenous novelty event / BTSP plateau on residual magnitude crossing a FIXED developmental threshold), a NEW factor/unit is recruited initialized to the residual. Capacity is GROWN. Replay re-settles stored occurrences and re-balances W → consolidation deepens the partition basins.
- **Gap it fills:** 
- **Why not a closed family:** NOT single-projection (read is multi-step nonlinear settling on a learned signed energy; frustration → no single dominant eigenvector). NOT k-means/nonlinear-partition-as-global-Lloyd — the oracle EXPLICITLY runs the k-means/k-WTA control and the claim is FALSIFIED if they match; differentiators: (a) per-OCCURRENCE codes not type-averaged build_S rows (what Report 127 partitioned), (b) ONLINE local plasticity + neurogenesis not an offline Lloyd pass, (c) soft graded multi-factor membership not hard one-hot, (d) FRUSTRATION (mixed-sign learned J → multiple basins) which plain k-WTA lacks. NOT frozen-random-slot (Report 129): W and M/J LEARNED locally, capacity grown. NOT inverse-frequency (Report 128): updates correlational/anti-correlational on per-occurrence codes, no global count reweight — PROVIDED the anti-Hebbian baseline is a local trace (the named honest risk). NOT type-collapse (per-occurrence sparse). NOT bolted-on repulsion (inhibition INSIDE the one Lyapunov). NOT depth-deflation-peel (Report 132): FLAT-by-justification (single factor layer, recurrent-within-layer un-mixing, not a stack). NOT PC=backprop (single layer, the reconstruction is the layer's own current guess, no chain).
- **Anti-homunculus check:** Who assigns an occurrence to a category? The spins/factors settling to a ground state — settling decides, no assignment module. Who separates categories? The anti-Hebbian/inhibitory term INSIDE the same energy (intrinsic), not an if-too-close-then-push pass. Who sets sparsity? The fixed k-budget scaffold + the single γ inhibitory settling scalar. Who grows a factor? An exogenous stochastic novelty-plateau gated on LOCAL residual magnitude crossing a FIXED developmental threshold (the most homunculus-adjacent piece — legal ONLY if the threshold is fixed-at-construction and the trigger is the local residual, like BTSP's Ca²⁺ plateau, NOT a runtime-tuned metric a controller reads). The mixed-sign J STRUCTURE is learned from data (not hand-set toward a target answer). The single global γ/h is a budget scalar that settles (legal neuromodulatory-gain form of global influence). PASSES, with the neurogenesis trigger as the audited boundary case.
- **Brain-principle grounding:** Pervasive inhibition / E-I balance (lateral inhibition as local competition = keep-apart, settling decides the winner); energy/attractor (Cohen-Grossberg/Lotka-Volterra / spin-glass Lyapunov, explicit); sparse+distributed nonneg rate code (the metabolic Φ/k-budget forces sparsity = Dorrell geometry-not-λ modularity-from-activation-cost); locality (Hebbian-minus-own-reconstruction + local-trace anti-Hebbian, single-layer so no backprop chain); development/structural plasticity (neurogenesis grows capacity by a local rule); metabolic budget as forcing function. Drift self-check: write f(pre,post,w,m); no .backward(); explicit Lyapunov; no static-slot-index binding; the anti-Hebbian baseline MUST stay a local trace (the named risk).
- **Honest risk:** TWO sharp failures. (1) The local anti-Hebbian decorrelation + recurrent un-mixing may settle to the SAME dominant-collocational partition k-means/Lloyd reaches (Report 127's lesson: ANY nonlinear partition of the co-occurrence operator gives the collocational answer; the paradigmatic subdominant structure may not be recoverable by competition over a type-aggregated SUPPORT even with per-occurrence traces, if W ends up encoding mostly first-order co-occurrence) — and if frustration adds nothing over plain k-WTA it collapses into the banked Report-127 wall. (2) The anti-Hebbian baseline ⟨s_i⟩⟨s_j⟩, if computed as a running average over ALL occurrences, IS a global statistic sneaking back in (the inverse-freq/global-PMI ghost) — it MUST be a LOCAL synaptic eligibility trace at the ij synapse or the locality invariant breaks. Also: frustrated glasses have MANY spurious local minima (settling lands in glassy noise); Cohen-Grossberg stability needs M symmetric (the local anti-Hebbian rule may violate it → limit cycles); neurogenesis threshold tuning could dominate (capacity-is-not-the-lever, Report 131).
- **Cheapest oracle:** Substrate-free, no FHRR, the decisive cheap kill-test that confronts BOTH the dominant-mode wall AND the k-means equivalence (Report-127): on a planted toy with a KNOWN category partition AND a KNOWN dominant collocational mode that CONFLICTS with it (the subdominant-mode wall, planted), learn J/W by the local sign-balanced rule from per-occurrence samples (numpy outer products + a LOCAL-trace anti-Hebbian baseline, no torch), then anneal/settle the ground states and check whether they recover the PLANTED PARTITION (class-level differential vs within-class shuffle) rather than the dominant collocational mode. Compare FOUR conditions head-to-head: Hebbian-only (predict: dominant mode = the wall), random-frustration (predict: glassy noise), learned-sign-balanced (the hypothesis), AND a plain global Lloyd k-means / online k-WTA on the SAME per-occurrence codes. KILL: if learned frustration's ground states don't beat both the dominant-mode and the k-means/k-WTA controls, frustration/local-settling bought nothing and it reduces to the closed nonlinear-partition family (Report 127) — killed for the price of a numpy script.
- **Novelty:** First per-occurrence representational unit in the project with NO per-word vector and NO sum primitive (the untested escape the brief names), AND the partition is the GROUND STATE of a FRUSTRATED energy learned by a local sign-balanced rule, reached by per-occurrence settling — the anti-Hebbian subtract-own-prediction is exactly the local decorrelation the project kept reaching for globally, and mixed-sign frustration is the mechanism that could give MULTIPLE basins instead of one dominant mode.

### CO-SETTLING LAMINA — genuine hierarchy as JOINT co-settling under one energy (each level holds a residual by equilibrium NOT a deflation-peel; anti-backprop via shared/local-interface weights; replay as the structure-generator)

- **One-line:** A genuine multi-level laminar substrate where levels CO-SETTLE simultaneously under ONE joint energy (not a feedforward/deflation-peel stack): the high/slow/coarse level proposes a structural frame, the low/fast/fine level holds the content residual, they reach mutual consistency by settling, the inter-level weight is SHARED both directions (the anti-backprop guard — no learnable transpose carrying a global error), and offline replay is the structure-generator that interleaves same-class slow-assemblies to write their shared residual; analogical/structural retrieval is the coarse level settling to the same basin for same-shape-different-content inputs.
- **Energy function:** L coupled levels (minimally fast a^0 / slow a^1, or coarse-to-fine sparse nonneg sheets), ONE joint Lyapunov with a SINGLE SHARED inter-level weight (used both up and down — the Millidge-trap escape: no separately-learned downward generative weight): E=Σ_ℓ[φ(a^ℓ) − ½(a^ℓ)^T W_ℓ a^ℓ (within-level recurrent support + lateral inhibition, the N7 signed-assembly mechanism per level) + ½λ_ℓ(1^T a^ℓ)^2 (per-level shared inhibitory scalar)] − Σ_ℓ γ_ℓ (a^{ℓ+1})^T U_ℓ a^ℓ (reciprocal coupling: the slow/coarse level EXPLAINS the fast/fine level; U_ℓ symmetric/shared so U^T is the same synapse, NOT a learned transpose). CRUCIAL anti-deflation-peel design: a^ℓ is NOT residual_input − projection(a^{ℓ+1}) computed feedforward then peeled (the forbidden Report-132 stack); instead the low level keeps WHATEVER it must to make E low GIVEN the high level's current state — the residual is an EQUILIBRIUM quantity held by JOINT settling, so deeper levels CONSTRAIN variance, they do not REMOVE it. The slow sheet has a long time-constant (τ_1>>τ_0) so it integrates the COMMON-mode across many fast occurrences while the fast sheet is rectified to hold the RESIDUAL. GENERATIVE: clamp a high-level frame, settle DOWN, the low levels synthesize a content-filled completion never stored. Joint block-coordinate descent on E is Lyapunov-monotone (symmetric U, bounded barrier).
- **Representational unit:** A per-occurrence MULTI-SCALE code: simultaneously a fine sparse content assembly at level 0 (per-occurrence, never averaged) and a coarse/slow sparse structural frame at the top. The 'structural part doing real work' is literally the high-level assembly. Higher levels are at progressively SLOWER timescales (τ_ℓ increasing) so they hold cross-instance regularities WITHOUT averaging the instances — they hold the slow COUPLING that explains the instances, not a centroid of them. A paradigmatic CLASS lives natively at the slow/coarse level (king-occurrences and queen-occurrences drive overlapping SLOW assemblies even when never co-occurring) — so a CLASS-level metric is native here (the headline reads slow-level overlap, never bare fast-pair-specificity, directly defusing the B-KILL arbiter landmine). A word-type is not a unit; an occurrence is a vertical stack of co-settled assemblies; binding is the cross-level coupling settled into consistency.
- **Local dynamics:** Recall: clamp level-0 from the (partial) cue, let ALL levels CO-SETTLE (block-coordinate: update each a^ℓ by its local gradient = drive from below's residual + consistency pull from above's current STATE through U + within-level inhibitory competition). The high level cannot copy the low level (too few units, hard budget k_L) so it settles to a compressed frame that best explains the low activity via U; reciprocally U's feedback sharpens which low units stay on. The joint fixed point is the multi-level interpretation. ANALOGICAL retrieval (the headline behavior): present 'X did Y to Z' as a low-level partial; the high level settles to the relational frame; querying for 'same high frame, different low content' surfaces stored occurrences whose high-level assemblies are in the same basin — structural matches with different content, because the match is at the level whose budget forced content-invariance. BIND-vs-BUNDLE discovery: a stable role-pair drives a consistent high-level frame (bound); mere co-occurrence does not (the high level sees them as separable low activity needing no joint frame) — DISCOVERED from where U's reciprocal support stabilizes during settling, not declared. Polysemy split: a low-level factor pulled by two incompatible high-level schemas → N7 neurogenesis splits it.
- **Learning rule:** All weights by LOCAL rules. Within-level W_ℓ, M_ℓ: the N7 signed-assembly rules (Hebbian-minus-own-reconstruction + anti-Hebbian decorrelation, single-level-local). The SHARED reciprocal U_ℓ: a LOCAL boundary prediction-error Hebbian rule ΔU_ℓ ∝ (a^ℓ − prediction)·a^{ℓ+1} or η m_replay(a^{ℓ+1}a^{ℓT} − a^{ℓ+1}(U a^ℓ)^T) (anti-Hebbian, available at that interface, corrects ONLY its own coupling — single-interface, no chain = not backprop; and because U is used both directions this ONE update serves up-explanation and down-generation, so there is NO independent downward weight carrying a global error). MULTI-TIMESCALE: W_ℓ fast (episodic, per-occurrence, gate m), U_ℓ/W_1 slow (consolidation, gate m_replay) — the sleep/wake split (U and slow W write mainly OFFLINE during replay, satisfying the runtime-write ban), Benna-Fusi fast/slow components per synapse. REPLAY is the structure-generator (the untapped lever): offline, the slow sheet is seeded and the fast sheet GENERATES samples from it (the generative direction through U); U/W_1 update on these self-generated fast-slow pairs — interleaving emerges because replay priority is a LOCAL eligibility trace (recently-surprising slow assemblies replay more), not a hand-set curriculum. This is where paradigmatic structure could be MANUFACTURED rather than grown: replay interleaves king/queen slow-assemblies, the anti-Hebbian U-rule writes their shared slow-residual.
- **Gap it fills:** 
- **Why not a closed family:** NOT a deflation-peel stack (Report-132, FORBIDDEN): levels hold residuals by JOINT settling under one energy, never feedforward-peel-then-subtract; the residual is an equilibrium quantity — this is the object Report-132's scope-caveat and Report-129's signpost explicitly left UN-RUN as the real frontier ('levels co-settle, NOT stacked projection-peel'; 'a genuine local multi-layer writer that HOLDS a residual at each level'). NOT backprop/PC-with-downward-generative-weights (Millidge-2020, banned): up and down paths SHARE the synapse (symmetric U), so no learnable transpose carries a global error — and the oracle explicitly tests this with a tied-weight-AE control. NOT single-projection (multi-level nonlinear joint settling). NOT k-means (the high-level frame emerges from budget-constrained reciprocal settling, not a global partition of a fixed matrix). NOT TEM-slot-assignment-by-backprop (Report-129): the slot/frame assignment is LOCAL settling under shared weights, the whole point being to do it WITHOUT backprop. NOT type-averaging (per-occurrence at the fast/fine level; the residual is preserved in the SAME energy). The two LIVE risks of reducing to a closed family (Report-128 inverse-freq via replay-priority; Report-132 SVD-by-stages via slow-factoring; plus the Millidge equivalence) are exactly what the oracle's kill-controls detect BEFORE banking.
- **Anti-homunculus check:** Who decides what is abstraction vs instance? The timescale constants τ_ℓ + the budget sizes k_ℓ (a fixed problem-generic scaffold, §4-legal) — the slow/small level automatically captures what recurs because its long time-constant low-pass-integrates the fast stream and its tight budget forces a shared frame; nobody routes content to a level. Who decides bind vs bundle? Where U's reciprocal support stabilizes during settling (a tension in the energy), not an if-then classifier. Who decides what to replay? A local eligibility trace (surprise-tagged decay), not a metric-reading scheduler. The no-backprop check is the sharpest: U is ONE shared weight used both up (drive) and down (prediction/STATE) — there is NO separately-learned downward generative weight approximating a transpose, so the Millidge-2020 'PC-with-downward-weights = backprop' trap is structurally avoided; self-check 'delete the downward path, does the local write still know what to do?' — yes, each level keeps its within-level Hebbian-minus-reconstruction, the downward U^T a^{ℓ+1} only sets the consistency TARGET, it carries no global error.
- **Brain-principle grounding:** Cortex is laminar/hierarchical/multi-area/multi-scale with RECIPROCAL (feedforward+feedback) connections (the load-bearing brain-isn't-flat mandate); prediction-without-backprop-teacher (each level predicts the level below via shared connections / its own lateral recurrence, learns from LOCAL mismatch, target self-generated by the cue); multi-timescale plasticity + consolidation/replay (τ_ℓ ladder + Benna-Fusi fast/slow per synapse, slow top crystallizes structure, the plasticity-rigidity escape); energy/attractor (one joint Lyapunov, co-settling IS the computation); inhibition per level (N7 within each); locality (single-interface boundary rules, no chain); sparse+distributed + per-occurrence at the fast level; replay as the structure-generator (the untapped CONTEXT.md §5 lever).
- **Honest risk:** This is the closest to the project's own banked nulls and the deepest bet. (1) The local single-interface rule, even shared/symmetric, may be INSUFFICIENT to push structural regularity to the top level — top levels may just learn slower copies of the bottom collocational structure (the dominant-mode problem, now stacked), giving Report-132's outcome (depth doesn't compose, L=1≥L=2≥L=4) for a DIFFERENT reason. Without the global error that lets backprop assign 'you, top level, handle abstraction,' there may be no LOCAL force that DIFFERENTIATES the levels' roles → they collapse to redundancy. (2) The shared-symmetric-U reciprocal settling might be mathematically equivalent to a shallow predictive-coding net whose generative weights equal the recognition weights — which Millidge 2020 says still computes backprop-like gradients in the iterated/deep limit → backprop-in-disguise → disqualified. (3) The replay surprise-priority could close any locality gap by re-weighting toward rare slow-assemblies = inverse-frequency again (Report-128). (4) Even if NOT a peel-stack, if the slow-sheet's joint-energy factoring converges to the same subspace SVD would peel, the honest ceiling is 'SVD-by-stages' and it does not beat global (Report-132's caveat). (5) Joint co-settling can be unstable (top-down can drive the fast sheet into a self-confirming hallucination — generative-model collapse) or need a hand-separated timescale that smuggles in a controller. (6) Real text may simply lack a recurring slow structure (memory-not-learner, Reports 056/119), leaving the slow sheet at the common-mode = the type-average. The honest position: its NULL would land where Report-132 did, but for a genuinely different mechanism — which is exactly why the cheap planted oracle + all three kill-controls exist.
- **Cheapest oracle:** Substrate-free, 2-level only, two-stage with explicit kill-controls. STAGE A (necessary): synthetic stream/data with a PLANTED two-level structure (a latent 'topic/role/schema' instantiated by MANY different content fillers, per-occurrence, with king/queen-style class members in matched slow-contexts but never adjacent) PLUS surface collocational noise. Run the 2-level joint co-settling with LOCAL rules only and measure (class-level differential, multi-seed): does the SLOW/TOP level develop attractors aligned with the PLANTED schemas (structural) rather than the surface collocations — top-level-attractor-purity for same-schema-different-content occurrences vs a shuffled-token control — with the FAST level correctly HOLDING the residual (fast overlap NOT inflated)? STAGE B (the kill controls): (1) collapse the hierarchy to flat (τ_1=τ_0 / 1-level baseline) — if the flat control reaches the SAME class-overlap, the hierarchy is doing no work and it reduces to a flat node (or a banked flat null) — the timescale/depth separation MUST be LOAD-BEARING. (2) if a BACKPROP-trained two-level AE with tied weights reproduces the top-level structure exactly, the no-backprop escape is suspect. (3) compare against an inverse-frequency-reweight control on the slow assemblies (Report-128 guard) — if that reproduces the slow-overlap, it is inverse-freq in disguise. Only graduate to FHRR/real-WikiText if planted-structure shows level-differentiation by local rules alone AND beats all three controls.
- **Novelty:** First hierarchical substrate in the project where levels settle JOINTLY under ONE shared energy with shared/reciprocal weights — categorically different from the banked depth-as-deflation-peel (Report-132 stacked projections) and from K-branch schema-prior (Report-041, which collapsed because branches shared a flat substrate). The shared-symmetric-weight-as-anti-backprop-guard is a novel structural answer to the Millidge trap the project has not tried, AND replay-as-structure-generator (the slow sheet generating fast samples that the anti-Hebbian rule consolidates) makes paradigmatic structure potentially MANUFACTURED rather than grown.

### HYDRA — communicating-vessel multi-timescale field with structural FISSION (atom-splitting for polysemy as a DOUBLE-WELL energy bifurcation, not a clustering call)

- **One-line:** A memory atom is a population of physical communicating-VESSEL units, each a coupled (fast,slow) liquid-level cascade; per-occurrence experience pours into fast vessels; the energy is the literal potential energy of the vessel system; consolidation is relaxation across timescales; and a vessel SPLITS by a double-well FISSION instability when its slow level becomes persistently bimodal — capacity (and polysemy-driven atom-splitting) is GROWN by an emergent energy bifurcation, not allocated by a controller.
- **Energy function:** State = for each unit i a hierarchy of L coupled levels u_i=(u_i^1 fast … u_i^L slow), each a nonnegative graded 'liquid level' (rate-coded amplitude — NOT unit-modulus phase). E=Σ_i[ Σ_l ½ c_l(u_i^l − u_i^{l+1})² (inter-level coupling: communicating vessels equalize across timescales) + ½ k_i(u_i^1 − x_i)² (fast level tracks current input drive x_i) ] − ½ Σ_{ij} W_ij φ(u_i^L)φ(u_j^L) (recurrent associative energy on the SLOW/consolidated levels, φ saturating) + λ Σ_i Ψ(u_i) (a bounded-range DOUBLE-WELL barrier whose two wells are the FISSION seeds). Settling = gradient descent; fast levels equilibrate to input within a wake step; over replay the coupling c_l transfers mass to slow levels (Benna-Fusi physics). The associative term on the slow level is the recall energy whose minima are completed memories; the double-well Ψ is what makes structural plasticity (fission) emerge FROM the energy.
- **Representational unit:** A per-occurrence deposit into the FAST level (a transient liquid pulse), held distinctly because fast levels relax slowly enough to co-exist within a consolidation window and are time-stamped by which replay cycle they entered. The TYPE is the slow-level basin many occurrences relax toward — but if occurrences are BIMODAL (polysemy), the double-well barrier makes the single slow vessel unstable and it FISSIONS into two units, so the type is never forced to average two senses into one centroid. The unit is defined by its participation (which slow vessels a context drives), not a fixed coordinate.
- **Local dynamics:** u_dot_i^l = −∂E/∂u_i^l = c_{l-1}(u_i^{l-1}−u_i^l) − c_l(u_i^l−u_i^{l+1}) [+ input term at l=1] [+ associative field at l=L] − λ Ψ'(u_i^l), rectified nonnegative. Every term reads only unit i's own adjacent levels and its synaptic neighbours' slow activity — fully local. The cross-LEVEL coupling is the multi-timescale hierarchy (each level holds a RESIDUAL the faster level hasn't yet consolidated) — a structural/dynamical hierarchy, NOT a projection-deflation peel (no level projects-then-subtracts the input; levels are coupled reservoirs at different relaxation rates). FISSION: when a slow vessel's deposit history has been bimodal long enough that Ψ'(u_i^L) crosses the double-well saddle, the unit DUPLICATES (structural plasticity) — an exogenous fixed local rule, not a metric-reading allocator.
- **Learning rule:** Slow-level associative Hebbian, local: ΔW_ij = η φ(u_i^L)φ(u_j^L)(1−W_ij) − η b_i φ(u_j^L), where b_i is unit i's OWN locally-predicted activity (Spisak-Friston anti-Hebbian self-prediction subtraction = intrinsic decorrelation/inhibition, so keep-apart is the SAME rule as keep-together, no bolted-on repulsion). The fast→slow mass transfer (the consolidation 'learning') is the vessel relaxation itself, governed by fixed coupling constants c_l — physics, not a fitted gradient. Replay re-pours stored fast deposits during the offline (low-ACh) window, reshaping slow basins; replay PRIORITY is the deposit with the largest unconsolidated fast-slow level GAP (a local per-unit quantity — the natural 'surprise', a per-unit STATE quantity NOT a global-count metric-reading scheduler).
- **Gap it fills:** 
- **Why not a closed family:** NOT a deflation-peel stack (Report-132): levels are coupled reservoirs at different relaxation rates; no level projects-then-subtracts. NOT single-projection (multi-timescale relaxation + a recurrent associative slow term). NOT type-averaging (fast-level per-occurrence buffer; fission PREVENTS averaging two senses). NOT frozen-random-slot (W learned, capacity grown by fission). NOT inverse-frequency — THE LIVE RISK (fission-on-frequency) the cheapest_oracle is built to detect (corr(freq,fission) vs corr(bimodality,fission)). NOT k-means. NOT backprop (anti-Hebbian self-prediction, no downward chain).
- **Anti-homunculus check:** Who decides to split an atom? Nobody — the double-well barrier crossing is a fixed local dynamical INSTABILITY (a physical bifurcation), triggered by the unit's own bimodal deposit history, not by a controller reading a bimodality metric and calling split(). Who decides consolidation order? The local fast-slow level gap (largest unconsolidated residual relaxes fastest — physics), not a scheduler inspecting per-item loss (which Report-128 showed deflates to inverse-freq — avoided because priority is a per-unit STATE quantity, not a global count). Who decides which memory wins at recall? The slow-level associative basin. Fixed scaffold (L levels, coupling c_l) = legal evolution-style prior. No supervisor, no if-then.
- **Brain-principle grounding:** Multi-timescale plasticity/consolidation (Benna-Fusi communicating-vessels as the literal energy; capacity ~N by fast→slow transfer); development/structural plasticity (fission = experience-grown capacity by a fixed local rule); metabolic constraints (bounded vessel range + double-well = the forcing function); prediction-without-backprop (anti-Hebbian self-prediction subtraction). Drift self-check: forgetting is fixed local relaxation across coupled timescales (not a global re-fit); the fission trigger is exogenous-state, not a runtime metric threshold.
- **Honest risk:** Most likely failure: the slow-level associative term is STILL a quadratic form whose dominant eigenvector is collocational — so consolidation may just reproduce the Family-1 bound one timescale slower, and FISSION may fire on FREQUENCY (high-count words look bimodal from variance) rather than genuine polysemy, re-deriving an inverse-frequency split (the Report-128 deflation in structural-plasticity clothing). Also the double-well Ψ is a hand-tuned barrier — Dorrell warns geometry-not-λ gates modularity, so a tuned barrier height may be the wrong knob and fission may be all-or-nothing brittle.
- **Cheapest oracle:** Substrate-free polysemy oracle: take a genuinely bimodal token (e.g. 'bank' = river/money) with per-occurrence context windows, run the fast→slow relaxation + fission rule, and test (a) does the vessel FISSION into two units whose slow basins separate the two sense-context clusters ABOVE a control where the token's contexts are randomly relabelled into two arbitrary halves (class-level differential), and (b) does a monosemous frequency-matched token NOT fission. KILL CRITERION: if fission rate correlates with token FREQUENCY rather than context-bimodality (corr(freq,fission)>corr(bimodality,fission)), it is the inverse-frequency deflation again (Report-128) in structural-plasticity clothing — dead. ~200 lines CPU.
- **Novelty:** Atom-SPLITTING as a double-well FISSION INSTABILITY in an energy landscape (structural plasticity emerging FROM the energy, not a clustering call) — never tried; the project's only splitting was a bolted-on splitting-tension force. The fast-level per-occurrence buffer feeding a slow associative basin is the per-occurrence escape made physical — distinct from the phase substrates in that per-occurrence distinctness here comes from TEMPORAL relaxation separation, not phase bins, and distinct from the co-settling/forward-only laminar nodes in that its headline contribution is the FISSION bifurcation (the only candidate making atom-splitting an energy event).

### STRATA — Self-Tiling Reaction-Tension Atlas (a variational free-energy substrate whose minima TILE a structured product manifold, separation done INTRINSICALLY by the KL/complexity term, hierarchy via prior-down / error-up)

- **One-line:** Build the energy as a variational free energy F = accuracy + complexity over a FACTORED hierarchical latent on a non-Euclidean product manifold, where the complexity (KL-to-a-tiling-prior) term does the orthogonalization/separation INTRINSICALLY (keep-together and keep-apart are both the gradient of one F), and the minima TILE the manifold coarse-to-fine, so abstraction = the coarse cells of the learned tiling and analogical retrieval = co-occupancy of a coarse cell, learned by a local prediction-error rule whose downward message is a PRIOR over tiles (NOT a transpose-weight reconstruction = the anti-backprop guard).
- **Energy function:** Latent is a HIERARCHY of factored fields z=(z^(1),z^(2),…) on a product manifold M=S^1×…×S^1×R_{>=0}^k (angular factors for cyclic/relational structure, nonnegative factors for sparse content), per-occurrence. F(z;x)=Σ_levels[½Σ_units ε_i^(ℓ)²/σ² + D_KL(q(z^(ℓ)) ‖ p(z^(ℓ)|z^(ℓ+1)))], where the prediction error ε_i^(ℓ)=z_i^(ℓ)−g(z^(ℓ);W^(ℓ))_i is the residual between a unit's value and what its SAME-LEVEL lateral context predicts (a within-level recurrent generative map g, NOT a downward inter-level reconstruction weight — the load-bearing distinction from PC=backprop). The KL/complexity term uses a SPARSE, FACTORIZED, TILING prior p whose components partition the manifold into cells; minimizing it forces q to factor (different relational roles → different angular factors, the Dorrell-style geometric separation done by the complexity term NOT a tunable λ) and pushes each occurrence toward exactly one cell at its level → minima TILE the space into a coarse-to-fine atlas. Higher levels have COARSER tilings (broader cells = abstractions); lower levels finer. Hierarchy is structural (each level holds a residual of the level above via the prior/error split), NOT a deflation-peel.
- **Representational unit:** A per-occurrence point in the product manifold, expressed as soft tile-memberships at each level (coarse tile at the top = the abstract role/class, fine tile at the bottom = the specific content). A word-TYPE is the SET of its occurrences' fine tiles, which may share a coarse tile (paradigmatic class) without sharing fine tiles (distinct words) — king and queen share the top-level 'royalty/role' cell while occupying distinct fine cells, by the tiling GEOMETRY not by co-occurrence. Sparse (each occurrence ~one tile per level), nonneg content factors, angular relational factors — on a structured manifold rather than R^D.
- **Local dynamics:** Settling = gradient descent on F w.r.t. z (predictive-coding-style error minimization but WITHIN level and laterally): dz_i^(ℓ)/dt = −[ε_i^(ℓ)/σ² − (lateral feedback from g) + ∂D_KL/∂z_i^(ℓ)]. The KL gradient is a local push toward the nearest tile centroid + a repulsion from the prior's between-tile low-density gaps — keep-together (toward your tile) and keep-apart (out of the inter-tile region) are the SAME derivative. Inter-level coupling is RESONANT not generative-downward: level ℓ+1's settled coarse tile sets the PRIOR (the p in the KL) for level ℓ, and level ℓ's residual drives level ℓ+1 — bidirectional message-passing where the upward message is a prediction-error and the downward message is a PRIOR/gain bias, NOT a transpose-weight reconstruction (the explicit Millidge-2020 guard: deleting the downward path changes the prior, not the local write target — ε is still defined). On a manifold, settling respects the geometry (angular factors wrap, nonneg factors stay in the cone).
- **Learning rule:** Local Hebbian-minus-own-prediction (the Spisak-Friston / FEP form, local): ΔW_ij^(ℓ)=η(z_i^(ℓ)z_j^(ℓ) − z_i^(ℓ)ĝ_j) where ĝ is the unit's own lateral prediction — Hebbian potentiation minus the network's own predicted correlation (the anti-Hebbian decorrelation term IS the inhibition/separation, intrinsic to the rule). Tile/prior parameters update by a local responsibility-weighted rule: a tile centroid drifts toward occurrences that settle into it (Δμ_c=η_c r_c(z−μ_c), r_c=soft membership); tile WIDTHS shrink under the complexity pressure (a bounded-resource budget — the metabolic invariant forcing finer tiles only where data demands, coarser elsewhere → the coarse-to-fine atlas emerges, not hand-set). Structural plasticity: a tile SPLITS when its occupancy becomes persistently bimodal under a FIXED local variance-test gated by replay (the atom-splitting-for-polysemy graduation behavior, as a local tile-fission dynamic, NOT a controller allocating a slot). All updates local; replay/ACh gate is the only global signal (a broadcast scalar).
- **Gap it fills:** 
- **Why not a closed family:** Not single-projection: settling is nonlinear gradient flow on a manifold free energy with a mixture prior — no fixed linear operator, no power-iteration. Not k-means partition (Report 127): tiles are soft, graded, hierarchical, learned with a complexity-pressured width budget — and the headline is class-level co-occupancy with a differential control, explicitly NOT the hard one-hot co-membership that reduced to Lloyd. Not type-averaging: per-occurrence tile memberships, never a centroid read. Not PC=backprop (the sharp one, guarded structurally): the downward message is a PRIOR over tiles (low-D, biasing), NOT a transpose-weight reconstruction; the local write target is defined without the downward path — failing this guard is named as the #1 honest_risk, not hidden. Not inverse-frequency (Report 128): no global counts. Not frozen-random-slot: tiles learned and split locally. Not deflation-peel-depth (Report 132): levels run different-granularity generative tilings with prior/error message-passing, never an orthogonal-projection residual-peel passed up for the same op.
- **Anti-homunculus check:** Who decides tile membership? The KL gradient settling — each occurrence flows to its lowest-free-energy tile, no assigner. Keep-together vs keep-apart? One gradient of one F (the KL term's pull-to-tile and the between-tile low-density repulsion are the same derivative). Who splits an atom? A fixed local bimodality/variance test gated by exogenous replay (developmental-critical-period class, the test is on the tile's OWN occupancy distribution, fixed-shape, not an outcome-inspecting controller). Who sets coarse-vs-fine granularity? The bounded-width budget under data pressure (Dorrell geometry-not-λ), emergent not hand-set. The downward inter-level message is a PRIOR/gain (the legal form of top-down influence — it biases, it does not specify the local write target = the anti-backprop guard). Watched corner: the within-level g must NOT secretly become a downward reconstruction of a lower level — code-review guard: g reads only same-level activity + the prior bias from above; the only downward signal is the scalar/low-D prior over tiles, never a full reconstruction.
- **Brain-principle grounding:** Energy/free-energy as computation (variational F, settling IS the computation); metabolic constraints (the complexity/KL term + bounded-width budget = Dorrell geometry-not-λ, doing the separation intrinsically); prediction-without-backprop (Spisak-Friston local FEP write; downward message is a PRIOR over tiles, low-D/biasing, not a transpose reconstruction — the explicit Millidge guard); pervasive inhibition (the anti-Hebbian decorrelation term in the FEP rule); structural plasticity (local tile-fission for polysemy); sparse+distributed nonneg + angular-relational factors; genuine hierarchy (coarse-to-fine tilings via prior/error message-passing). Non-Euclidean structured manifold is the distinctive geometric commitment.
- **Honest risk:** Most likely failure: it IS predictive coding, and despite the within-level/prior-down framing, the lateral generative map g + the local error rule may compute the same gradients as a global objective — the Millidge-2020 trap bites and STRATA is backprop-in-disguise; OR the guard (prior-down-only) so weakens the top-down signal that the hierarchy never forms meaningful coarse tiles (the levels decouple, each becomes an independent flat tiling = the flat substrate again, n levels of it). Second: the tiling prior is a strong assumption — if real linguistic structure does NOT partition cleanly into coarse-to-fine cells (paradigmatic structure may be graded/overlapping, not tiled), the KL forces a wrong factorization and king/queen get split into different coarse tiles by a spurious dominant axis (the dominant-mode problem as 'the tiling aligns to the collocational axis'). Third: variational free energy with a mixture/tiling prior is hard to settle stably (mode-collapse onto one tile, or oscillation) without careful tempering.
- **Cheapest oracle:** Substrate-free, ~100 lines, the coarse-tile-sharing test with a class-level differential control. On per-occurrence context vectors for a curated paradigmatic set, run ONLY the single-level KL-tiling settle + the local FEP update (skip the hierarchy first), and measure: do non-co-occurring same-class items (king/queen, walk/run) end up in the SAME tile MORE than a shuffled-class control AND more than co-occurring different-class items (the class-level differential the brief demands instead of pair-specificity)? If a single FEP-tiling level cannot make same-class-non-co-occurring share a tile above BOTH controls, the hierarchy won't save it — kill it for ~1-2 GPU-hours. Then a second cheap oracle: does adding ONE coarse level above (prior-down only) make the coarse tile MORE class-aligned than the fine level — testing that the hierarchy does real work and hasn't decoupled — before any FHRR/substrate integration.
- **Novelty:** The only candidate that puts the energy on a non-Euclidean structured PRODUCT MANIFOLD and makes the COMPLEXITY/KL term of a variational free energy (intrinsic, geometry-not-λ) do the keep-apart work — so abstraction = the coarse cells of a learned TILING and analogical retrieval = co-occupancy of a coarse cell, a generative hierarchical landscape whose GEOMETRY is the rule. Distinct from all co-settling/forward-only laminar nodes in its variational/tiling formulation and its prior-down (not shared-weight, not forward-only) anti-backprop guard.

## Killed (1)

### LPR — Laminar Predictive Reservoir (multi-timescale hierarchy with NO downward generative weights; abstraction comes from TIMESCALE separation + sparsity, not from a peel or a feedback path) — KILLED
- killed-by **admissibility**: FATAL — invariant (1) violation: PC-with-downward-error-weights (the Millidge trap), present in the specification, not merely a risk. The author claims the anti-backprop move is "structural absence of the downward path" and that there is "NO W_ℓ^T generative weight." This is mathematically false given the energy function they wrote. With ONE shared scalar energy E = Σ_ℓ[½‖x^(ℓ) − W_ℓ x^(ℓ-1)‖²₊ + ...] and settling defined as rectified gradient flow τ_ℓ dx^(ℓ)/dt = −∂E/∂x^(ℓ), the variable x^(ℓ) appears in TWO reconstruction terms: its own (level ℓ) AND level ℓ+1's term ½‖x^(ℓ+1) − W_{ℓ+1} x^(ℓ)‖². Therefore −∂E/∂x^(ℓ) necessarily contains +W_{ℓ+1}^T·(x^(ℓ+1) − W_{ℓ+1} x^(ℓ)) — the TRANSPOSE of the forward weight carrying the level-(ℓ+1) prediction error DOWNWARD into level ℓ. The local_dynamics field even names this term ("the local partial-derivative of level ℓ+1's reconstruction term w.r.t. x^(ℓ)"). That IS a downward error/generative path: a top-down error signal routed via W^T (weight transport). The defense "the SAME forward W_ℓ appears so it does not implement transpose-credit-assignment" is self-refuting — using the forward weight's transpose for the downward error is precisely weight transport, the canonical mechanism by which predictive coding equals backprop (Millidge 2020). The project's OWN admissibility line (notes/emergent-codebook/phase-3-multilayer-deflation-precommit.md:36-44, §2) sets the structural guard verbatim: "signal flows UP ONLY — NO downward weight, NO target/label tensor, NO two-phase free/nudge, NO autoencoding objective." N9's per-level reconstruction-of-the-faster-level IS an autoencoding/reconstruction objective whose shared-energy settling carries a downward W^T error term — it fails this guard at every clause except absence of an explicit label tensor. The author's honest_risk concedes the knife-edge ("if making it WORK requires propagating error through MORE THAN ONE level... it becomes deep predictive coding = backprop = BANNED"), but the violation does not require multi-level propagation: a single level of downward W^T·error flow is already a downward error path, and it is in the spec as written, not a hypothetical. NOTE: the energy/Lyapunov invariant (4) is satisfied (an explicit shared E whose settling is the computation) and the learning rule's outer-product form is locally computable IF the post-error were truly local — but the post-error at level ℓ is contaminated by the downward W^T term during settling, so the "local pre × post-error" learning rule consumes a non-local (downward-propagated) quantity, which independently brushes invariant (3). The dispositive violation is invariant (1)/(2)-Millidge.

## Completeness critic (the un-invented direction)

The code confirms the brief's claims (bundle = normalize(sum); Hopfield energy is logsumexp-over-stored). I have what I need. The KILLED candidate (LPR) and all 11 survivors are fully specified in the brief. Writing the critique.

---

**COMPLETENESS CRITIQUE — from-scratch invention round**

**(1) The substrate-class NO seed invented.** Every survivor is a *node-and-edge* energy: state lives on units/oscillators/traces, and learning shapes pairwise (or at most 3-body) *couplings between fixed-identity units*. No seed invented a substrate where **the metric/geometry of the space is itself the plastic, energy-bearing object** — i.e. where "two things are similar" is not stored in a weight `W_ij` but in a *learned distance/connection-density* that reshapes which states are even reachable. The brain-principles point straight at this: "ENERGY/ATTRACTOR SETTLING IS THE COMPUTATION" + "the landscape geometry IS the rule" (sense-3) implies the *manifold* should be grown, not just the couplings on a fixed manifold. STRATA gestures at a "product manifold" but treats it as a fixed coordinate system the tiling lives *on*; it does not let experience *grow the manifold's connectivity*. The un-invented class: a **growing relational graph/simplicial-complex substrate** where per-occurrence experience adds nodes and *edges* (structural plasticity = the substrate's topology changes), the energy is a graph-Laplacian/diffusion-settling functional, and abstraction = emergent *graph communities* (the partition global k-means reaches becomes a **spectral-clustering-by-local-diffusion** that never computes a global eigenvector — diffusion settling reaches community structure locally). This is the one shape that natively dodges the single-projection bound: communities are not eigenvectors of a fixed operator over a flat code; they are basins of a *grown topology*. No seed has it.

A second un-invented class: a **stochastic / sampling energy** (Langevin/Gibbs settling, an explicit *temperature* as a neuromodulator). Every survivor settles *deterministically* (coordinate descent, gradient flow, Kuramoto sync). But "generative" in the strict sense — synthesize a completion never stored, drawn from a learned distribution — wants *sampling from* the energy, not descending *to its argmin*. Deterministic descent over a multi-basin energy gives you the nearest stored basin = the memory-not-learner floor in new clothes. A sampling substrate (energy as `-log p`, settling = annealed sampling, ACh/NE as the temperature schedule, replay = sampling-to-consolidate) is the most direct route to a *generative* energy and to bind-vs-bundle as a *likelihood-ratio* (bound = a sharp joint mode, bundled = a factorizable near-independent pair — a quantity the energy *measures* without any arbiter). No seed sampled.

**(2) The box-within-the-box — three shared assumptions:**

- **(a) Couplings are plastic, identities are fixed.** All 11 survivors keep a fixed unit/cell/trace *roster* and learn the *weights* between them (even HYDRA's fission and SPARTA's neurogenesis *add* fixed-identity units; they don't make identity itself fluid). The deeper brain fact — that *which* cells code *what* is itself continuously re-tiled (representational drift, remapping) — is untouched. The metric isn't plastic; only the springs on a fixed lattice are.

- **(b) Settle-to-a-point (or to-a-phase-config), then read.** Even the "generative" seeds output the *fixed point* of a descent. None makes the *trajectory* the answer. The brain-principle on oscillatory multiplexing + the target's *analogical/structural retrieval* both suggest the object that matters is a **relational trajectory** ("X did Y to Z" is a *path through state space*, and a same-shape-different-content match is a *path of the same shape* offset in content) — i.e. abstraction as an **invariant of the flow field, not a basin of the potential**. RAFT alone reaches toward this (tangent-match), but it stores tangents as discrete cells and still settles to a point; it does not make the *vector field itself* the memory. A substrate whose memory is a *learned dynamical flow* (the energy gradient field is the object, structural match = two cues whose local flows are conjugate) is un-invented.

- **(c) Time is a settling-loop counter, not a substrate dimension.** The oscillator seeds (EMBER, RESONANT-OFFSET, TEPID) use phase, but the *slow consolidation/replay time axis* is still an outer loop the designer runs. No seed makes **continuous time a genuine coordinate of the state** (a delay-embedding / spatiotemporal substrate where "now" and "recent-past" are *the same vector*, the way cortex holds a temporal context window in its persistent activity). Per-occurrence is treated as *which token*, never as *when*, despite "instance-resolved" in the gap_shape. The temporal-context substrate is the most literal reading of "per-occurrence held distinctly" and nobody built it.

**(3) Was LPR killed too hastily? — Yes, and revealingly so.** LPR (multi-timescale hierarchy, NO downward weights, abstraction from *timescale separation + sparsity*) was almost certainly killed because it reads as Report-132 ("depth + cheap deflation does not compose, mildly hurts"). But that is a **mis-identification**: Report 132 killed *deflation-PEEL stacking* (feedforward project-then-subtract). LPR's claim is the *opposite mechanism* — timescale separation under *joint* dynamics, no peel. The survivor CO-SETTLING LAMINA is **LPR with reciprocal coupling added back**, so the round implicitly conceded LPR's core was sound and only its missing-feedback was the flaw. Killing LPR while promoting CO-SETTLING LAMINA means the round killed the *honest no-feedback null hypothesis* and kept its more-decorated cousin. The genuinely-new bedrock LPR carried — **abstraction as a pure consequence of multi-timescale integration with NO inter-level weights at all** (the slowest level sees only the *common-mode* because slowness is a low-pass filter on *time*, not on *space*) — was never actually tested and is not equivalent to spatial deflation. That is a real un-banked escape mistaken for the banked Report-132 wall.

**(4) Honest meta — new, or recombination?** Mostly **sophisticated recombination of the project's own prior vocabulary**, with two genuine novelties. The recombination is structural: SPARTA, SIGNED-ASSEMBLY GLASS, and CO-SETTLING LAMINA are the *same* sparse-nonneg-recurrent-inhibition energy at 1, 1-with-frustration, and L levels — they are one idea at three depths, dressed as three inventions. The four oscillator seeds (EMBER, RESONANT-OFFSET, TEPID, and the phase-half of RAFT) are one idea — "store the relation as a phase offset" — and the brief itself flags that the phase channel was *already exercised and the single-projection bound held there* (Report 130). So roughly half the survivors re-enter a channel the null record already closed; their novelty is "but per-occurrence this time," which is real but is *one* new ingredient shared across all of them, not eleven inventions. The two genuine novelties are **(i) RAFT's tangent-operator energy** (judge by the *transform* not the *point* — this is the only seed that changes what the energy is *of*) and **(ii) STRATA's complexity-term-does-the-separation** (separation as the gradient of a *single* free energy, not a bolted-on force) — and even these are restatements of the Spisak-Friston and Dorrell *cards already in the brain_principles list*. The round did not escape its own gravity well; it re-derived its source cards as architectures.

**The single most important un-invented direction:** a **growing relational-graph substrate whose plastic object is the topology/metric itself, with settling by local diffusion and generation by annealed sampling** — i.e. (a)+(2a)+(1-sampling) fused. Concretely: per-occurrence experience instantiates nodes and adds *edges by a local co-activation rule*; the energy is a diffusion/graph-Laplacian functional; the partition that global k-means/SVD reach emerges as **graph communities found by local diffusion settling** (never a global eigenvector — community structure is a basin of a *grown topology*, the one object provably outside the fixed-operator single-projection bound that closed Families 1-3); abstraction = coarse community structure that appears as the graph densifies; bind-vs-bundle = an edge that *persists across contexts* (a stable structural tie) versus one that *decays* (transient co-fire) — a property the local edge-update dynamics *measure*, no arbiter; analogical retrieval = community-isomorphism surfaced by parallel diffusion from two cues; and it is generative by *sampling* the diffusion rather than descending it. This attacks the diagnosed root (no flat type-averaged code — the code is the node's *position in a grown graph*), it is the only substrate-class where the load-bearing global-partition step has a *provably-local* analogue (diffusion → spectral communities), and it is genuinely outside the project's prior vocabulary, which has never once let the *substrate's topology* be the plastic variable.
