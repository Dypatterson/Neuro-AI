# Neuro-AI — Research-Grounded Forward Plan (FINAL)
> Produced 2026-05-30 by the `neuro-ai-ground-and-brainstorm` + `neuro-ai-plan-and-grill` workflows
> (domain-expert grounding on the ground-truth corpus + 21 PDFs + 33 new web sources; 3-framing plan panel;
> 12-question grill with domain-expert defenders; anti-homunculus + done-gate + terminology audits; hardened).
> **Nothing here edits binding docs — every STATUS/spec change is surfaced for your sign-off.**

## Approach
**Surgical-in-place, gated by five run-first adjudicators (G-0 algebra / G-A frozen-refit / G-B swap-negative A/B / G-C N-primary capacity surface / G-D three-read Selectivity-Δ), with rebuild-from-Phase-1 kept fully-specified but data-gated**

> Accept the verified root cause — the Phase-3/4 per-event consolidation write is an ALREADY-MARGINED pull/push contrastive blend (error_driven_learner.py:147-162) that failed (phase3b Fail Rate 1.0, seed 17) for three uncontrolled confounds (self-mined collapsing negative, top-1 readout, single seed), so a recoverable role→target basin was never written (verdict reached BY ELIMINATION, Reports 062-064, not by EBM's flat-surface theorem) — and run five cheapest-decisive run-first gates (G-0 algebra + G-A readout, co-equal first) to separate readout-vs-objective-vs-capacity-vs-binding-algebra before any objective rewrite or rebuild, then integrate the ONE winning structure-writing objective into the existing consolidation and re-validate the ladder; hand the user the evidence-backed four-way fork (readout-fix / surgical / mild-rebuild / rebuild) rather than presuming surgical.

## Core thesis
SPINE (rewritten per grill q1): the per-event consolidation write at error_driven_learner.py:147-162 (mirror reconstruction_learner.py:147-162, verified byte-identical) is a pull/push contrastive blend that ALREADY HAS a margin/push term over a self-mined predicted_id negative (sims.argmax at :106-107, used at :144) — it is NOT a members-mean / zero-margin / push-down-only write. consolidation.py:549/636 members.mean lives inside _basin_covariance / _spatial_bimodality_signal (docstring :538-540 'Return (centroid, members, tr(Σ))') — the tr(Σ)/anti-collapse DIAGNOSTIC, not the per-event write; BOTH the WF-1 diagnosis and the synthesis mis-located it (verified at primary this session). The write FAILED (phase3b Fail Rate 1.0000, ~29-45 events, seed 17; generalization 0.0397 < bigram 0.1987) — so 'add a margin' is NOT a novel fix, it is an already-falsified one. The defect is 'never wrote a RECOVERABLE role basin,' reached BY ELIMINATION (hit_role=0.000 exactly across D1/D3/E1/M1, Reports 062-064; sharpening monotonically WORSE, rank_role 205→403 / 205→498, 063:99-101 = anti-overlap signature of an ABSENT write), NOT by EBM-2006's flat-surface theorem (LeCun 2006, pdf:energy-based-learning-2006, stale_pdf, carded, reader-p.10/24) — which is a LOOSE ANALOGY the primary does not prove for a margin-bearing FHRR/MHN contrastive blend (a contrastive write WITH a push term is the regime EBM calls collapse-safe-ish, so EBM demotes from proof to motivating analogy). The three confounds the surgical re-run must change: (i) self-mined predicted_id → precommitted role-SWAP negative; (ii) top-1 cosine → basin-membership (top_index_hits/tix, 066:19); (iii) seed-17 → multi-seed. Two independent project lines converge on this ONE locus: Phase-5 role-binding (hit_role=0.000) and Phase-3 Frame B (consolidation injects variance σ_A=0.157 vs frozen σ_C=0.069, corr_AC_BD=0.10, Reports 116-118). The disciplined move is the cheapest decisive test FIRST — and write-then-read with a basin-membership readout is the experimental primitive the project has literally never run as a positive test.

## THE IMMEDIATE GATE (do this first — a few days of local MPS on a 4-role toy)

**Headline:** Stage-1 Selectivity-Δ (G-D), THREE NAMED READS (per grill q4 headline split — never one banner straddling reads of opposite empirical status): (R1) VALUE-CODEBOWK basin-membership Selectivity-Δ = top_index_hits(true-role cue) − top_index_hits(role-shuffled cue) over the value/atom codebook — the WORKING read (066:34/74-75, bundle-first tix=3072) that G-A/G-D/swap/predictive actually measure; (R2) KEY-ONLY-over-bound-pairs recoverability — the read 065/066 KILLED (tix=0/3072 FHRR+GHRR-native), re-probed by G-0/BTSP with a margin write; (R3) the Phase-5 role-binding read, re-opened only after a recoverable role basin is proven. The G-D graduation headline is R1. recoverability ≡ top_index_hits (066:19), NOT top-1 cosine. PASS only if: absolute true-role arm Wilson-CI lower bound > chance (1/N = 0.25 on the 4-role toy) AND Δ Wilson-CI > 0 (BOTH reported; a Δ>0 driven by a sub-chance shuffled arm is the anti-overlap signature and is REJECTED, not a pass) AND no-negatives ablation collapses Δ→0 AND random-codebook ablation collapses Δ→0. Ship ent + margin drill-downs so tix=0 is disambiguated from sharp-wrong-basin (066:36).

**Steps:**
1. WALK-BACK FIRST (per grill q7, CLAUDE.md:65 'walk-back is the first edit'; surfaced, NOT silently edited, HARD RULE 10): this is a (b)-shaped move — CLOSE the Frame-B level-DiD program and OPEN a new Phase-3 consolidation-write sub-program (different estimand: Recall@K level-DiD → top_index_hits Selectivity-Δ; different substrate: wikitext → 4-role toy; different control: real-vs-shuffle-world → role-shuffle). Edit 1 (mandatory, first): amend STATUS:18 Active-deliverable from 'STRATEGIC DECISION PENDING' to 'Frame-B level-DiD program CLOSED — not economically rescuable (Report 117), all three rescue levers closed (116/117/118). Superseded by a new Phase-3 consolidation-write sub-program (root cause: already-margined contrastive write that failed phase3b for three confounds; diagnosis 00-synthesis §1/§7).' Edit 2 (the headline re-point) is sign-off-gated, not done here.
2. START-NOW locus correction (does NOT change the spine, determines WHERE the surgical edit lands): the verified write is the pull/push blend at error_driven_learner.py:147-162 (avg_dir = normalize(stack(queries).sum); pull (1-lr)*atom+lr*avg_dir at :149-152; push (1+lr)*atom−lr*avg_dir at :158-160) with the self-mined predicted_id negative at :106-107/:144 and a runtime quality_threshold gate at :99. consolidation.py:549/636 members.mean is the tr(Σ)/anti_collapse DIAGNOSTIC (docstring :538-540), NOT the per-event write. Surface to the user.
3. Build the missing write-then-read primitive FIRST (per grill q2 — G-A is NOT 'zero new architecture'; the helper must precede the run): a basin-membership / top_index_hits readout helper (~40 lines reusing phase2/metrics.py:53 wilson_interval + the exp42 argmax-against-values logic + the phase4 basin buffer). Verified: no such reusable helper exists in src/ (grep empty). A measurement helper fit OFFLINE on a held-out split, never selecting at runtime — anti-homunculus exempt.
4. G-0 (co-equal RUN-FIRST with G-A, per grill q4): one-shot BTSP instructive write per (role,filler) on the 4-role toy, then read KEY-ONLY top_index_hits (the exact 065/066 null re-measured WITH a margin write). PREREQUISITE (HARD RULE 3): card BTSP-HDC (bioRxiv 2025.05.15.654220) + the HDC-attractor primary (PMC10484462) from primary BEFORE G-0 is load-bearing — schedule in parallel with the G-A card work. G-0 forms a key-only basin where 065/066 found none → algebra rescuable in-place → objective-rewrite program well-posed; G-0 still nulls → algebra wall holds even with a margin write → re-route fork toward bundle-first/substrate-swap WITHOUT burning the G-B+predictive+lower-D battery. NOTE: G-0 tests a fix-CANDIDATE (margin write), not the algebra in isolation; the already-run passive 065/066 key-only null is the clean algebra-only adjudicator, so a G-0 null strengthens but does not by itself prove the rebuild trigger.
5. G-A (co-equal RUN-FIRST, citation-light per grill q2/q11): run the EXISTING contrastive consolidation on the 4-role toy, freeze it, refit a fresh top_index_hits readout on a HELD-OUT role-cue split. DECISION RULE GROUNDED ON Reports 066 (top-1-vs-tix disagreement, ~7pt lucky-decode gap 066:37/78) + 111 (storage-changed-but-held-out-recoverability-didn't, 111:71/83-92) + existing random-codebook/role-shuffle controls — NOT on 2310.05644 (ABSENT from manifest; demoted to citation-pending-primary-read, the frozen-probe MECHANISM is self-contained). PRE-REGISTERED before the run: chance = 0.25 (4-role/N=4); refit recoverability Wilson-lower > 0.25 AND paired refit−production gap CI excludes 0; n ≥ 100 held-out cues (below n=100 underpowered, 'recovery' not declarable); random-codebook arm must give refit Wilson-upper ≤ 0.25 AND near-zero gap (else readout leaks labels → abort/rebuild helper); ≥5 seeds, verdict by per-seed sign agreement. Refit RECOVERS → defect is READOUT (flip rank-1, un-park GSBC as read-side fix, every write-side idea premature); refit FAILS → structure truly never written.
6. State the mandatory experiment preamble BEFORE running any gate (CLAUDE.md): Active phase = Stage-1 toy (pre-Phase-5′, fence DOWN); Headline = R1 value-codebook top_index_hits Selectivity-Δ, two-floor rule, per 00-diagnosis-synthesis.md §6 G-D + 01-brainstorm idea-9; Required controls = role-shuffled (read-time pairing-shuffle, see G-D spec) + no-negatives + random-codebook + content-matched non-positional shuffle + lower-D control + perfect-cue UPPER-BOUND arm; Last verified result = Reports 062-066 null, 116-118 Frame-B closed; Why now = directly tests the elimination-reached root cause before any rewrite. NOTE: until the Phase-3 headline re-point is signed, Selectivity-Δ at Phase-3 scale is by the preamble rule a DRILL-DOWN, not a graduation metric — the report must say so.
7. Run the decision-FREE manifest/card prereqs IN PARALLEL but INVERT the dependency (per grill q11): carding 2310.05644 + 1909.03368 from PRIMARY must PRECEDE any numbered-report CITATION of them and is LICENSED to return 'does not transfer' and drop the cite (2310.05644's abstract describes a frozen-deep-net linear-probe protocol, NOT FHRR/MHN basin membership — same over-claim flagged for EBM-2006; brief line 70 open TODO confirms the primary was never opened). Also card: ArSyD 2412.19847 (ABSENT) + FEP 2505.22749 (link_only) before any swap/predictive write; MQAR 2312.04927 / 2406.03980 (link_only) — the project has NO carded write-then-read methodology. 1909.03368 is NOT 2406.03980 ('Embracing Negative Results') — do not swap one for the other. EBM-2006 carded but its FHRR/MHN collapse is an ANALOGY — label as such.
8. Surface (do NOT silently edit, HARD RULE 10) the STATUS range-lane contradiction: propose a STATUS amendment splitting 'range_postsettle storage-side CLOSED (Report 111: not_viable...novelty_without_retrieval, 111:44/71)' from 'Dorrell nonneg-AE TRAINING-objective OPEN/deferred (Report 068:120-128 "That work is deferred")'. Append the readout-scope clause (per grill q8): Dorrell is Phase-3/4-owned (manifest phase_relevance:[phase3,phase4]), fence-CLEAR as a Stage-1 candidate ONLY when read by Selectivity-Δ; the ΔE re-run bundled into 068:128 remains FENCED. 068:188's 'Phase 5' design question' tag refers to the windowed-vs-single rebind VARIANT, not the objective. Verified at primaries; mirrors the freq-weighted-α walk-back precedent.
9. G-B (sign-off gated, behind the Cell-A/Cell-B margin-existence prereq per grill q3): FIRST run the cheap no-sign-off margin-existence gate reusing experiments/42_mqar_external_gate.py — Cell-A bundle-first arm (066:53, tix=3072) as positive control for 'a writable key-only margin can exist on this substrate at all,' in the SAME readout harness; Cell-B single unbind(bind(k,v),k)→v round-trip margin floor (066:19) with no write. THEN re-run the phase3b error_driven harness with the three surgical edits (swap-negative replacing self-mined predicted_id at :106-107/:144 — PRECOMMITTED real other-example filler sharing the role, drawn by fixed role-equivalence-class membership + seed-fixed permutation, NEVER sims.argmax; basin-membership replacing top-1; multi-seed) A/B vs the recorded Fail Rate 1.0000. Precommitted falsifier: G-B is falsified-as-contrastive ONLY IF its swap-negative Selectivity-Δ is null WHILE (i) bundle-first holds tix>0 in the identical harness AND (ii) G-A refit on the G-B-trained codebook also fails. If bundle-first ALSO collapses → run VOID (readout defect). If bundle-first holds and G-B nulls → verdict 'contrastive-write cannot carve the bind-pair landscape' → route to bundle-first ARCHITECTURE, NOT to 'substrate can't hold a margin.' Report whether the swap-negative lands at non-trivial similarity (drill-down) — else a null is uninterpretable.
10. G-C (sign-off gated; N-PRIMARY capacity surface per grill q5, MANDATORY not optional per grill q9): vary N as the primary axis (N↓ predicted tix RISES toward low-N residual — the real capacity lever), D as a separately-swept secondary axis (D↑ predicted flat-to-slightly-better; D↓ a PREDICTED-NULL CONTROL, not a rescue arm — lowering D at fixed N makes 1/√D crosstalk WORSE), and score-scale (β) as its own axis (separates 'basin exists but blurred' from 'no basin'). ONE axis per arm, sign pre-committed in the preamble. Read on tix not top-1. Specify the D grid (512/1024/2048/4096) so a monotone trend not one point adjudicates. The rebuild trigger fires only on the CONJUNCTION; lower-D demoted from 'adjudicator' to 'predicted-null control.'
11. G-D: compute the R1 value-codebook Selectivity-Δ headline with the two-floor rule + Wilson CI for each candidate write, with the mandatory collapse + selectivity + upper-bound controls. WRITE reports/NNN + STATUS walk-back as an explicit final gate step (per done-gate audit gap: report/STATUS must be enumerated INSIDE the gate, not left only in done_gates).

**Controls:**
- random-codebook ablation (must collapse Δ→0; must give G-A refit Wilson-upper ≤ 0.25 AND near-zero refit-vs-production gap — else readout leaks labels, abort)
- no-negatives / no-swap ablation (must collapse Δ→0 for any contrastive/swap candidate)
- role-shuffled cue = READ-TIME role-filler PAIRING shuffle over fixed DISTINCT position-roles (per grill q6: passes the C1/C2 gauge-vacuity falsifier of note:66-86 because roles within an item are distinct, NOT exchangeable; NOT a codebook-relabel which would be C.3-vacuous); plus a precommitted C.3-style E-arm (identity-shuffle → byte-identical; random-shuffle → Δ scatters around 0 from ABOVE chance)
- content-matched non-positional shuffle (per grill q6: because role≡position at encoding.py:38, random-codebook + no-negatives exclude NEITHER position-legibility NOR content-overlap; this is the control that makes Δ>0 a ROLE result; restored from idea-9 01:77)
- perfect-cue / clean-codebook UPPER-BOUND arm on the same test set (per done-gate audit: bounds the ceiling so a null is attributable to 'objective didn't write' vs 'toy has no recoverable structure in principle'; 066:74-75 perfect-cue=100%)
- lower-D control arm (G-C: D↓ is a PREDICTED-NULL control; the capacity rescue direction is N↓ and D↑, not D↓)
- Cell-A bundle-first positive control + Cell-B single-round-trip margin floor (margin-existence prereq ahead of G-B)
- frozen-substrate refit readout (G-A: rules out a readout-only effect)
- ent + margin drill-downs (066:36: disambiguate tix=0 'no basin' from sharp-wrong-basin)

**What the gate's output resolves:** Resolves readout-vs-objective-vs-capacity-vs-binding-algebra WITHOUT presuming surgical at the gate's output. G-A refit-RECOVERS → READOUT defect, objective-rewrite premature, un-park GSBC (read-side fix). G-0 forms a key-only basin → algebra rescuable, fold BTSP into Phase-4 consolidation. A candidate write produces R1 Δ>0 with controls collapsing AND the upper-bound arm non-trivial → SURGICAL-IN-PLACE. G-C N-sweep shows a margin-write lift above the no-write control at low N, OR D↑ rescues → CAPACITY/load defect, fix is dimension/orthogonalization, GSBC un-parks (mild-rebuild, Phase-1-substrate-only). G-B null WHILE bundle-first holds tix>0 → 'contrastive-write cannot carve the bind-pair landscape' → route to bundle-first architecture (NOT a false 'substrate can't hold a margin'). Full-null trigger (G-A fail ∧ G-0 fail ∧ Stage-1 null across contrastive AND predictive ∧ G-C no N-rescue/no D↑-rescue ∧ BTSP key-only fail) → rebuild-from-Phase-1 fires. At the gate's OUTPUT hand the user the evidence-backed four-way fork (readout-fix / surgical / mild-rebuild / rebuild).

## Every-phase map

### Phase 0 — `keep-as-is`
- **Purpose:** Energy-memory kernel: prove the smallest memory-first substrate retrieves by lived association, not content similarity (temporal recall beats shuffle; coupled content+temporal rescues entangled anchors; trajectory metrics expose commitment vs ambiguity; MPS hot path).
- **Changes vs current:** No change. Phase 0 implemented (PROJECT_PLAN:103-116, Reports 001-015); nothing in the verified diagnosis touches the kernel. The diagnosis locus is the consolidation/learning WRITE, far downstream.
- **Headline:** Temporal-recall Recall@K minus temporal-shuffle Recall@K, strictly > 0 with CI.
- **Controls:** temporal shuffle; content-similarity distractors
- **Anti-homunculus:** PASS unchanged: coupled settling is a local energy dynamic; no module decides which subsystem wins. No new mechanism added.

### Phase 1 — `amend`
- **Purpose:** Scaled-substrate validation: toy D=512 → realistic D=4096 FHRR; batched MPS bind/unbind/Hopfield; quantify footprint and latency.
- **Changes vs current:** LABEL CHANGED per grill q9 (surfaced, HARD RULE 10): was 'keep-as-is'; corrected to 'exit-criterion-incomplete'. Phase 1 passed throughput/round-trip/footprint (Report 014; PROJECT_PLAN:130-135) but NEVER gated key-only / role-only basin recoverability — the exact capability Reports 065/066 found absent (key-only→chance with N; 065 D-2 perfect-cue=100% storage works; 066 bundle-first tix=3072 sole rescue). That capability is OPEN, tracked FORWARD on the consolidated write (G-D) and the lower-D/N arm (G-C), NOT retroactively re-gated on the raw untrained bind. Do NOT promote raw-bind key-only basin recoverability to a current-D Phase-1 re-run gate — that imports the rebuild branch's exit criterion and pre-decides the fork; 065 D-2 geometry (bind(k,v)⊥k at cos≈1/√D) shows raw-bind key-only addressability is marginal by construction and was never an intended Phase-1 deliverable. In the full rebuild branch this becomes the load-bearing phase.
- **Headline:** Bind/unbind round-trip fidelity at D=4096 (cosine of recovered vs original) above a fidelity floor; bundling recovery stable across context sizes. (Rebuilt branch ONLY: key-only top_index_hits at D=4096 as the NEW exit gate — the 065/066 null inverted; fenced inside the rebuild branch, NOT a current-D gate.)
- **Controls:** D-sweep (D grid 512/1024/2048/4096, same arm G-C uses); random-codebook baseline for round-trip
- **Anti-homunculus:** PASS: bind/unbind/bundle are fixed algebraic operations; the D-sweep is an offline batch comparison, not a runtime metric-reader.

### Phase 2 — `amend`
- **Purpose:** Static contextual-completion baseline: static random codebooks + Hopfield retrieval for masked-token completion; full matrix runner; WikiText-2 pipeline; bigram/unigram/random baselines; cap-coverage / metastable / entropy / energy metrics.
- **Changes vs current:** One ADDITION, not a rebuild: register the top_index_hits (basin-membership) readout built in the immediate gate as a first-class Phase-2 eval primitive alongside top-1 Recall@K, because Report 066:37 shows top-1 cosine and top_index_hits can disagree (~7pt FHRR gap) and the project has only ever evaluated top-1 or storage-side deltas. Requires the eval-doctrine card (2312.04927/2406.03980, link_only) first per HARD RULE 3.
- **Headline:** Masked-token Recall@K vs bigram/random baselines (memorization clear, generalization failure modes measured not guessed).
- **Controls:** random codebook; shuffled token; bigram/unigram baselines
- **Anti-homunculus:** PASS: the new top_index_hits readout is an offline statistic fit on a held-out split that never selects at runtime; a measurement, not an arbiter. Random-codebook control must give a near-zero refit-vs-production gap.

### Phase 3 — `amend`
- **Purpose:** Growing codebook / continual learning: atoms that drift, stabilize, split, decay, consolidate from experience; Hebbian success pathway; error-driven learning ONLY in offline/batch.
- **Changes vs current:** Substantive (b)-shaped amendment (per grill q7): CLOSE the Frame-B level-DiD program (three levers closed: slope corr_AC_BD=0.10 R116; level-DiD not economically rescuable R117; landscape lever closed σ_A-irreducible R118) and OPEN a new Phase-3 consolidation-write sub-program. The diagnosis reframes the Frame-B variance finding as a SYMPTOM of the same root cause (consolidation injects variance because the contrastive write never deposits a recoverable basin). Make Phase-3's consolidation objective the carrier of the winning Stage-1 structure-writing write; change the error_driven_learner negative from self-mined predicted_id to PRECOMMITTED role-swap and its readout to top_index_hits. ⚠ This is TWO distinct acts: (1) a mandatory FIRST-edit Current-state walk-back closing level-DiD (start_now); (2) the headline re-point (level-DiD STATUS:17 → held-out top_index_hits Selectivity-Δ R1), a BINDING-SPEC change requiring explicit user sign-off (CLAUDE.md experiment-preamble rule). Until signed, Selectivity-Δ at Phase-3 scale is a DRILL-DOWN, not graduation; the report must say so. The preamble citation must point at the (amended) Phase-3 design doc, which does not yet exist — the spec edit is itself a sign-off-gated prerequisite.
- **Headline:** R1 held-out VALUE-CODEBOOK top_index_hits Selectivity-Δ of the consolidated codebook (true-role cue recoverability minus role-shuffled cue recoverability), two-floor rule (absolute true-role arm Wilson-lower > 1/N; Δ Wilson-CI > 0). The Phase-3 exit control is the read-time role-PAIRING-shuffle selectivity control (per grill q6), NOT the gauge-vacuous C.3 token-shuffle (STATUS:16).
- **Controls:** role-shuffled cue (read-time pairing-shuffle over fixed distinct position-roles; C1/C2 falsifier pass; precommitted C.3-style E-arm); content-matched non-positional shuffle (makes Δ>0 a ROLE not position/content result); no-negatives / no-swap ablation → Δ→0; random codebook → Δ→0; perfect-cue upper-bound arm; lower-D/N arm (G-C); real-vs-shuffle-world matched control (Frame-B's control, retained as a drill-down)
- **Anti-homunculus:** PASS for offline-statistic candidates (swap-reconstruction, rectangular-support AE): negatives are PRECOMMITTED real-example swaps drawn by FIXED role-equivalence-class membership + seed-fixed permutation, NEVER by sims.argmax / nearest-mean / most-offending (per grill q12 + AH review — the current error_driven_learner.py:106 argmax-mined predicted_id is the thermostat being REMOVED; at scale the partner must be drawn uniformly-at-random within the role class under a fixed seed, never by metric). Modularity is the geometric consequence of a fixed batch objective. PASS for local-dynamic candidates (FEP self-orthogonalization, predictive settling, EM clustering) ONLY if the target is a fixed function of cue/data, never a label. BANNED: any runtime error-driven update (STATUS:44) and any recall-gated plasticity whose gate reads a metric and branches (thermostat — the single flagged thermostat candidate, explicitly NOT authorized).

### Phase 4 — `amend`
- **Purpose:** Replay and trajectory consolidation: turn successful settling paths into reusable memory grooves; replay scheduler on local tension; replay-and-re-encode for stale patterns; multi-timescale consolidation.
- **Changes vs current:** This is the SURGICAL TARGET. The verified write is the pull/push contrastive blend at error_driven_learner.py:147-162 / reconstruction_learner.py:147-162 (verified byte-identical), NOT consolidation.py:549/636 (the tr(Σ)/anti_collapse diagnostic — both the WF-1 diagnosis and the synthesis mis-located it). The change: replace/augment the per-event write with the Stage-1-winning structure-writing objective so consolidation lays down a recoverable role→target basin WITH A MARGIN (the surgical edit lands at :147-162, a negative-construction + readout change — the margin term already exists; the fix is the SELF-MINED negative + top-1 readout, not 'add a margin'). KEEP the basin-buffer, the tr(Σ)/anti-collapse covariance diagnostic, the Benna-Fusi multi-timescale machinery, and the tension-driven replay scheduler; swap ONLY the write rule. Re-validate exit criteria against top_index_hits. Retain PROJECT_PLAN:194-196 standing exit ('replayed trajectories improve future retrieval') as secondary so the basin-membership headline does not silently replace the existing Phase-4 exit without sign-off.
- **Headline:** Δ retrieval-recoverability (top_index_hits) of replayed-then-consolidated role→target associations vs frozen pre-replay, Wilson CI > 0 on a held-out split; replayed-trajectory Recall@K improvement retained as the standing Phase-4 exit (PROJECT_PLAN:194-196).
- **Controls:** no-replay control (same test set); random-codebook; shuffled role-filler pairing; frozen-substrate refit readout (rules out a readout-only effect); centroid-mean / current-write arm (must reproduce the null — built-in same-test-set negative anchor); perfect-cue upper-bound arm; lower-D/N arm
- **Anti-homunculus:** PASS: the replay scheduler stays tension-driven (a fixed function of local tension, NOT a controller that reads a metric and triggers replay). The new write rule must be the offline/batch ReconstructionLearner-style pass (STATUS:44 online error-driven BANNED), NOT wired into the streaming loop; do NOT add a runtime gate that reads recoverability and triggers re-consolidation (thermostat). The covariance/centroid diagnostics are measurements of a local dynamic (exempt; anti_collapse_force at consolidation.py:556-581 is a fixed local repulsive force with a λ=0 short-circuit, not an if/then). Sanctioned drift sources unchanged (phase-4-unified-design.md:296-309).

### Phase 5 — `amend`
- **Purpose:** Structure and abstraction: role/filler FHRR binding beyond fixed position vectors; bind-vs-bundle discovery; atom splitting for persistent multimodality; higher-level phrase/schema attractors; retrieval-shaped analogy probes.
- **Changes vs current:** Revisited once the write is fixed. Phase-5 retrieval-time interventions are CLOSED at smoke scale (D1/D3/E1/M1 null, Reports 062-066) and the binding-algebra wall (065/066: no key-only basins for ANY operator) is NOT excluded by the diagnosis. The change: do NOT re-run Phase-5 role-binding until Stage-1 (R1) + Phase-4 integration prove a recoverable role basin exists to retrieve; THEN re-open the role-binding probes (R3) against the now-non-flat consolidation. Bundle-first (066/067, tix=3072 — the sole demonstrated key-only rescue, an anti-homunculus-clean storage-architecture change) is the carded surgical candidate if the algebra is the wall. If G-0/BTSP show the binding ALGEBRA itself cannot hold a key-only basin even with a margin write, Phase 5's role/filler clause forces the deeper substrate question — the surgical/rebuild hinge. A null on the R2/key-only read must be labeled 'algebra wall, not objective failure' to avoid mis-attribution.
- **Headline:** R3 key-only (role-only cue) top_index_hits recoverability above chance with Wilson CI — the exact 065/066 null (tix=0/3072 FHRR + GHRR-native), re-measured after the write is fixed. Distinct from the Phase-5-prime ΔE energy headline (paused). A null is attributed via the BTSP/bundle-first A/B as 'algebra wall' vs 'objective failure.'
- **Controls:** random codebook; shuffled role-filler eligibility → no basin; key-only vs full-cue comparison; bundle-first vs bind-pair architecture A/B (the adjudicating arm); lower-D/N arm
- **Anti-homunculus:** PASS: bind/unbind are fixed algebra; bundle-first reorganizes WHAT is stored WHERE (storage architecture, the only AH-clean rescue per 066:53), not a runtime arbiter; atom splitting must read local basin bimodality geometry (a fixed function of the basin buffer), NOT an 'if recoverability < θ then split' rule. Retrieval is settling, not arbitration.

### Phase 5-prime — `paused-no-change`
- **Purpose:** Role-prior energy: ΔE = E_content-prior − E_role-prior as a geometric pressure that makes role-structured completions lower-energy than content-only completions.
- **Changes vs current:** PAUSED — fence stays DOWN. NO change authorized. The Stage-1 write-then-read toy is legitimately PRE-Phase-5-prime (it uses R1 Selectivity-Δ recoverability, NOT ΔE) and does NOT require lifting the fence. FENCE BRIGHT LINE (per grill q10, grep-able + refactor-proof, REPLACING the 'different module' framing which is FALSE — m1_role_energy.py DEFINES and CALLS mhn_energy() = -logsumexp(βs)/β at :407/:453/:634 and stored ΔE≈-0.32 in Report 064, so the energy reduction is co-located IN the 062-064 module Phase 5 re-runs): the shared torch_hopfield._scores / substrate.similarity_matrix matmul is PERMITTED (a measurement, AH-exempt); a read path is ACROSS the fence iff it feeds those scores into _energy_from_scores / mhn_energy to emit a per-branch scene-energy SCALAR aggregated by min/argmin-over-branches and compared as ΔE. A compliant Stage-1/Phase-5 graduation read path MUST terminate in a top_index_hits equality COUNT (Wilson-CI'd) and MUST NOT call cue_conditioned_scene_energy / delta_e_content_minus_role (bridge_readouts.py:17,38) / raw_scene_energy_v0 / any min-over-branch-energy aggregator. A Stage-1 win must NOT silently creep into ΔE work. A successful native role-writing rebuild could make the ΔE retrofit OBSOLETE.
- **Headline:** ΔE = E_content-prior − E_role-prior, magnitude floor 5.5e-3, CI strictly > 0 (phase-5-unified-design.md:282-297) — BINDING ONLY when Phase 5 reopens and the fence is lifted. Not measured in this plan.
- **Controls:** the raw_scene_energy_v0 saturating readout (audit:128-129, E=-1.0) must be de-saturated first — a floor on a saturating quantity is uninterpretable; the min_branch aggregator (audit:130-131) must be DE-ARBITRATED (anti-homunculus FAIL) before any reopen; standard role-shuffle + random-codebook controls (apply only after the readout is de-saturated)
- **Anti-homunculus:** FLAGGED — FAIL on the EXISTING min_branch aggregator: it is an arbitration-shaped router that selects the winning branch (thermostat) per the 2026-05-26 audit §9 (audit:130-131). It is a PRE-EXISTING fence, NOT a new proposal; this plan does NOT authorize it and requires it be de-arbitrated (replace min-over-branches with a single fixed energy over the joint state, or recast as an offline statistic that never selects at runtime) before any Phase-5-prime reopen. ΔE itself is a measurement of an energy difference (exempt) once the readout saturation is fixed. This plan does not touch it.

### Phase 6 — `not-yet-reached`
- **Purpose:** Predictive world model and latent rollouts: small JEPA-style latent transition model; latent rollout search over candidate next states; coherence/value model from success and feedback; energy coupling between memory, world, value terms.
- **Changes vs current:** Not yet reached. One forward-looking cross-link: the candidate-2 predictive consolidation write (DreamWeaver/JEPA port, cue-derived self-target; 2501.14174 ABSENT from manifest, pdf:pam-2026 carded stale_pdf, 2502.05164 link_only) is a SMALL down-payment on Phase 6's JEPA transition model. If the predictive write wins Stage-1, Phase 6 inherits a validated FHRR-native unbind-then-rebind predictor — an acknowledged phase-order skip (flag per CLAUDE.md). The EMA/stop-grad self-target is local-dynamic AH-PASS (per grill q12) conditional on the target being a fixed function of cue/data, never a runtime-selected exemplar. No change to Phase 6 scope now.
- **Headline:** Latent-rollout problem-solving accuracy vs one-step retrieval, CI > 0.
- **Controls:** one-step retrieval baseline; shuffled-transition control; value-pressure-ablated control
- **Anti-homunculus:** PASS by design: value must remain a local energy term, not a controller (PROJECT_PLAN:235); rollout search is settling over candidate states, not an if/then arbiter; the Phase-6 predictive target must be a fixed function of data, not a label.

### Phase 7 — `not-yet-reached`
- **Purpose:** LLM interface: input parser text→workspace cues; memory-to-language decoder; small local LLM for fluent generation; conversation traces into episodic memory; user feedback as consolidation signal.
- **Changes vs current:** Not yet reached. No change. The non-negotiable rule (LLM is voice, not the source of persistence or identity; PROJECT_PLAN:276-277) stands and is unaffected by the consolidation-write repair.
- **Headline:** Fraction of LLM outputs grounded in retrieved/consolidated memory (grounded vs hallucinated), with swap-the-LLM identity-preservation check.
- **Controls:** swap-the-LLM control (identity must persist); memory-ablated control (output must degrade)
- **Anti-homunculus:** PASS by design: the LLM is a decoder over settled memory state, not a controller; user feedback enters as a consolidation signal (a write), not a runtime arbiter.

## Rebuild-from-Phase-1 branch (fully specified, data-gated, NOT authorized now)

**Trigger (must ALL hold):** Fires ONLY if ALL of these return null together: (1) G-A frozen-refit FAILS to recover role-target above chance (structure truly never written, NOT a readout artifact); AND (2) Stage-1 R1 Selectivity-Δ is null across BOTH the contrastive/swap family (G-B swap-negative + top_index_hits, multi-seed) AND at least one predictive/local-dynamic family (FEP single-phase OR predictive cue→co-filler), with no-negatives/random-codebook controls confirming the nulls are real AND the perfect-cue upper-bound arm proving the toy is in-principle recoverable; AND (3) the G-C N-sweep shows NO margin-write lift above the no-write control at low N AND D↑ does NOT rescue (rules out a capacity/load floor — if N↓ or D↑ RESCUES, the defect is CAPACITY and the fix is dimension/GSBC, a MILD-rebuild, NOT a full rebuild; note D↓ is a predicted-null control and its null is NOT evidence for rebuild per grill q5); AND (4) the G-0/BTSP key-only probe also fails to form a key-only basin even with a margin write (the binding ALGEBRA, not just the objective, is the wall: 065/066's no-key-only-basin-for-ANY-operator holds even with a margin write). Any single rescue keeps the surgical branch alive. SOFTER MILD-REBUILD sub-branch: G-C N↓/D↑ RESCUES → GSBC un-parks (2303.13957, link_only, card first), Phase-1-substrate-only, Phases 2-5 amend not rebuild. HONEST CAVEAT: the verified lean is SURGICAL-IN-PLACE (00:104); this full rebuild is the LOW-probability contingency and is NOT authorized now — it requires the user's explicit go after the gate returns the all-null verdict.

**Sketch:** SALVAGED (non-negotiable keeps): pure-Python FHRR+Hopfield reference backend (PROJECT_PLAN:278), torch/MPS batched ops, phase2/metrics.py (cap-coverage/metastable/entropy/Wilson), phase2/encoding.py, Benna-Fusi multi-timescale machinery + tension-driven replay scheduler (only the per-event write swapped), atom metadata/decay/anti-collapse/bimodality machinery, the WikiText-2 pipeline, regime_diagnostic.py. REWRITTEN: the binding-substrate representation + the per-event consolidation write. Rebuilt Phase 1 re-poses the substrate question 065/066 keep live — the FHRR bind/unbind algebra cannot hold a key-only basin for high-D approximately-random bound vectors (066:45 conclusion). It swaps the binding substrate rather than the objective; candidates each carded from primary first: bundle-first storage as the native architecture (066/067: tix=3072, the sole demonstrated key-only rescue — store the bundled superposition in one slot, MHN over the VALUE codebook as cleanup); GSBC confined-crosstalk (2303.13957, link_only, the parked kickoff at notes/notes/2026-05-30-gsbc-substrate-swap-kickoff.md un-parks here, but ONLY paired with a write objective — GSBC is a READ-side legibility upgrade that writes nothing, never substituted for a write); or a Krotov-Latham dense-AM. ⚠ DENSE-AM AH CONSTRAINT (per grill q12 + AH review): the Krotov-Latham dense-AM is admissible ONLY as a control ARCHITECTURE (fixed threshold-nonlinearity energy landscape); its native NON-LOCAL learning rule is BANNED as a write (FAILS the AH filter — non-locality is the recurring signature of a hidden global-objective supervisor; the local write must be supplied by swap/BTSP/predictive). OWED CONTROL: does the dense-AM attractor structure depend on having been WRITTEN by its non-local rule? Control: does a local-write-into-dense-AM produce key-only basins AT ALL? If the landscape is only well-formed when the non-local rule built it, 'architecture-only' is not separable and the dense-AM arm is not load-bearing for the rebuild verdict; before load-bearing, open dense-associative-memory-2016 (stale_pdf, carded) at primary and confirm its update is a fixed energy-gradient dynamic, not a runtime-metric-gated update. The rebuilt Phase-1 exit gate becomes the 065/066 null INVERTED: key-only top_index_hits above chance at D=4096 with a no-write control that must fail. Rebuilt Phase 2 re-runs the static baseline on the new substrate with top_index_hits as a first-class metric. Rebuilt Phase 3 grows the codebook with the structure-writing objective baked INTO the consolidation write. Pivot fallback (if even the rebuilt algebra shows no key-only basin at lower-D): accept a weaker contextual-completion claim or pivot the architecture target — do NOT keep adding objective terms to a substrate that structurally cannot hold the basin. COST: HIGH (weeks, cascades Phase 0→2 re-port). The user is WILLING but only AFTER the cheap write-then-read toy, never before.

## Start-now steps (decision-free)

1. WALK-BACK FIRST (per grill q7, CLAUDE.md:65; surfaced not silently edited, HARD RULE 10): amend STATUS:18 Active-deliverable from 'STRATEGIC DECISION PENDING' to record the resolution — 'Frame-B level-DiD program CLOSED — not economically rescuable (Report 117), all three rescue levers closed (116/117/118). Superseded by a new Phase-3 consolidation-write sub-program (root cause: already-margined contrastive write that failed phase3b for three confounds; diagnosis 00-synthesis §1/§7).' This is the FIRST edit, distinct from and prior to the sign-off-gated headline re-point.
2. Surface the load-bearing locus correction (does NOT change the spine, determines WHERE the surgical edit lands): the verified write is the pull/push contrastive blend at error_driven_learner.py:147-162 (avg_dir=normalize(stack(queries).sum); pull :149-152; push :158-160) with the self-mined predicted_id negative at :106-107/:144 (sims.argmax) and a runtime quality_threshold gate at :99, mirrored byte-identically at reconstruction_learner.py:147-162. consolidation.py:549/636 members.mean is the tr(Σ)/anti_collapse_force DIAGNOSTIC (docstring :538-540), NOT the per-event write — correcting BOTH the WF-1 diagnosis and the synthesis. The spine (the write failed for three confounds; basin never written by elimination) holds either way.
3. Build the missing write-then-read primitive FIRST (gates G-A/G-D, per grill q2): a top_index_hits / basin-membership readout helper reusing phase2/metrics.py:53 wilson_interval + the exp42 argmax-against-values logic + the phase4 basin buffer (grep confirms none exists in src/). A measurement helper fit offline on a held-out split, not a new architectural module.
4. Run G-0 + G-A as co-equal RUN-FIRST gates (per grill q4) once the readout helper + cards exist. G-0 = BTSP key-only margin write (cards BTSP-HDC 2025.05.15.654220 + PMC10484462 from primary first, HARD RULE 3). G-A = frozen-substrate refit-readout, grounded on Reports 066+111 (NOT absent 2310.05644), pre-registered: chance 0.25, refit Wilson-lower > 0.25 AND paired gap CI excluding 0, n≥100, random-codebook control, ≥5 seeds.
5. State the mandatory experiment preamble BEFORE running any gate: Active phase = Stage-1 toy (pre-Phase-5-prime, fence DOWN); Headline = R1 value-codebook top_index_hits Selectivity-Δ, two-floor rule; Required controls = role-pairing-shuffle + content-matched non-positional shuffle + no-negatives + random-codebook + perfect-cue upper-bound + lower-D/N; Last verified result = Reports 062-066 null, 116-118 Frame-B closed; Why now = directly tests the elimination-reached root cause before any rewrite. Until the Phase-3 headline re-point is signed, Selectivity-Δ at Phase-3 scale is a DRILL-DOWN.
6. Run the manifest/card prereqs with INVERTED dependency (per grill q11): carding 2310.05644 + 1909.03368 from PRIMARY must PRECEDE any numbered-report CITATION of them and is LICENSED to return 'does not transfer' and drop the cite. Card ArSyD 2412.19847 + FEP 2505.22749 before any swap/predictive write; MQAR 2312.04927 / 2406.03980 (link_only) — no carded write-then-read methodology exists. 1909.03368 ≠ 2406.03980. EBM-2006 carded but its FHRR/MHN collapse is an ANALOGY — label as such. Fix the research-brief citation-hygiene slips (Report 066 content tagged with a 1909.03368 URL, Report 111 with a 2310.05644 URL — internal claims carry reports/NNN, not arxiv URLs, HARD RULE 5).
7. Surface (do NOT silently edit, HARD RULE 10) the STATUS range-lane contradiction: propose a STATUS amendment splitting 'range_postsettle storage-side CLOSED (Report 111: not_viable...novelty_without_retrieval, 111:44/71)' from 'Dorrell nonneg-AE TRAINING-objective OPEN/deferred (Report 068:120-128 "That work is deferred")', with the readout-scope clause: Dorrell is Phase-3/4-owned (manifest phase_relevance:[phase3,phase4]), fence-CLEAR as a Stage-1 candidate ONLY when read by Selectivity-Δ; the ΔE re-run bundled into 068:128 stays FENCED; 068:188's 'Phase 5' tag refers to the rebind-variant choice, not the objective. Verified at primaries; mirrors the freq-weighted-α walk-back.

## Open user decisions (your call — sign-off items folded in here)

1. Run the Stage-1 write-then-read toy (the direct positive test) before any rebuild, or close the program on elimination? (Plan strongly recommends the former — G-A could falsify the whole objective-rewrite program in one afternoon.)
2. SIGN-OFF REQUIRED (binding-spec change, CLAUDE.md experiment-preamble rule): replacing the active Frame-B level-DiD headline (STATUS:17) with held-out value-codebook top_index_hits Selectivity-Δ (R1) for Phase 3. Approve, or keep level-DiD as the Frame-B headline and run Selectivity-Δ as a Stage-1-only metric? (The FIRST-edit walk-back closing the level-DiD program is mandatory regardless; only the headline re-point needs sign-off.)
3. SIGN-OFF (per grill q4): promote the BTSP key-only probe to a co-equal RUN-FIRST gate G-0 (gated on carding BTSP-HDC + PMC10484462 from primary), and split the Selectivity-Δ headline into its three named reads (R1 value-codebook / R2 key-only-over-bound-pairs / R3 Phase-5 role-binding)? Both touch binding structure.
4. DECIDE (per grill q3): given 065/066 already show bundle-first is the sole substrate config holding a key-only margin, DEMOTE G-B behind the Cell-A/Cell-B margin-existence gate (plan's recommendation: keep G-B but strictly gated, as cheap insurance on the negative-construction question) or CANCEL G-B and route straight to the bundle-first architecture candidate?
5. Confirm the STATUS range-lane amendment (storage-side range_postsettle CLOSED via 111 vs Dorrell TRAINING-objective OPEN/deferred via 068:128) AND the readout-scope clause (Dorrell fence-clear under Selectivity-Δ; ΔE readout fenced) before the Dorrell nonneg-AE candidate is treated as live.
6. Does Report 111's novelty-that-didn't-convert re-open a readout/retrieval-geometry reading that would un-park GSBC? (Affects whether G-A refit-recovers is interpreted as a readout-fix.)
7. Card the link-only / absent flashy candidates (2310.05644, 1909.03368, ArSyD 2412.19847, FEP 2505.22749, Dorrell 2410.06232, GSBC 2303.13957, BTSP-HDC) before leaning on them as load-bearing? (Plan treats carding 2310.05644 + 1909.03368 + ArSyD/FEP + BTSP as prerequisites, not optional — HARD RULE 3; their interpretation is licensed to return 'does not transfer'.)
8. Scope = the consolidation OBJECTIVE (surgical), or does the 065/066 binding-algebra wall warrant the deeper substrate question first (shifting the fork toward rebuild)? The G-0/BTSP key-only probe is the only candidate that adjudicates this directly; the already-run passive 065/066 null is the clean algebra-only adjudicator.
9. For G-D's role-shuffle control: commit to the read-time role-filler PAIRING-shuffle form + add the content-matched non-positional control + card 1909.03368 first (fork A), OR replace the free-standing role-shuffle with idea-19 swap-reconstruction as BOTH the objective AND the selectivity arm (fork B, dissolves the position/content artifact by construction but couples the headline to one candidate)? (Plan leans B for cleanliness; A preserves the cross-candidate Selectivity-Δ table.)
10. Lift the Phase-5-prime fence? Plan says NO for Stage-1 (legitimately pre-5-prime); only a direct ΔE-headline attack needs the fence lifted AND the raw_scene_energy_v0 saturating-readout + min_branch arbitration-shaped-aggregator audit §9 fence (audit:128-131) cleared first.
11. At the gate's OUTPUT, which fork branch to execute: readout-fix / surgical-in-place / mild-rebuild / rebuild? (Plan hands you the evidence packet rather than presuming surgical.)

## Done-gates (every experiment must clear these)

1. Headline metric reported with a confidence interval — R1 top_index_hits Selectivity-Δ with the TWO-FLOOR rule (absolute true-role arm Wilson-lower > 1/N=0.25 on the 4-role toy AND Δ Wilson-CI > 0; both reported; a Δ>0 from a SUB-CHANCE shuffled arm is the anti-overlap signature and is REJECTED, not a pass).
2. A control condition run on the SAME test set: random-codebook AND no-negatives must collapse Δ→0; the role-PAIRING-shuffle (not codebook-relabel) is the selectivity control; the content-matched non-positional shuffle excludes position/content artifacts (role≡position at encoding.py:38); the perfect-cue UPPER-BOUND arm bounds the ceiling; the lower-D/N arm (G-C) for the capacity confound.
3. Drill-down metrics explain anomalies: rank_role distribution, refit-vs-production gap, key-only vs full-cue recoverability, per-rule N-sweep + D-grid table, ent + margin (066:36 — disambiguate tix=0 'no basin' from sharp-wrong-basin).
4. Written up as a numbered markdown report under reports/ — this step is ENUMERATED INSIDE each gate's own step list, not deferred to done-gates only (per done-gate audit gap), so a gate cannot finish without reports/NNN.
5. STATUS.md (Current-state block + one Recent-updates entry ≤5 lines) and the user-memory project_status note updated — walk-back FIRST if any banner is contradicted (the Frame-B level-DiD closure, the range-lane closed-by-association status, the Phase-3 headline swap). The Frame-B closure walk-back is a start-now FIRST edit, not a done-gate contingency.
6. Every mechanism cites ≥1 source_id (from source_manifest.jsonl) + ≥1 principle check; link_only/absent candidates carded from primary (and added to the manifest where absent) BEFORE being treated as load-bearing — carding is LICENSED to return 'does not transfer' and drop the cite (HARD RULE 3 + the inverted-dependency rule).
7. Multi-seed with Wilson/bootstrap CIs (single-seed-17 was the phase3b confound); ≥5 seeds for G-A, verdict by per-seed sign agreement.
8. ANTI-HOMUNCULUS gate: the swap-negative is PRECOMMITTED real-example role-swap drawn by fixed role-equivalence-class membership + seed-fixed permutation, NEVER sims.argmax/nearest-mean/most-offending (the current :106 argmax-mined predicted_id is the thermostat being removed); the new write is offline/batch (STATUS:44); the replay scheduler stays tension-driven; no runtime gate reads a metric and branches. The dense-AM native non-local rule is BANNED as a write (control-architecture only).
9. FENCE gate: no Stage-1/Phase-5 graduation read path calls _energy_from_scores/mhn_energy → min/argmin-over-branches → ΔE (the grep-able refactor-proof bright line, per grill q10); a compliant read path terminates in a top_index_hits equality count. The shared _scores matmul is permitted.

## Risks

1. The never-written verdict is reached by ELIMINATION, not a positive write-then-read; G-A could falsify the entire objective-rewrite program in one afternoon (if a fresh readout recovers role-target on the frozen substrate, the locus is the READOUT). This is a feature — run G-A FIRST — but the surgical-write spine is provisional until G-A returns refit-fails.
2. SPINE CORRECTION (grill q1): 'zero-margin / push-down-only / members-mean write' is a MISNOMER — the write at error_driven_learner.py:147-162 ALREADY has a margin/push term (:158-160) and ALREADY FAILED (phase3b Fail Rate 1.0, seed 17). 'Add a margin' is an already-falsified fix, NOT a novel one. EBM-2006's flat-surface theorem applies to architectures LACKING a pull-up (Fig 9b) and is collapse-safe for a contrastive blend WITH a push — so EBM demotes from PROOF to LOOSE MOTIVATING ANALOGY. The surviving verdict rests on elimination (062-064), not EBM. Do not over-claim the root cause as proven by EBM.
3. LOCUS-CORRECTION RISK: BOTH the WF-1 diagnosis (cited 'members mean' / reconstruction_learner.py centroid) AND the synthesis (cited consolidation.py:549/636 members.mean) mis-locate the write. Verified at primary: :549/:636 are the tr(Σ)/anti_collapse diagnostics (docstring :538-540); the actual write is the pull/push blend at error_driven_learner.py:147-162 / reconstruction_learner.py:147-162. The surgical edit must land there, not on the diagnostic.
4. MANIFEST GAP (HARD RULE 3): 2310.05644 (G-A's interpretive basis), 1909.03368 (G-D's selectivity-control basis), ArSyD 2412.19847, DreamWeaver 2501.14174 are ABSENT from source_manifest.jsonl (not even link_only; corpus has ZERO carded-from-primary, only 100 link_only + 21 stale_pdf). 2310.05644's primary was NEVER opened (brief line 70 open TODO); its abstract describes a frozen-deep-net linear-probe protocol, NOT FHRR/MHN basin membership — the transfer is an ANALOGY (same over-claim as EBM). G-A/G-D MEASUREMENTS are re-grounded on internal Reports 066+111 + existing controls; the borrowed citations move behind a primary-read card LICENSED to fail. Building any gate's interpretation on the absent papers before carding repeats the documented fabricated-citation failure mode.
5. The contrastive family was already falsified single-seed (phase3b Fail Rate 1.0000, seed 17, self-mined predicted_id negative, top-1 readout). The live unresolved risk is constructing a non-self-collapsing negative in complex-phasor FHRR space and whether complex phasors hold a margin AT ALL — a SUBSTRATE property, not a negative-construction choice. G-B is gated behind the Cell-A bundle-first positive control + Cell-B single-round-trip floor (grill q3) so a G-B null is interpretable (negative-wrong vs substrate-can't-hold-a-margin) rather than a dressed-up re-run; G-B must report whether the swap-negative lands at non-trivial similarity or a null is uninterpretable.
6. The binding-algebra wall (065/066: no key-only basins for ANY operator; the load-bearing number is rank_role 205-540 mid-pack + the ~7pt FHRR gap at 066:37, NOT the discarded 95pt GHRR-confounded cell) is UPSTREAM of any consolidation objective and NOT excluded. Reports 065/066 ARE the already-run PASSIVE phasor-margin probe (explicit margin diagnostic 066:19; harness exp42:121-122), returning at-chance/null for the passive bind(k,v) key-only round-trip (tix 4.2%→0.1%; 1/√D orthogonality 065:74; O(1/m) noise-lottery 066:68). The genuinely-open residue is narrower: whether a WRITTEN/TRAINED margin survives where the passive round-trip doesn't — the only thing G-0/G-B may claim to test. Only the G-0/BTSP key-only probe adjudicates the algebra directly (a margin-write probe, not the algebra in isolation; the passive 065/066 null is the clean algebra-only adjudicator).
7. G-C 'lower-D rescues' is MECHANISTICALLY BACKWARDS as originally framed (grill q5): lowering D at fixed N makes 1/√D crosstalk WORSE, so a lower-D null is PREORDAINED and reading it as 'capacity isn't the issue → rebuild' is unsound. The real capacity lever is N (load) and the per-pattern score scale (β/m-normalization), possibly HIGHER D. G-C is re-specified as an N-PRIMARY surface with pre-committed signs (N↓ rises, D↑ flat-to-better, D↓ predicted-null control, β its own axis); the rebuild trigger never rests on the lower-D point; G-C is MANDATORY (grill q9) because it retroactively audits Phase 1's never-gated key-only capability.
8. STATUS is STALE on the range lever: says range-shaped-replay closed via Report 111, but 111 closed the storage-side range_postsettle proxy (111:44/71 decision_id=not_viable...novelty_without_retrieval), NOT the Dorrell TRAINING-objective variant Report 068:120-128 deferred. Treating it as closed-by-association mirrors the freq-weighted-α walk-back; surface, do not paper over. Dorrell is fence-clear as a Stage-1 candidate ONLY under Selectivity-Δ; the bundled ΔE readout stays fenced (grill q8).
9. BINDING-DOC DRIFT: the Phase-3 move is (b)-shaped (CLOSE level-DiD, OPEN a new sub-program with different estimand/substrate/control), not an (a)-shaped headline swap (grill q7). The walk-back closing level-DiD is a MANDATORY FIRST edit (CLAUDE.md:65); the headline re-point is a binding-spec change needing explicit user sign-off — both must be SURFACED, not drifted in.
10. G-D is NOT falsifiable as originally written (grill q6): recoverability must be pinned to top_index_hits (066:19) with chance = 1/N = 0.25 on the 4-role toy (a Δ>0 from a sub-chance shuffled arm faked a pass); the role-shuffle must be the read-time PAIRING-shuffle over fixed distinct position-roles (NOT a codebook-relabel, which is C.3-gauge-vacuous per note:66-86) AND a content-matched non-positional control is required because role≡position at encoding.py:38 so random-codebook/no-negatives exclude neither position-legibility nor content-overlap. 1909.03368 (the control basis) is absent — card before load-bearing.
11. FENCE CREEP (grill q10): a Stage-1 win must NOT silently become ΔE work. The 062-064 module (m1_role_energy.py) DEFINES and CALLS mhn_energy() (-logsumexp(βs)/β at :407/:453/:634) and stored ΔE≈-0.32 (Report 064) — so 'different module' is a FALSE bright line; the fence must be drawn at the REDUCTION (logsumexp-scalar + branch-min → ΔE), grep-able and refactor-proof. The min_branch aggregator is an arbitration-shaped thermostat (audit §9, AH FAIL) that must be de-arbitrated before any Phase-5-prime reopen. Targeting ΔE requires lifting the fence AND clearing the saturating raw_scene_energy_v0 readout first.
12. Scaling to wikitext / full Phase-4 integration before Stage-1 writes a recoverable basin would scale a null and waste compute/Colab credits (Report 068 deferred integration for this reason). The 4-role toy go/no-go is the gate; keep it local-MacBook-cheap. AH thermostat-RISK boundary to hold at implementation across G-B/Phase 3/Phase 4: the contrastive negative must stay a PRECOMMITTED real-example role-swap; if implementation reverts to per-event runtime argmax-mining of the hardest negative inside a streaming loop (the original :106 pattern), it re-introduces a runtime metric-read and FAILS the filter.
13. REBUILD-BRANCH AH RISK (grill q12): the Krotov-Latham dense-AM candidate has a NON-LOCAL native rule — admissible ONLY as a control architecture (fixed energy landscape), BANNED as a write. An owed empirical control: does the dense-AM landscape's basin structure require the non-local rule to have built it? If so 'architecture-only' is not separable and the arm is not load-bearing. Card dense-associative-memory-2016 (stale_pdf) at primary and confirm the rule is a fixed energy-gradient dynamic before load-bearing.