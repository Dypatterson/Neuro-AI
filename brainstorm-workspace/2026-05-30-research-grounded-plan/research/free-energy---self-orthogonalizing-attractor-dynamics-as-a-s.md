# Research angle: Free-energy / self-orthogonalizing attractor dynamics as a structure-WRITING consolidation objective, and whether it stays anti-homunculus. Core thesis: the production phase4 consolidation centroid (members-mean) is push-down-only with no anti-overlap pressure (the seed's verified FLAT/ABSENCE diagnosis); the FEP self-orthogonalizing attractor network (arxiv:2505.22749, Spisak and Friston, now peer-reviewed Neurocomputing 2026) supplies EXACTLY the missing pressure as a LOCAL learning rule whose anti-Hebbian (predicted-correlation) term IS the contrastive margin the seed's rank-1 objective prescribes — but derived from free-energy minimization rather than imposed, and crucially running in a SINGLE phase (no clamped/free split), which defuses the seed's stated rank-1 risk that EqProp needs symmetric real fixed-points unavailable in FHRR. Orthogonalization here is an emergent consequence of the complexity (KL-to-prior) term, i.e. a fixed local dynamic, not a readout or supervisor. Surprise: its falsification record in this project is EMPTY — reports 111/068 falsified the Dorrell-Whittington range REPLAY-SAMPLER lane, a different mechanism the seed conflates with it.

## Searches
- self-orthogonalizing attractor network free energy principle 2025 Cerebral Cortex
- Tolman Eichenbaum Machine factorized structure learning 2025 2026 grid cell entorhinal generalization
- grid cell structured representation learning objective path integration disentangle 2025 conformal isometry place cell
- energy-based associative memory orthogonalization separation maximization learning rule 2024 2025 dense Hopfield decorrelation
- predictive coding factorized representation learning roles fillers binding variational free energy 2025 hippocampal index
- contrastive Hebbian learning without clamped free phase predictive coding equivalence local learning rule 2024 2025
- vector symbolic architecture HRR cleanup memory orthogonalization decorrelation learned codebook attractor 2025 unbinding noise

## Key findings
- (IN-CORPUS) FEP self-orthogonalizing attractor net (arxiv:2505.22749) has an EXPLICIT local coupling-update rule (Eq.25): dJij prop sigma_i*sigma_j - L(bi+sum_{k!=i}Jik*sigma_k)*sigma_j -- observed correlation (Hebbian) MINUS predicted correlation (anti-Hebbian), L = Langevin function (the network's own prediction). The anti-Hebbian term is the contrastive/margin pull-up the seed's rank-1 objective (EBM-2006 push-down/pull-up) prescribes -- DERIVED from free-energy minimization, not imposed, and run in a SINGLE phase (no clamped/free split). Opened the arxiv HTML primary and project page; quoted the equation from the HTML.  <https://arxiv.org/html/2505.22749v1>
- (IN-CORPUS) Orthogonalization in arxiv:2505.22749 EMERGES from F = DKL[q(sigma_i)||p(sigma_i)] (complexity) - E[ln p(sigma_\i|sigma_i)] (accuracy), Eq.14. The complexity (KL-to-prior) term forces new patterns to encode residual variance orthogonal to learned directions (paper compares to Sanger online PCA). Paper states Hebbian-alone CANNOT do this and 'has to be counter-balanced by the anti-Hebbian term' -- orthogonality requires the complexity term specifically. This is the anti-overlap pressure the seed says the members-mean centroid LACKS. Opened primary (HTML).  <https://arxiv.org/html/2505.22749v1>
- (IN-CORPUS) Empirical demos in arxiv:2505.22749: Simulation 1 transforms correlated inputs (r=0.77) into near-orthogonal attractors (r=-0.19); memory maintained 'well beyond the classical Hopfield capacity bound of ~0.14N'; Simulation 3 -- sequential presentation yields asymmetric couplings + sequence recall, random yields symmetric weights. Smallest demo: 25 subparticles (5x5 images), 2 correlated patterns. Code (Python + vectorized JAX, CC-BY-4.0) at github.com/tspisak/fep-attractor-network. Opened the project page.  <https://pni-lab.github.io/fep-attractor-network/>
- (IN-CORPUS) SURPRISE: reports 111 and 068 contain NO reference to FEP, self-orthogonalizing, Friston, Spisak, or arxiv:2505.22749 (grep empty). They falsified the Dorrell-Whittington (arxiv:2410.06232) range-shaped REPLAY-SAMPLER lane ('novelty without retrieval'; 111 decision_id=not_viable_current_range_replay_downstream_novelty_without_retrieval). The seed's rank-3 risk note conflates the range replay sampler with the FEP self-orthogonalizing WRITE. The FEP write objective has NEVER been built/run here -- its falsification record is empty. Re-read 111 and 068 at primary.  <https://arxiv.org/abs/2505.22749>
- (NEW) Single-phase contrastive-Hebbian is a real, robust class (Hoier et al., 'Two Tales of Single-Phase Contrastive Hebbian Learning', ICML 2024, PMLR v235; 'dual propagation' 2024) -- CHL/EqProp/predictive-coding unified under EBM, all recover backprop gradients in the vanishing-nudge limit, but single-phase variants remove separate clamped/free phases. Independently confirms the FEP rule's single-phase property and defuses the seed's rank-1 risk ('EqProp assumes symmetric real fixed-points not obviously true for FHRR'): a single-phase predictive-residual rule does NOT need two-phase fixed-point symmetry. Opened the PMLR listing only.  <https://proceedings.mlr.press/v235/hoier24a.html>
- (NEW) K-Hop (Kernelized Hopfield, baseline cited inside arxiv:2511.20609 'Adaptive Hopfield Network', Nov 2025) trains a learnable projection by a SEPARATION LOSS maximizing expected Euclidean distance between every pair of stored memory patterns -- an OFFLINE, batch, self-supervised (label-free) statistic over the whole stored set. 'Under separation maximization, the size of metastable states dramatically decreases within ~20 iterations.' An explicit anti-overlap WRITE expressed as an offline statistic (anti-homunculus-exempt). NEW. Opened arxiv:2511.20609 HTML; A-Hop itself uses likelihood-matching, the separation loss is the K-Hop baseline it cites.  <https://arxiv.org/html/2511.20609v1>
- (NEW) VSA/HRR cleanup-memory literature: unbinding noise variance is ~1/N so SNR is low and cleanup is essential; cleanup memory is standardly a Hebbian/Hopfield auto-associator where fillers are attractor basins; near-orthogonality of stored symbols is 'critical to ensuring symbols can still be distinguished'. Says the FEP self-orthogonalizing write is well-matched to FHRR's actual failure surface (crosstalk between near-collinear stored fillers), and frames a lower-D control as testing whether the project null is a 1/sqrt(D) crosstalk floor (the seed's open capacity confound) vs an objective defect.  <https://arxiv.org/pdf/2506.15793>

## New sources
- **Two Tales of Single-Phase Contrastive Hebbian Learning (Hoier et al.)** (2024) <https://proceedings.mlr.press/v235/hoier24a.html> — Defuses the seed's rank-1 EqProp risk. Shows contrastive-Hebbian can run SINGLE-phase (no clamped/free split), unifying CHL/EqProp/predictive-coding under EBM. The FEP rule (arxiv:2505.22749) is an instance. Card before leaning on it (opened only the listing).
- **Adaptive Hopfield Network: Rethinking Similarities in Associative Memory (contains K-Hop separation loss)** (2025) <https://arxiv.org/abs/2511.20609> — K-Hop separation loss = offline batch statistic maximizing pairwise Euclidean distance between stored memories, shrinking metastable states. An anti-homunculus-clean (offline) alternative anti-overlap WRITE to the FEP rule. NEW; link_only until carded; opened the HTML for the loss definition.
- **Provably Optimal Memory Capacity for Modern Hopfield Models (spherical codes)** (2024) <https://arxiv.org/abs/2410.23126> — Frames the capacity wall the seed flags open (058/065): optimal Hopfield capacity = packing stored patterns as a spherical code (maximally separated on the sphere). Gives a principled target geometry for a successful self-orthogonalizing write and a yardstick for the lower-D control.
- **Linearithmic Clean-up for Vector-Symbolic Key-Value Memory** (2025) <https://arxiv.org/pdf/2506.15793> — Quantifies VSA/HRR unbinding noise (~1/N variance) and cleanup-as-attractor-basin. Grounds the seed's capacity/wrong-contrast confound and motivates the lower-D arm: tests whether the project null is a 1/sqrt(D) crosstalk floor vs an objective defect.
- **On Conformal Isometry of Grid Cells: Learning Distance-Preserving Position Embedding** (2024) <https://arxiv.org/abs/2405.16865> — Grid-cell-side structure-writing objective: a distance-preserving (conformal-isometry) loss forces structured, factorized, near-orthogonal multi-scale codes from a path-integration objective with no supervisor. Candidate structured role-prior writer; offline geometric loss, anti-homunculus-plausible.

## Concrete ideas

- **Stage-1 toy: replace the phase4 members-mean centroid write with the FEP single-phase self-orthogonalizing update (arxiv:2505.22749 Eq.25) on a ~4-role FHRR substrate -- continuous-learning coupling updates dJij prop sigma_i*sigma_j - L(bi+sum_{k!=i}Jik*sigma_k)*sigma_j over role-cue/filler bindings. The anti-Hebbian (predicted-correlation) term supplies the contrastive margin the production write lacks; orthogonalization emerges from the complexity/KL term, not imposed.** [local-dynamic; pre-Phase-5' Stage-1 toy (consolidation objective, phase4-derived)]
  - writes structure: The write is the coupling matrix J updated by Hebbian-minus-predicted-correlation. New role/filler bindings are forced to encode residual variance orthogonal to already-stored directions (Sanger-PCA-like), so role-discriminative attractors SEPARATE instead of collapsing to the zero-margin members-mean. Structure is written into J by the local dynamic; read out as basin membership (top_index_hits), never via a readout or supervisor.
  - test: Port github.com/tspisak/fep-attractor-network minimal demo (25 units, 2 patterns) to the FHRR toy; readout = basin-membership top_index_hits above chance for role-target (NOT top-1, per report 066). Controls: (a) anti-Hebbian/complexity term ablated must FAIL to orthogonalize (paper's own claim, doubles as anti-homunculus proof); (b) random-codebook must fail; (c) shuffled-role gauge dE~0; (d) lower-D arm to separate the 1/sqrt(D) crosstalk floor (058/065) from objective defect. Phase: pre-Phase-5' Stage-1 toy (Phase 5' PAUSED per STATUS.md:20 -- no fence-lift needed; targeting dE headline directly DOES need it).

- **Diagnostic correction before any build: re-scope the seed's rank-3 'partially falsified by report 111' claim. The FEP self-orthogonalizing WRITE and the Dorrell-Whittington range REPLAY-SAMPLER are different mechanisms; 111/068 falsified only the latter. The FEP write has an EMPTY falsification record here. Promote it from a rank-3 footnote to a first-class Stage-1 arm.** [offline-statistic; cross-phase lit routing (phase3/phase5)]
  - writes structure: Scoping correction, not a mechanism -- but it unblocks treating the FEP local rule as the primary structure-writing candidate rather than assuming it was already ruled out.
  - test: grep -inE 'fep|2505.22749|self-orthog|friston|spisak' reports/111*.md reports/068*.md returns EMPTY (run this session). Confirm with the user, then update the seed objective table. Zero compute.

- **Offline separation-maximization consolidation pass (K-Hop-style, arxiv:2511.20609): after collecting the replay buffer of role/filler traces, run a BATCH gradient step on a learnable projection/codebook that maximizes expected pairwise distance between stored role-target patterns (separation loss), then store. An offline anti-overlap write that is a pure statistic over the buffer.** [offline-statistic; phase4 consolidation objective]
  - writes structure: The separation loss maximizes inter-pattern Euclidean distance across the whole stored set, shrinking metastable/spurious states (paper: metastable size drops in ~20 iters). Role-discriminative structure is written by pushing role-target centroids apart in the codebook geometry -- the opposite of the members-mean collapse. No runtime branch: one batch statistic over all traces.
  - test: On the Stage-1 toy: consolidate under separation loss vs members-mean; readout basin-membership top_index_hits. Falsifier: separation loss on a random/shuffled buffer must NOT separate role-targets. Lower-D control. Cite arxiv:2410.23126 (spherical-code optimum) as target geometry. NOTE: arxiv:2511.20609 is link_only/uncarded -- card before it carries weight.

- **Conformal-isometry / distance-preserving structured-prior writer (arxiv:2405.16865) as the role-geometry prior: train the role basis under a distance-preserving (isometry) loss so the role manifold is factorized and near-orthogonal by construction, then let fillers bind into it. Targets the seed's 'binding-algebra has no key-only basins for ANY operator' alternative locus (065/066), not just the objective.** [offline-statistic; phase5/phase5' role-prior (encoding side)]
  - writes structure: A distance-preserving loss on the role embedding forces a structured, separated role basis (grid-cell-like factorization) BEFORE any filler is bound -- so unbinding lands in distinct role basins rather than a degenerate key-only manifold. Structure is written by the offline geometric loss on the basis; read out as key-only basin existence.
  - test: Add a role-basis isometry-pretraining arm to the Stage-1 toy; readout = does key-only basin retrieval exceed chance for any operator (the 065/066 null)? Control: random (non-isometric) role basis must keep the key-only null. Adjudicates objective-wrong vs algebra-has-no-key-basins -- the disambiguation the seed says 065/066 leave open. link_only -- card before load-bearing.

## Surprises


## Leads

