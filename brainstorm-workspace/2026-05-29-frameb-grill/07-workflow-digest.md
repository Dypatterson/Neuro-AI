# Frame B — contradiction audit + grilled next-steps plan (workflow w1p042x05)

Date: 2026-05-29 · 39 agents · audit(6 streams x verify→refute) → plan(3-panel+judge+synth) → grill(12 Q, separate defender, judge) → harden.

Source result: /tmp/wf_result.json (parsed from the workflow task output).


## Plan scores
- **window-averaging-first (Frame A level-DiD √K, slope held open) = 9/10** — +Most faithful execution of the VERIFIED 06 synthesis (re-read 06:46-63, 95-101): window-averaging FIRST (cheap, dominant lever attacking the ~92-96% within-seed window variance, keeps the Frame A LEVEL headline, no new substrate mechanism), slope held open as escalation gated strictly on the residual ~0.0224 atom-draw floor still blocking power (06:49-50,61-63). Best cost/ROI ordering of the three: spends the cheap dominant lever before the expensive build (Criterion 3). Correctly honors Q1-open by NOT killing the slope on a window-draw read it is invariant to (06:13-23), and pre-commits the Stage-1B read to the existing table while labeling it FALSIFY-only (05:34) so corr_AC_BD cannot be fished into licensing a build. Uniquely surfaces a real hidden cost the audit missed and the other plans under-weight: the n=10 Gate-0 summary JSON is NOT on disk (verified directly via find + git log empty), so 'zero-cost' Stage-1B silently depends on recovering/re-emitting it — this is the single biggest unstated dependency and Plan A makes it Step-1 decision-critical. Discharges done-gate #4 (Report 115), puts the walk-back first (STATUS:16, verified at STATUS:65), and annotates the un-amended spec §Why-a-slope/§Variance-handling (the verified moderate gap). Anti-homunculus clean; window_seed_override is the verified read-only knob, consolidation still fires on the unchanged buffer-fill event. Carries the √K pool-independence caveat (06:57-59) into an explicit Step-3 pool-budget gate.  / −Does not itself MEASURE σ(β)/σ(Δβ) — the actual open Q1 conditional — unless escalation fires; it shrinks σ on the level-DiD and infers the slope is only needed for the residual floor. Per 06 this is correct sequencing, not a defect, but it means the open question stays open longer than under Plan B. Heaviest step-count of the three (11 steps), some of which (Step 7 slope-draft floor/G2/σ-gate fixes, Step 9 slope escalation) are pre-staged for a branch that may never fire — mild risk of doing slope-side editorial work before it is needed. The 'keep the LEVEL headline as the active graduation estimand' move is a real headline-pin decision and the plan correctly flags it as the user's, but it edges toward de-facto re-headlining the level-DiD; the experiment-preamble drill-down labeling mitigates this.
- **slope-smoke-first (two-stage holdout pilot gating the Frame B build) = 7/10** — +The ONLY plan that actually closes the verified-open Q1 σ-kill conditional: Stage 2 measures σ(Δβ) and G1-G4 (corr(intercept,slope), fanning, non-vacuous control) that the endpoint decomposition provably cannot reach (06:91-94, re-read). Its Step 3 is the single best-reasoned step across all three plans: it explicitly refuses to apply the stale pre-06 binary (05:23 'slope not the lever'; 01:56-58 'do NOT reframe') and routes window-draw dominance to option (e), reconciling the 06 over-read BEFORE acting — exactly the walk-back-first discipline. Strong on the verified threshold defects: unifies the σ gate (placeholder vs 04 bare build gate), forces an explicit G2=0.40 ruling (01 'needs your number' vs 04:62 'user-approved'), recompute the slope floor on the corrected 2.303-e-fold span (not 02's retired 4.135-span numbers 06:67 stale-blessed), and adds the two-corr-objects glossary. Runs on HOLDOUT seeds 1000..1009 disjoint from graduation 0..9. test 4a written before the hook (TDD). Honors smoke-before-build and the Q2 pin gate.  / −Spends the EXPENSIVE lever (build the read-only checkpoint hook, ~0.5-1 day) relatively early — after only a falsify-only Stage 1 — before the CHEAP dominant window-averaging win is realized on the level-DiD. This is the precise ROI inversion the verified 06 synthesis warns against ('window-averaging FIRST ... THEN see if the residual atom floor needs the slope', 06:61-63). It treats the slope path as the spine and window-averaging as merely a routed alternative, inverting the recommended priority. Carries the most in-scope build code. Self-flags that several design-spec/02/03/06 citations rest on the verified-audit re-opens rather than firsthand re-reads (tool-channel intermittency) — honest, but means an executing agent must re-verify before binding. Net: technically the most complete on the open question, but it gets there by spending scarce compute/eng ahead of the cheap dominant lever.
- **prereqs-first-then-user-branch = 8/10** — +Cleanest on done-gate / no-premature-pin discipline and lowest spend: every step is genuinely decision-free (test 4a, fixed-mask hygiene, n_consolidation_events rename + axis-lock with a HARD fence against touching the floor, Report 115, zero-cost corr_AC_BD with NO threshold applied), then it returns the branch to the user with corr_AC_BD in hand. This faithfully honors the verified 06:104 ruling that 'the reframe call and the window-averaging-first call are the user's', and the audit's no-premature-pin finding. Its scope-fence on Step 3 (lock axis + schedule VALUES only, do NOT recompute the floor — a deferred threshold) is the sharpest discipline move in any plan and directly prevents the verified-moderate floor-span staleness from leaking into a binding edit. Correctly threads the CORRECTED variance read (~92%/3.8%) into Report 115 and the STATUS walk-back rather than the stale 'codebook intercept is the problem' framing. Distinguishes corr_AC_BD's 0.3 cutoff from G2's 0.4 object (no conflation). Anti-homunculus clean throughout.  / −Under-delivers on Criterion 2 and Criterion 3: it neither TESTS σ(β) nor SPENDS the cheap dominant lever — it stops at computing the descriptive corr_AC_BD readout and hands the window-averaging-vs-slope choice back to the user. But 06 already RECOMMENDS window-averaging-first as the concrete next action, and the user has already ruled 'accept the reframe' (Q2), so pure deferral is slightly too passive: it does the prereq hygiene but not the cheap dominant lever the review names. Like Plan A it depends on the Gate-0 summary artifact for corr_AC_BD; it flags absence-surfacing but does not make recovery a first-class compute-vs-recover decision as crisply as Plan A. Net: excellent prerequisite/discipline scaffolding and the right framing of the branch, but it leaves the actual variance-reduction work undone.

**Winner: window-averaging-first (Frame A level-DiD √K, slope held open)**

Adopt Plan A as the spine — it is the only plan whose center of gravity matches the verified 06 synthesis (window-averaging FIRST as the cheap dominant lever, slope held OPEN as escalation gated on the residual ~0.0224 atom floor), and it uniquely surfaces the load-bearing hidden cost (the n=10 Gate-0 summary JSON is not on disk, so Stage-1B is only zero-cost if recoverable; make this the first decision-critical prerequisite per Plan A Step 1). Then graft three things:\n\n(1) From Plan C, take the HARD SCOPE FENCE on the axis-lock/rename step: lock the exposure axis and schedule VALUES {100,178,316,562,1000} only, and explicitly DO NOT recompute or sign off the slope floor in the prereq phase (the floor is a deferred threshold tied to the corrected 2.303-e-fold span — verified moderate staleness). Plan A's Step 7 stages floor/G2/σ-gate fixes; restructure so those slope-draft fixes stay strictly deferred-and-fenced until the escalation branch actually fires, matching Plan C's discipline, so no slope-side editorial work is done before it is needed.\n\n(2) From Plan B, graft Step 3 verbatim in spirit: BEFORE applying any Stage-1/Stage-1B read, explicitly reconcile the 06 over-read into the stale pre-06 binary (01:56-58 'do NOT reframe'; 05:23 'slope not the lever') — route window-draw dominance to option (e) keep-slope-open, never to the binary kill. This is the walk-back-first obligation and it must precede acting on corr_AC_BD. Also graft Plan B's explicit threshold-honesty package for the escalation branch: unify the σ gate (no bare 04 build gate), force a recorded G2=0.40 ruling (01 'needs your number' vs 04 'user-approved'), recompute the floor on the 2.303-e-fold span, and add the two-corr-objects glossary (0.3 corr_AC_BD endpoint vs 0.4 corr(intercept,slope) slope).\n\n(3) From Plan C, take the framing that the window-averaging-vs-slope choice and any headline-pin are the USER'S calls (06:104) — present them as complementary/orthogonal (not rivals), with σ(β) stated plainly as still-untested / Q1-open. This guards Plan A against edging toward de-facto re-headlining the level-DiD.\n\nNon-negotiables all three already agree on, keep: walk-back FIRST into STATUS:16 Current-state (the 05-29 over-read currently leads with the un-amended gauge framing); annotate the un-amended spec §Why-a-slope/§Variance-handling with a pointer to the 05-29 decomposition (verified moderate gap, do regardless of branch); write Report 115 to discharge done-gate #4 with an explicit DRILL-DOWN (not graduation) label and the experiment-preamble citing the design-spec headline line; write test 4a as a runtime get_state/set_state byte-equality guard before any hook is trusted; honor the Q2 pin gate (pin only after the σ-kill smoke clears). Carry the √K pool-independence caveat (06:57-59) as an explicit pool-budget check before committing K, and the missing-Gate-0-summary risk as the top hidden cost. Do NOT close the σ(β) conditional by building the Stage-2 hook ahead of the window-averaging win — only escalate to it if the realized window-averaged level-DiD at n=10 still cannot clear the bar because of the atom floor.

## Final plan: window-averaging-first (Frame A endpoint level-DiD √K, slope held open) — fully grill-hardened: walk-back+contradiction-flag pass first, off-zero clearance precheck before any GPU spend, structural verdict-type firewall, tri-state artifact gate, pilot-not-build escalation trigger
Do the decision-free walk-back + draft-contradiction-flag pass FIRST (STATUS:16 lead correction split into a qualitative walk-back that runs now vs a point-split citation gated on artifact resolution; flag the G2 "(user-approved)"/bare-0.05 phantoms on the same pass), build a narrow codebook-only eval-isolation guard (not full spec-4a), add a STRUCTURAL type-disjoint verdict firewall so a level-DiD run can never serialize a slope graduation verdict, resolve the Gate-0 summary JSON as a tri-state artifact verdict, run an off-zero clearance precheck that already shows the window-averaged level-DiD CANNOT clear zero at n=10 by construction — then hand the user a crisp scope choice (labeled variance-mechanism drill-down vs jump to n≈27 seeds / slope) before spending a single GPU hour; the slope/checkpoint build stays fenced and its trigger escalates to the σ(β) PILOT, not the build.

### Decision-free prereqs (start now)
- Step 1a qualitative walk-back + contradiction-flag pass — CLAUDE.md mandates the walk-back as the FIRST edit; the flags record disputes and pick no value (q7+q9+q10). Mechanical grep-and-paste edit discipline (q5).
- Step 3a narrow codebook-only eval-isolation guard — the correctly-scoped anti-homunculus binding guard for the mechanism the cheap path actually adds (q6); reuses tests/test_gate0_frame_a.py:75-81. (Full spec-4a deferred to Step 12.)
- Step 3c structural type-disjoint verdict firewall — makes a level-DiD graduation verdict a type error rather than a discipline failure (q3); lands before Step 10.
- Step 4 axis-TYPE lock + n_consolidation_events rename — a true naming bug fix touching no substrate dynamic; NO anchor set / span / floor signed off (q4).
- Step 5 Report 115 headline + gauge-E section for the existing Gate-0 result — writes up a result that already ran; done-gate #4 hygiene, labeled DRILL-DOWN, discharged by the on-repo DiD (q2). (Variance section gated on Step 2.)
- Step 8 pool-budget √K check + OFF-ZERO clearance precheck — read-only inspection + offline arithmetic; decides the achievable K AND whether any achievable σ can clear zero at n=10 BEFORE any GPU spend (q8).
- Step 2 Gate-0 summary tri-state artifact resolution — re-verify the firsthand-confirmed absence with a working channel; supplies the Stage-1B + Report-115 source and gates the point-split citation (q2+q7).

### Branch decision
The window-averaging path and the slope path are COMPLEMENTARY, not rivals (per the 06 review's orthogonal framing — judge-verified firsthand this session via 06:47-63; NOT re-verified by me this session due to the tool-channel failure). Window-averaging attacks the ~96% within-seed corpus-window-draw component by √K; the slope attacks any per-seed ADDITIVE level shift. CRITICAL FINDING grafted from grill q8 (judge-verified arithmetic from STATUS:18 + 06:47-50): even the best window-averaging-achievable K=9 (σ→0.044) gives an n=10 Student-t CI of approx [-0.013,+0.051] around the +0.019 point — it STILL spans zero; only the physically-unreachable atom floor (σ=0.0224) barely clears at [+0.003,+0.035]; n≈27 SEEDS (not more windows) is the lever that actually clears zero. So the window-averaging run CANNOT clear the level-DiD off zero at n=10 by construction — it succeeds only at its stated drill-down purpose (proving the √K mechanism shrank σ from 0.115→0.044 and feeding realized σ into honest-n). The reframe is ACCEPTED-IN-DIRECTION (q2 grill USER RULING, self-fenced: pin gated behind the σ-kill smoke) but PROPOSED-pending-sign-off on every binding surface (spec:6, deep-dive:189/191, STATUS:17); σ(β) is still genuinely untested (STATUS:18) and only a Stage-2 holdout pilot can settle it. The branch: run the cheap dominant lever as an explicitly-labeled variance-mechanism drill-down (or skip straight to n≈27 / the slope — USER'S CHOICE with the precheck arithmetic in hand) FIRST; the slope/checkpoint-hook BUILD becomes the next lever ONLY via the Stage-2 σ(β) PILOT, which the corrected escalation trigger now routes to (the floor diagnosis licenses the ~1hr pilot, NOT the ~1-day build). The corr_AC_BD Stage-1B read is a FALSIFY-only endpoint diagnostic that CANNOT confirm a slope and never licenses a build. Whether to keep the LEVEL headline vs authorize the slope, and whether to PIN any headline, are the USER'S calls; nothing is committed/pushed and nothing is pinned into phase-3-deep-dive.md until the user signs off and the σ-kill smoke clears. An FB→pass would graduate Phase 3 ONLY; nothing here unpauses Phase 5′.

### Sequenced steps
1. **[start_now]** STEP 1a — QUALITATIVE WALK-BACK FIRST + DRAFT CONTRADICTION-FLAG PASS (decision-free; runs now per CLAUDE.md/STATUS:65 'walk-back is the first edit'). (1) Lift the 2026-05-29 over-read correction into the STATUS:16 Active-phase Current-state LEAD (it currently leads with the un-amended 05-28 gauge/codebook reframe; correction sits only at STATUS:18+27). REMOVE the stale 'codebook intercept is the dominant additive problem' framing — this is the QUALITATIVE direction correction (window-draw dominates ~24x), independent of any exact percentage. (2) Correct the 92-vs-96 discrepancy: STATUS:18/27 say '~92%' but the cited source 06:51 says '96%' and the complement of 3.8% is 96.2% — prefer ranking language ('within-seed corpus-window variance dominates the codebook/atom component ~24x; point split ~96/4 on a single fragile 10x4 run, df=9') over any bare percentage in any binding annotation. (3) Annotate spec §Why-a-slope (approx 25-36) + §Variance-handling (approx 110-119, esp. the σ²_intercept attribution) with a POINTER to the 05-29 decomposition, removing the stale codebook-intercept-dominant claim; keep the spec PROPOSED, amend no binding numbers. (4) Reflect the over-read into drafts 01-smoke-spec-DRAFT.md / 04-smoke-harness-DRAFT.md / 05-stage1-read-protocol.md so the stale pre-06 binary ('do NOT reframe; Frame A is correct' at 01:56-57 / 05:23 / 04:43) no longer stands un-walked-back. (5) ON THE SAME PASS through 01/04 (grill q9+q10 graft — decision-free contradiction flags, NOT value choices): flag 04:62 '(user-approved)' on G2=0.40 as '(approval-state DISPUTED: 01:50-51,71 say needs-your-number/your-call; NOT recorded as approved — do not treat 0.40 as live)'; flag 04:61 bare 'σ(Δβ)≤0.05' as '(PROVISIONAL placeholder per 01:47-49 / 03 H1:23-25; not a derived gate)'; add the two-corr-objects glossary line (0.3 corr_AC_BD ENDPOINT cutoff per 05:30 vs 0.4 corr(intercept,slope) SLOPE gate per 01:50-51/04:62 — distinct objects). MECHANICAL EDIT DISCIPLINE (grill q5 graft): key every edit on a unique verbatim substring (not a line number), run grep -n '<substring>' <file> and paste the hit into the walk-back report BEFORE the Edit fires so a non-match blocks the edit; the Edit-tool exact-match requirement then fails loud on any drift. Use full filenames (01-smoke-spec-DRAFT.md etc.), not bare 01/04/05.
   - rationale: CLAUDE.md makes the walk-back the FIRST edit, in Current state. Splitting off the qualitative correction (q7 graft) lets the direction fix land immediately without waiting on the unresolved artifact, while the false-freshness rule (q9+q10) demands that co-located stale threshold claims be flagged on the SAME pass — otherwise a freshly-edited draft varnishes the surviving '(user-approved)' G2 and bare 0.05, re-introducing exactly the staleness the rule exists to kill (the documented 2026-05-24 'freq-weighted α never built' failure).
   - cost: ~45-60 min doc edits. STATUS edit must stay within <250-line/<20KB budget (scripts/check_status_size.sh, STATUS:62) — trim Recent-updates before adding if near budget. | depends_on: none — do FIRST. | anti-homunculus: Documentation only; flags record contradictions, pick no value. No mechanism, no metric-reads-then-acts. PASS.
2. **[sign_off_gated]** STEP 1b — POINT-SPLIT CITATION (the act of writing any specific percentage as if reproducible). Gated on Step 2's tri-state artifact verdict. Under verdict (a) JSON recovered+reproduces: cite the ranking-plus-fragile-point-split WITH the df=9 caveat. Under (b) recovered-but-not-reproducing: escalate to user, pin nothing. Under (c) JSON gone: cite ONLY the qualitative ranking (window>>atom ~24x, single 10x4 run, point estimate not independently reproducible this session) and flag the source artifact as missing.
   - rationale: grill q7 graft: Steps 1, 2, 5 were all start_now with no ordering edge, so the parallel schedule could bake an unverifiable (and partly already-wrong: 92 vs 96) number into a binding doc before anyone knew it was unverifiable. Separating the act-of-citing-a-number from the qualitative walk-back forces the artifact verdict to precede any pinned figure.
   - cost: ~15 min once Step 2 returns; zero compute. | depends_on: Step 2 (artifact-verdict). | anti-homunculus: Documentation only. PASS.
3. **[start_now]** STEP 2 — GATE-0 SUMMARY JSON: TRI-STATE ARTIFACT VERDICT (decision-critical prerequisite; grill q2+q7 graft). Re-verify with a WORKING tool channel: find for *gate0*/*frame_a*/*frameb*/*variance*/*summary* .json across the repo AND git log --all --diff-filter=A. Judge-verified firsthand this session: NO such JSON on disk (only config/phase5_m1_codebook_registry.json, .claude/settings.local.json, notes/emergent-codebook/theta_prime_calibration.json) and git added the gate0 CODE + notebooks + the 06 markdown but NEVER any gate0_summary.json/variance_decomp.json; scripts/colab_frameb_smoke_stage1.ipynb reads the summary from a Google DRIVE path, not in-repo. Return a TRI-STATE verdict: (a) recovered from off-repo (Colab/Drive/scratch) + arithmetic reproduces; (b) recovered but does not reproduce 92/96/3.8; (c) gone. SPLIT into TWO distinct compute items: (i) Gate-0 GRADUATION summary -> enables corr_AC_BD/Stage-1B + Report 115 headline DiD (--variance-report consumer); (ii) variance_decomp GRID -> would re-reproduce the 3.8% (separate run, separately costed) but is NOT needed if 115 cites the split as a provenance-flagged qualitative drill-down.
   - rationale: The 'zero-cost Stage-1B' framing depends entirely on this artifact being on hand; it is firsthand-confirmed absent. The two artifacts have DIFFERENT recovery costs (graduation summary vs variance-decomp grid) which the original plan conflated. The 3.8% is an over-read being retired, so only the qualitative ranking is load-bearing and it survives the artifact loss.
   - cost: 5-30 min if recoverable from Drive; ~1hr Colab GPU for a fresh n=10 Gate-0 re-run of (i); a SEPARATE ~1hr for the variance-decomp grid (ii) if the user wants the 3.8% reproduced. | depends_on: none — can run in parallel with Step 1a. | anti-homunculus: Pure artifact recovery / re-emission. No mechanism. PASS.
4. **[start_now]** STEP 3a — NARROW CODEBOOK-ONLY EVAL-ISOLATION GUARD (decision-free; grill q6 graft, REPLACES front-loading full spec-4a). Write a get_state/set_state byte-equality test asserting the FINAL CODEBOOK (and buffer/C.2 state) is bit-identical with vs without a read-only eval/window-average call. Do NOT assert final-RECALL byte-identity (averaging over K window-seeds changes measured recall ON PURPOSE per spec approx 103-105). Reuse the existing gauge-arm precedent at tests/test_gate0_frame_a.py:75-81. Place under tests/ alongside the orthogonal byte-identity parity test; do not modify it. Snapshot/restore RNG so the read-only eval cannot perturb updater/Hopfield/C.2 state.
   - rationale: grill q6 (judge-verified): the window-averaging cheap path adds NO in-loop checkpoint hook — window_seed_override only feeds sample_windows BEFORE the consolidation loop, read-only at the end. So full spec-4a (checkpointed-vs-non-checkpointed final codebook + recall byte-identity) guards a mechanism the cheap path never adds; for the cheap path it tests trivial self-identity (K=1) or is false-by-design (K>1 changes recall). The correctly-scoped guard is codebook-only eval-isolation — the actual thermometer-not-thermostat proof for the eval surface.
   - cost: ~1 hr (one regression test + harness wiring); CPU, no GPU. | depends_on: none — start now; lands before Step 9. | anti-homunculus: The test IS the anti-homunculus guard — proves codebook byte-identity, the operational definition of read-only eval. PASS by construction.
5. **[start_now]** STEP 3c — STRUCTURAL VERDICT-TYPE FIREWALL (decision-free; grill q3 graft; lands alongside Step 3a, BEFORE Step 10). Make the slope graduation verdict TYPE-UNREACHABLE from any code path that did not compute Δβ over the checkpoint schedule. The slope classifier (gate0._classify_verdict / _classify_frame_b_verdict — emits FB->pass/weak/null-slope/dead-slope/confound/non-linear only, per spec approx 147-154) and the new level-DiD classifier MUST be DISTINCT functions with NON-OVERLAPPING verdict enums (level emits e.g. LVL->tighter / LVL->still-flat only). The level-DiD serializer must be physically incapable of emitting any FB-* verdict. Add a regression test asserting (i) a level-DiD run object cannot serialize any FB-* verdict, and (ii) the slope graduation function RAISES on any input lacking per-checkpoint slope data (spec approx 144-145: verdict is a pure function of stored per-seed slope stats).
   - rationale: grill q3 (judge-verified): the cheap path runs the ENDPOINT level-DiD at n=10 to clear STATUS blocker (2) while the PROPOSED headline (slope-DiD) is never measured. The spec's brakes (graduation clauses written over Δβ only at spec:121-140; the experiment-preamble) are DOCUMENTARY — they depend on the next agent re-reading the spec and obeying the preamble, the same class of defense that failed before (K-branch state_divergence drift). A type-disjoint verdict enum converts 'agent promises to label it a drill-down' into a type error: the level-DiD physically cannot serialize a graduation verdict.
   - cost: ~1 hr (verdict-enum split + regression test); CPU. | depends_on: none — start now; must land before Step 10. | anti-homunculus: A measurement-typing constraint, not a supervisor. No metric-reads-then-acts. PASS.
6. **[start_now]** STEP 4 — AXIS-LOCK / RENAME with NARROWED SCOPE (decision-free; grill q4 graft). Lock ONLY (i) the axis TYPE (exposure-counted, one observe() per unit) and (ii) the n_consolidation_events -> n_observations/n_exposures rename at all call sites + referencing tests; run the full suite (547+ tests per STATUS:31) after. Do NOT name, choose, or lock ANY checkpoint anchor set or its e-fold span in this prerequisite phase. The per-e-fold floor is a pure function of the locked span (floor = numerator / log10(max/min)); naming/locking the {100,178,316,562,1000} VALUES would pin the 2.303-e-fold span (04:69-71) and therefore HALF-PIN the floor denominator (0.06/2.303 ~= 0.026/e-fold) the user has not signed off. The anchor-set choice, its e-fold span, AND the DRAFT-02 floor re-derivation (02:23-37) are ONE coupled threshold decision and move TOGETHER into the deferred slope-escalation sign-off (Step 12 / spec sign-off items #1 and #4).
   - rationale: grill q4 (judge-verified arithmetic): you cannot honestly call the floor 'deferred' while the span — a pure function of the locked anchor values — is locked. The rename is a true naming bug (each loop iteration is one observe(), not a consolidation event) and is genuinely decision-free; locking the axis TYPE touches no substrate dynamic. But locking the VALUES is a premature pin of the floor. The QUESTION mis-attributed the 2.303-vs-4.135 contrast to 02:23-26; it actually lives in 04:69-71 + 02:12 — immaterial to the fix.
   - cost: Low-to-medium. Rename touches the c3 signature + call sites + tests; full suite run. | depends_on: none — parallel with Steps 1-3. | anti-homunculus: A rename + axis-TYPE lock touch no substrate dynamic. Consolidation still fires on the unchanged local buffer-fill event. PASS.
7. **[start_now]** STEP 5 — REPORT 115 for the EXISTING Gate-0 n=10 result (discharges done-gate #4; STATUS:18,30 flag it pending; no reports/115* exists). Mandatory experiment-preamble: Active phase 3; Headline per spec approx 40-46 = the PROPOSED slope-DiD; Required controls per spec approx 100-108 = global token-stream shuffle primary + gauge arm E confound check; Last verified result G0->weak DiD +0.019 CI [-0.121,+0.159], 6/10 positive, per-seed σ≈0.19 per STATUS:18; Why now: discharges the standing report gap. LABEL IT EXPLICITLY A DRILL-DOWN / NOT-A-GRADUATION-CLAIM. Done-gate #4 is discharged by the ON-REPO headline DiD +0.019 CI [-0.121,+0.159] (STATUS:18), NOT by the decomposition. The variance drill-down section is GATED on Step 2's artifact verdict (q7) and must cite the 92/96/3.8 split as a PROVENANCE-FLAGGED, single-run, non-reproduced QUALITATIVE drill-down (source variance_decomp.json ephemeral/off-repo, NOT regenerated this session) — never as a figure discharging the reproducibility gate. Report the headline DiD with CI + the gauge arm E control on the SAME test set (codebook-byte-identity per Step 3a + Δ-CI contains 0) + the Stage-1 variance-ranking drill-down.
   - rationale: grill q2+q7 (judge-verified): a numbered report that cites a JSON nobody can open fails the very gate it exists to discharge. Pinning Report 115's headline to the on-repo DiD (which IS independently sourced in STATUS:18) keeps it valid even under artifact verdict (c). The variance section, which consumes the off-repo number, must wait for the Step-2 verdict and carry the provenance flag.
   - cost: ~2-3 hr write-up. No compute for the headline (the run is done). | depends_on: none for the headline + gauge-E section; the variance drill-down section depends on Step 2 (artifact-verdict). corr_AC_BD from Step 7 can fold in as an addendum if it finishes first. | anti-homunculus: Reporting only. PASS.
8. **[sign_off_gated]** STEP 6 — RECONCILE the 06 over-read into the stale pre-06 binary BEFORE acting on any Stage-1/Stage-1B read (walk-back-first applied to the analysis step). Route window-draw dominance to option (e) = window-average FIRST + keep-slope-open, NEVER to the stale binary kill ('do NOT reframe; Frame A is correct' / 'slope reframe not the lever'). The OLS slope is invariant to per-seed ADDITIVE level shifts including window-draw, so window-dominance does NOT auto-kill the slope; only a SHAPE-changing variance fraction defeats the slope, and the endpoint decomposition never measures that — so the Q1 σ(β) conditional stays OPEN, not failed.
   - rationale: 01/04/05 carry the pre-06 binary un-walked-back; acting on the stale verdict would wrongly kill a slope the data is invariant to. The single most important interpretive correction the audit surfaces. Must precede acting on corr_AC_BD.
   - cost: ~30 min interpretation; zero compute. | depends_on: Step 1a (the walk-back edits) so the reconciliation is consistent with the corrected STATUS/spec. | anti-homunculus: Offline routing of a human research decision, computed after runs. Never gates the substrate at runtime. PASS.
9. **[sign_off_gated]** STEP 7 — Stage-1B corr_AC_BD re-analysis (NOT unconditionally zero-compute; grill q2 graft). Zero-compute IFF the n=10 Gate-0 graduation summary was recovered from off-repo in Step 2; OTHERWISE it is a ~1hr n=10 Gate-0 re-run. Experiment-preamble REQUIRED: Active phase 3; Headline per spec approx 40-46 = slope-DiD PROPOSED; Required controls per spec approx 100-108; LABEL THIS A DRILL-DOWN (corr_AC_BD is a variance-decomposition diagnostic, NOT the spec headline); Last verified result G0->weak per STATUS:18; Why now: settles the 05-29 over-read residual. Run gate0_frame_a.py --variance-report against the recovered summary; read corr_AC_BD against the pre-committed 0.3 cutoff. Report it as a DESCRIPTIVE number with NO gate that licenses a build. Per the guardrail (05:34): Stage-1B can FALSIFY but CANNOT confirm.
   - rationale: Highest-ROI step ONLY once the summary exists. Tightens confidence in the qualitative window>>atom ranking and is a FALSIFY-only branch input. corr_AC_BD (endpoint analog) is NOT the σ(β) measurement and cannot stand in for the slope shape test.
   - cost: Minutes of CPU IF the summary was recovered; else ~1hr Colab GPU for the re-run. | depends_on: Step 2 (summary recovered/committed) + Step 6 (reconciliation must precede acting on the read). | anti-homunculus: Offline statistic on stored per-seed Δ; routes a human research decision, never the substrate. No new substrate mechanism. PASS.
10. **[start_now]** STEP 8 — POOL-BUDGET √K CHECK + OFF-ZERO CLEARANCE PRECHECK (decision-free, zero-compute; grill q8 graft — the sharpest hit). (A) √K independence: inspect existing test-window sampling; compute the max disjoint K window draws the held-out pool supports; the √K reduction saturates sub-√K if the pool is small. (B) NEW dispositive precheck, run BEFORE Step 10 is authorized: for the max achievable K AND for the unreachable atom floor (sd=0.0224), compute the projected n=10 Student-t CI half-width = t(df=9=2.262)·sd_K/√10 around the +0.019 point and report whether each excludes zero. RECORD the judge-verified result: K=9 (best window-averaging-achievable, sd=0.044) gives CI approx [-0.013,+0.051] — SPANS ZERO; only the physically-unreachable atom floor gives [+0.003,+0.035] (barely clears); n≈27 SEEDS (not more windows) is the actual lever. So the window-averaged level-DiD CANNOT clear zero at n=10 by construction.
   - rationale: grill q8 (judge-verified arithmetic from STATUS:18 + 06:47-50): the original plan committed a ~1hr GPU run as the 'headline-clearing' step without a back-of-envelope showing the achievable σ-shrink can lift +0.019 off zero. It cannot — n=10 is the binding constraint even with an infinite perfect pool. This precheck is decision-free, zero-compute, and dispositive, so it must precede any GPU spend.
   - cost: ~30-60 min read-only inspection + a few lines of arithmetic. Zero compute. | depends_on: none — parallel with Steps 1-7. | anti-homunculus: Read-only inspection + offline arithmetic. PASS.
11. **[branch_dependent]** STEP 9 — Implement window-averaging on the EXISTING Frame A endpoint level-DiD via the read-only window_seed_override knob (defaults to the atom seed so K=1 is byte-identical to today). Average r over K window-seeds per (seed, world) before forming the per-seed endpoint DiD; aggregate via the existing c3._delta_ci_stats. NO new substrate mechanism — this is 'running more test windows'. KEEP the LEVEL headline; do NOT switch to the slope. Gate Step 3a (codebook-isolation) + Step 3c (verdict firewall) land first.
   - rationale: Directly attacks the dominant ~96% within-seed window-draw variance — the cheap dominant lever. Reuses verified-existing code (window_seed_override). Implementation is decision-free; the SCOPE/EXPECTED-OUTCOME of the subsequent run is now governed by the Step-8 precheck and the user's Step-10 scope choice.
   - cost: Small code change (loop K window-seeds + average) + tests; the run is K× the per-arm eval cost, no extra consolidation runs. | depends_on: Step 8 (K choice + clearance precheck) + Step 6 branch + Step 3a + Step 3c. | anti-homunculus: Pure measurement change. window_seed_override only selects the window-sampling seed; never reads recall to gate the consolidation loop. PASS.
12. **[sign_off_gated]** STEP 10 — Run the window-averaged Frame A level-DiD at n=10 — SCOPE GATED on the Step-8 precheck + the user's choice. May proceed ONLY as a variance-mechanism DRILL-DOWN whose preamble states the a-priori expected outcome: 'level-DiD CI will span zero at n=10 by construction (per the Step-8 precheck); this run proves the √K shrink moved σ from ~0.115 to ~0.044, it is NOT expected to clear zero.' If the user instead wants an off-zero result, Step 10 must be RE-SCOPED to the n≈27 level-DiD (or the slope path) BEFORE the GPU spend. FULL experiment-preamble: Active phase 3; Headline per spec approx 40-46 = slope-DiD PROPOSED; THIS run measures the ENDPOINT level-DiD = the continuity drill-down / variance baseline per spec:219-220 + sign-off item 9 (spec:270) [DRILL-DOWN, NOT the spec headline — re-open spec:40-46 and spec:219-220/270 with a working channel before binding this claim, per q8 caveat]; Required controls per spec approx 100-108 (stream-shuffle primary + gauge arm E); Last verified result G0->weak DiD +0.019 CI [-0.121,+0.159] (STATUS:18); Why now: STATUS blocker (2) + the 06 recommendation to shrink σ first. Report headline level-DiD with Student-t 95% CI AND realized σ(DiD) + honest-n (via variance_report_from_summary), on the SAME test set, with gauge arm E codebook-byte-identity (Step 3a) confirmed.
   - rationale: grill q8: committing this as a 'headline-clearing' run is wrong by construction — it lands FB->weak at n=10 regardless of pool. Reframing it as a labeled variance-mechanism drill-down (or letting the user jump to n≈27/the slope) prevents the sunk-GPU trap CLAUDE.md's preamble rule exists to prevent. The structural firewall (Step 3c) additionally guarantees this run cannot serialize a graduation verdict.
   - cost: ~1 hr Colab GPU (n=10 × 2 worlds + gauge ≈ 30 procs, --device cuda, parent must not touch CUDA, pre-warm wikitext cache); K× eval cost, same consolidation-run count. If re-scoped to n≈27: proportionally more. | depends_on: Step 9 (implementation), Step 8 (precheck + K), Step 3a + Step 3c, Step 2 (summary tooling), and the USER'S Step-10 scope choice. | anti-homunculus: Offline statistics decide the verdict, never the substrate. Verdict firewall (Step 3c) enforces this structurally. PASS.
13. **[sign_off_gated]** STEP 11 — DONE write-up + STATUS update for the window-averaging result. Amend/extend Report 115 (or a follow-on) to satisfy all five done-gates: headline (window-averaged level-DiD) with Student-t 95% CI; control on the SAME test set (stream-shuffle primary + gauge arm E codebook-byte-identity per Step 3a); drill-downs (realized σ(DiD) demonstrating the √K shrink 0.115->0.044 + honest-n vs 0.196; the Step-8 off-zero clearance precheck showing n=10 cannot clear zero by construction and n≈27 is the real lever; per-arm curves; Stage-1B corr_AC_BD; the √K-vs-saturation check); STATUS Recent-updates entry (≤5 lines, within budget) + memory update. Then RETURN THE BRANCH TO THE USER as one clean decision with corr_AC_BD + realized σ + the off-zero arithmetic in hand: (a) accept a labeled variance-mechanism drill-down result, (b) commit to the n≈27 level-DiD, or (c) escalate to the slope. State them as COMPLEMENTARY/orthogonal, with σ(β) plainly still-untested / Q1-open.
   - rationale: Closes the done-loop and honors the ruling that the scope/headline-pin choices are the user's calls. Presenting them as complementary guards against de-facto re-headlining the level-DiD; the verdict firewall (Step 3c) makes that re-headlining a type error rather than a discipline failure.
   - cost: ~2-3 hr write-up + STATUS/memory update. | depends_on: Step 10 (the run), Step 7 (Stage-1B), Step 8 (precheck), Step 1a/1b (walk-backs). | anti-homunculus: Reporting + a decision hand-off to a human. PASS.
14. **[branch_dependent]** STEP 12 — ESCALATION (conditional, NOT authorized now; trigger CORRECTED per grill q1 to escalate to the PILOT, not the build). TRIGGER (rewritten): IF the window-averaged level-DiD still straddles zero after a verified √K shrink, the residual is atom-floor-dominated -> escalate to the Stage-2 σ(β) PILOT to TEST whether that residual is intercept-loaded (slope removes) or shape-loaded (slope keeps). Build the ~1-day slope rig ONLY on a G1-G4 PASS — the level-floor diagnosis licenses the ~1hr pilot, NOT the build (the floor citation is 06:49, not 06:41). ORDER, with all slope-draft VALUE-fixes staying fenced until this branch fires: (i) slope reframe sign-off (Q2 pin fires only AFTER the σ-kill smoke clears); (ii) the THRESHOLD-HONESTY VALUE PACKAGE — ONLY the derive-the-value / user-ruling ops (the flag-the-contradiction ops already moved to Step 1a per q9+q10): recompute the DRAFT-02 floor on the finally-agreed e-fold span (input-gated on the still-open schedule question 04:72), replace the provisional 0.05 with the pilot-derived factor-F gate (input-gated on the unrun pilot, 03:23-25), record the user's G2=0.40 ruling (input-gated on user sign-off, 01:51/71); (iii) build the in-loop checkpoint hook AND its companion guards — FULL spec-4a (checkpointed-vs-non-checkpointed final-codebook + final-recall byte-identity, spec approx 169-172/187) PLUS the FIXED-mask hygiene fix (spec approx 101-102); (iv) the Stage-2 HOLDOUT pilot (seeds 1000..1009, disjoint from graduation 0..9) with G1-G4 to MEASURE σ(Δβ). Alternatively invoke CUPED on the e=0 covariate (spec approx 115-116) if Stage-1B returned C>0.3. The Sense-B novelty-allocation mechanism stays fenced and needs its OWN anti-homunculus reviewer pass (spec approx 177-180) — NOT built here.
   - rationale: grill q1 (judge-verified): the level-floor cannot prove the slope WILL help — that is a σ(β)-shape question the level run never returns. The corrected trigger escalates the floor diagnosis to the cheap ~1hr σ(β) PILOT (G1-G4 are exactly the additive-vs-shape discriminator), and the PILOT (not the 1-day rig) is what the floor heuristic gates into — non-circular and cheap-before-expensive. Full spec-4a + the fixed-mask fix belong HERE (grill q6) because they guard the in-loop hook that only exists on this branch.
   - cost: σ(β) pilot ~1hr GPU; IF G1-G4 pass: slope hook + orchestrator + tests (~1 day eng) + n=10 (or n≈27) slope graduation run ~1hr GPU. | depends_on: Step 10 result (level-DiD still straddles zero after verified √K shrink), Step 3a, user sign-off on the slope reframe + thresholds + the Q2 pin gate. | anti-homunculus: The checkpoint hook is read-only measurement at fixed data-independent event boundaries with no if-recall<θ-then-act branch (spec approx 161-167); full spec-4a byte-identity is the binding guard; consolidation still fires on the local buffer-fill event. Sense-B explicitly NOT built here. PASS for the hook.

### Open user decisions
- STEP-10 SCOPE (dispositive, decide with the Step-8 arithmetic in hand BEFORE any GPU spend): the window-averaged level-DiD CANNOT clear zero at n=10 by construction (+0.019 ± pool-limited σ spans zero even at K=9: CI approx [-0.013,+0.051]; only the unreachable atom floor barely clears). Choose: (a) run Step 10 as a LABELED variance-mechanism drill-down that proves √K shrank σ and feeds realized σ into honest-n, explicitly NOT clearing zero; (b) skip to the n≈27 level-DiD; or (c) escalate to the slope. The arithmetic says n≈27 SEEDS, not more windows, is the lever that clears zero.
- MAIN BRANCH (returned after Step 10 with corr_AC_BD + realized σ + off-zero arithmetic in hand): keep the LEVEL headline (window-averaged Frame A endpoint level-DiD) with the slope held open as the escalation lever, vs authorize the slope build in parallel. The two are complementary, not rivals; σ(β) is still untested (Q1-open); the headline-pin is yours.
- ARTIFACT RECOVERY — TWO DISTINCT ITEMS (Step 2): (a) recover the n=10 Gate-0 GRADUATION summary from off-repo Drive/scratch (enables zero-compute corr_AC_BD/Stage-1B + Report 115 headline DiD) OR re-run n=10 Gate-0 ~1hr Colab GPU; and SEPARATELY (b) re-run the variance-decomp GRID ~1hr GPU to reproduce the 3.8% — but (b) is NOT needed if Report 115 cites the split as a provenance-flagged qualitative drill-down. Recommendation: recover (a) from Drive; do NOT re-run (b) (the 3.8% is an over-read being retired; only the ranking matters).
- K (number of averaged window draws): pick K∈{2,4,9} subject to the Step-8 pool/independence budget. K=9 gives the largest projected σ↓ ONLY if the pool supports 9 independent draws; otherwise it saturates sub-√K. NOTE per the Step-8 precheck, even K=9 does not clear zero at n=10 — so this choice only affects the drill-down's demonstrated σ-shrink, not whether the level-DiD clears zero.
- G2 0.40 APPROVAL STATE (escalation-only, but the Step-1a flag is decision-free and lands now): was 0.40 ever verbally approved? Drafts contradict ('needs your number' at 01:50-51/71 vs '(user-approved)' at 04:62) with NO recorded sign-off event. Absent a recorded event the agent treats 0.40 as PROPOSED (conservative 01:71 reading). A recorded ruling is required before any draft feeds a binding slope spec.
- SLOPE REFRAME PIN (escalation-only): the reframe is accepted-in-direction (q2 USER RULING, self-fenced) but PROPOSED-pending-sign-off on every binding surface (spec:6, deep-dive:189/191, STATUS:17). The pin into phase-3-deep-dive.md §Headline fires ONLY after the σ-kill smoke clears + your sign-off. Confirm you want the pin gated this way (do not pin pre-emptively).
- SLOPE SIGN-OFF NUMBERS (escalation-only): the PROPOSED items at spec approx 256-273 remain pending — checkpoint schedule + its e-fold span (still-open: extend e_max above 1000? 04:72), n, 0.70 robustness, the floor (re-derived on the finally-agreed span), the realized-σ gate form, two-clause criterion, control set, pin. None bind until you approve; the window-averaging-first path does NOT need these.
- σ GATE FORM (escalation-only): keep 0.05 as an explicitly-PROVISIONAL placeholder (flagged in Step 1a), or replace with the pilot-derived factor-F gate (03 H1)? The bare 0.05 is circular as a build gate (you don't know σ is small until you run the pilot).
- COMPUTE TARGET: local MPS vs Colab CUDA for the ~30-proc window-averaging run (and any n≈27 re-scope). Colab is tolerated-not-default; substrate auto-detect is MPS/CPU only so Colab MUST pass --device cuda and the parent must not touch CUDA before launching workers. Recommend local MPS first; Colab only if it stalls.

### Done gates
- Walk-back landed FIRST (Step 1a): the 05-29 over-read correction is in the STATUS:16 Current-state LEAD (not just 18/27); the 92-vs-96 discrepancy is corrected to ranking-language; spec §Why-a-slope + §Variance-handling annotated with a pointer to the 05-29 decomposition; drafts 01/04/05 reflect the over-read AND every co-located stale threshold claim is FLAGGED not varnished (04:62 G2 marked DISPUTED, 04:61 0.05 marked PROVISIONAL, two-corr glossary added). Size hook (STATUS:62) passes. NO un-flagged stale threshold claim remains on any line the walk-back pass touched.
- No specific percentage was pinned to a binding doc or Report 115 before Step 2's tri-state artifact verdict returned (Step 1b gated on Step 2).
- Anti-homunculus guard landed (Step 3a): a CODEBOOK-ONLY byte-identity test passes (read-only eval/window-average => bit-identical final codebook + buffer/C.2 state; final-recall NOT asserted, since averaging changes recall on purpose); full suite (547+ tests) still green. (Full spec-4a with the fixed-mask fix is deferred to Step 12 with the checkpoint hook.)
- Structural verdict firewall landed (Step 3c): a level-DiD run object cannot serialize any FB-* slope verdict, and the slope graduation function RAISES on inputs lacking per-checkpoint slope data; regression test green. The level-DiD physically cannot become a graduation claim through any code path.
- Axis-lock/rename done with NOTHING over-locked (Step 4): n_consolidation_events renamed to an exposure-accurate name at all call sites + tests, full suite green; the axis TYPE is locked; NO anchor set, NO e-fold span, and NO floor signed off in the prereq phase.
- Report 115 written under reports/ (no 115 existed): experiment-preamble citing spec approx 40-46 headline + spec approx 100-108 control; headline DiD +0.019 CI [-0.121,+0.159] (on-repo, STATUS:18) with the gauge arm E control on the same test set; explicit DRILL-DOWN / NOT-GRADUATION label. The variance drill-down section (gated on Step 2) cites the 92/96/3.8 split as a PROVENANCE-FLAGGED qualitative ranking, never as the figure discharging done-gate #4. Done-gate #4 discharged by the on-repo DiD.
- Stage-1 read reconciled against the pre-06 binary BEFORE acting (Step 6; routed to option (e) keep-slope-open, never the stale kill); corr_AC_BD computed + recorded as a DESCRIPTIVE number with NO build-licensing gate — and Step 7 is labeled zero-compute IFF the summary was recovered, else a ~1hr re-run.
- Off-zero clearance precheck recorded BEFORE any GPU spend (Step 8): for the max achievable K and the atom floor, the projected n=10 CI is reported; the finding that K=9 spans zero and n≈27 is the real lever is in the plan and surfaced to the user; Step 10 proceeds only under an explicitly-labeled scope (drill-down, or user-chosen n≈27/slope).
- Window-averaged Frame A endpoint level-DiD reported at n=10 (Step 10/11) with Student-t 95% CI (via c3._delta_ci_stats); realized σ(DiD) + implied honest-n vs 0.196 reported, demonstrating the √K shrink actually moved σ (0.115->0.044); control (stream-shuffle primary + gauge arm E with codebook-byte-identity) on the SAME held-out windows; the Step-8 √K-vs-saturation + off-zero precheck reported as drill-downs.
- STATUS + memory updated (Step 11); the scope/branch decision returned to the USER with corr_AC_BD + realized σ + off-zero arithmetic in hand, the level and slope stated as complementary/orthogonal, σ(β)-untested / Q1-open stated plainly; NO threshold and NO headline-pin signed off before the user chooses.

### Risks
- TOOL-CHANNEL FAILURE (process, ACUTE this session): the intermittent silent-tool-output failure documented across the q2/q3/q7/q8/q10 judge_notes RECURRED for ME this session — Bash and Read returned EMPTY for STATUS.md, the spec, reports/, the JSON find, and the grill drafts after the harness had already loaded CLAUDE.md. I could NOT re-open any disputed line firsthand this session. Every line number in this plan therefore rests on (a) the grill JUDGES' firsthand re-opens documented in their judge_notes (which independently confirmed the defenders and corrected the questions' drifted cites), and (b) the synthesized plan's own firsthand-verified set. Given the repo's fabricated-citation history, an executing agent MUST re-open EVERY line cited here with a working channel BEFORE binding any edit. Treat all line numbers as SEARCH HINTS, not edit anchors; key every Edit on a unique verbatim substring + a pasted grep hit (Step 1 mechanical discipline).
- OFF-ZERO BY CONSTRUCTION (dispositive, judge-verified arithmetic): the window-averaged level-DiD CANNOT clear zero at n=10 — K=9 gives CI approx [-0.013,+0.051], only the unreachable atom floor barely clears, n≈27 seeds is the real lever. If Step 8's precheck is skipped, the team burns ~1hr GPU on a run that lands FB->weak by construction. The precheck is decision-free and MUST precede the spend.
- ESCALATION-TRIGGER CIRCULARITY (corrected, q1): the level-floor cannot prove the slope WILL help (that is a σ(β)-shape question the level run never returns). If the corrected trigger wording is dropped, an agent could waste a ~1-day slope build on variance the slope cannot touch. The trigger now escalates to the ~1hr σ(β) PILOT (G1-G4), and only a pilot pass licenses the build. Floor citation is 06:49, not 06:41.
- ARTIFACT UNREPRODUCIBLE (q2, judge-verified): the n=10 Gate-0 summary JSON and variance_decomp.json are firsthand-confirmed ABSENT from disk and git (summary lives on Drive per scripts/colab_frameb_smoke_stage1.ipynb). Stage-1B is zero-compute ONLY if recovered; the 3.8% point estimate is unreproducible from on-repo artifacts. Only the qualitative window>>atom ranking is load-bearing and it survives the loss; the 3.8% is an over-read being retired. Report 115 and the spec walk-back must cite the split as provenance-flagged, never as a reproducibility-gate figure.
- DOCUMENTARY-FIREWALL FAILURE (q3): the spec's brakes (Δβ-only graduation clauses spec:121-140 + the preamble) are documentary and have failed before (K-branch state_divergence drift). The level-DiD is structurally positioned to be over-claimed as graduation. Mitigated by the STRUCTURAL type-disjoint verdict firewall (Step 3c) — but if Step 3c is skipped, the cheap path's strongest result can silently re-headline the PROPOSED slope-DiD.
- FALSE-FRESHNESS STALENESS (q9+q10): walking back the over-read in 04 while leaving co-located stale threshold claims ('(user-approved)' G2 at 04:62, bare 0.05 at 04:61) varnished re-introduces the exact staleness the walk-back-first rule exists to kill (2026-05-24 'freq-weighted α never built' chain). Mitigated by flagging all co-located stale claims on the SAME Step-1a pass; the derive-the-value fixes stay correctly fenced in Step 12.
- PREMATURE FLOOR PIN VIA SPAN-LOCK (q4): floor = numerator / span; locking the {100,...} anchor VALUES pins the 2.303-e-fold span and half-pins the floor (0.06/2.303 ~= 0.026/e-fold) the user has not signed off. Mitigated by locking ONLY the axis TYPE + rename in Step 4; the anchor set, its span, and the floor move together into the deferred Step-12 sign-off.
- ATOM-DRAW FLOOR (~0.0224) is untouchable by window-averaging; if the level-DiD signal is small relative to it, no √K shrink lifts the n=10 CI off zero — already computed in Step 8, not a downstream surprise. The binding lever is n≈27 seeds or the slope, not more windows.
- WRONG-SCOPE TEST 4a (q6): full spec-4a guards an in-loop checkpoint hook the cheap path never adds; front-loading it tests trivial self-identity or is false-by-design (averaging changes recall). Mitigated by the narrow codebook-only guard now (Step 3a) and deferring full spec-4a + the fixed-mask fix to Step 12 with the hook.
- DRILL-DOWN MISLABEL (q3/q8): corr_AC_BD and the window-averaged level-DiD are NOT the spec headline (slope-DiD, spec approx 40-46). Every preamble MUST label them drill-downs, or the project repeats the diagnostic-drifts-into-headline failure. The 'Step 10 is only a drill-down' framing leans on spec:219-220/270 which I could NOT re-verify this session — re-open before binding.
- STAGE-1B CANNOT CONFIRM (05:34): corr_AC_BD can falsify but not confirm the slope; the σ(β) conditional (Q1) stays genuinely untested and only the Stage-2 holdout pilot can settle it.
- PHANTOM APPROVALS (q9): two unrecorded approvals exist — reframe 'conditionally-approved' (STATUS:27, self-fenced by the q2 pin gate) and G2 '(user-approved)' (04:62, NO brake, in the more-developed draft). The G2 phantom is neutralized by the Step-1a flag; absent a recorded event the agent treats 0.40 as PROPOSED.
- BRANCH HYGIENE: work is on codex/phase5-prime-bundle-first-scene-memory, not main. Nothing is committed/pushed unless the user asks; the pin into phase-3-deep-dive.md is the one binding edit, gated on the σ-kill smoke + the Q2 ruling — do not pin pre-emptively. An FB->pass graduates Phase 3 ONLY; nothing here unpauses Phase 5′.

### Grill hardening applied
- q1 (escalation-trigger circularity): REWROTE the Step-12 trigger to escalate the atom-floor diagnosis to the ~1hr σ(β) PILOT (G1-G4 measure the additive-vs-shape discriminator), NOT directly to the ~1-day slope build — closing the circularity where the level run cannot prove the slope will help. Fixed the floor citation to 06:49 (was 06:41). The PILOT, not the rig, is what the floor heuristic gates into; only a G1-G4 pass licenses the build.
- q2 (zero-compute artifact premise): STOPPED calling Stage-1B unconditionally zero-compute — it is zero-compute IFF the off-repo Drive summary is recovered, else a ~1hr re-run. Made Step 2 return a TRI-STATE verdict and SPLIT the artifact recovery into two distinct compute items (graduation summary vs variance-decomp grid). Report 115 + the spec walk-back now cite the 92/96/3.8 split as a PROVENANCE-FLAGGED qualitative drill-down; done-gate #4 is discharged by the on-repo DiD, not the decomposition.
- q3 (level-DiD as de-facto headline): ADDED a STRUCTURAL type-disjoint verdict firewall (Step 3c) so a level-DiD run can never serialize an FB-* slope graduation verdict and the slope graduation function RAISES on inputs lacking per-checkpoint slope data — converting the documentary drill-down label into a type error. Recorded the corrected citation finding (spec is 273 lines, 10-item sign-off, item 9 @ 270 = continuity drill-down; the grill's 183-line/8-item premise was FALSE).
- q4 (axis-lock half-pins the floor): NARROWED Step 4 to lock ONLY the axis TYPE + the rename; do NOT name/lock any anchor set or its e-fold span (floor = numerator/span, so locking values half-pins the floor). Moved the anchor-set choice + span + DRAFT-02 floor re-derivation together into the deferred Step-12 sign-off. Updated done-gate #5 accordingly.
- q5 (walk-back on unverified cites): HARDENED Step 1 from a trusted 're-open before editing' instruction to a MECHANICAL gate — key every edit on a unique verbatim substring, run grep -n and paste the hit BEFORE the Edit, and use full draft filenames. (q5's factual premise was judge-confirmed FALSE — the inherited audit cites match disk, the question's replacements did not — so this is hardening, not a sequencing change.)
- q6 (test 4a guards a mechanism the cheap path lacks): SPLIT test 4a — Step 3a is now a NARROW codebook-only eval-isolation guard (no final-recall assertion, since averaging changes recall on purpose), correctly scoped to the cheap path; full spec-4a + the fixed-mask hygiene fix are DEFERRED to Step 12 with the in-loop checkpoint hook they actually guard. Fixed done-gate #2 (dropped '+ final recall'). Re-pointed spec:NNN citations from the 47-line superseded phase-3-deep-dive.md to the 2026-05-28 dated design note.
- q7 (parallel writes bake an unverified number): ADDED a hard dependency edge — SPLIT Step 1 into 1a (qualitative walk-back, runs now) and 1b (point-split citation, gated on Step 2's verdict); gated Report 115's variance section on the same verdict; corrected the 92-vs-96 discrepancy as part of the qualitative walk-back; re-filed the artifact recovery decision to gate the WRITES, not only the corr_AC_BD diagnostic.
- q8 (saturation/off-zero discovered after the spend): ADDED a decision-free OFF-ZERO CLEARANCE PRECHECK to Step 8 (judge-verified: K=9 spans zero at n=10, n≈27 is the real lever), gated Step 10 to proceed only as a labeled variance-mechanism drill-down or be re-scoped to n≈27/slope BEFORE the GPU spend, added the dispositive Step-10-scope open user decision, and restated the atom-floor risk as an already-computed precondition rather than a downstream surprise.
- q9 (phantom G2/reframe approvals): ADDED decision-free contradiction-FLAG ops to Step 1a's scope (mark 04:62 G2 as DISPUTED, demote to PROPOSED) so the silent-promotion is neutralized while the file is open; kept the 'was 0.40 approved' user decision; noted the reframe phantom is self-fenced by the q2 pin gate and needs no new step.
- q10 (threshold fixes fenced while drafts go half-edited): SPLIT the Step-12 threshold-honesty package — the three DECISION-FREE flag-the-contradiction ops (G2 disputed, 0.05 provisional, corr-glossary) move into the Step-1a walk-back pass so the drafts are left internally consistent; ONLY the three DERIVE-THE-VALUE / user-ruling ops (floor on the agreed span, factor-F gate, recorded G2 ruling) stay fenced in Step 12 because their inputs do not exist until escalation. Updated done-gate #1 to require no un-flagged stale threshold claim survives the walk-back pass.

## Grill outcomes (12 Q; 6 resolved, 3 need plan revision, 3 need user decision)

### q1 [needs_plan_revision] — Step 6/7 + main_branch_decision: the still-OPEN Q1 σ-kill premise
**Q:** The plan's escalation gate (Step 12) fires the slope build ONLY if the window-averaged level-DiD 'still does NOT clear the bar BECAUSE the residual ~0.0224 atom-draw floor blocks power.' But 06:78-90 (firsthand) says σ(β)/σ(Δβ) was NEVER measured by Stage-1 — only the ENDPOINT level variance was. The 0.0224 atom-floor (06:41) is a LEVEL-variance floor, not a σ(β) floor. By what computation does the plan distinguish 'level-DiD failed because of the atom floor' (slope can't help — the shape-changing fraction defeats it) from 'level-DiD failed because of an additive shift the slope WOULD remove'? If that distinction can only be made by the Stage-2 holdout pilot (06:92-96), then the escalation TRIGGER itself silently presupposes the very σ(β) measurement the plan defers to escalation — making the trigger condition unmeasurable at the moment it must be evaluated.
**Judge:** Narrow hit lands. The pilot measures slope variance inside escalation, so no real circularity, but the trigger wording wrongly licenses the build at the floor.
**Plan change:** Rewrite the trigger to escalate to the pilot not the build, and fix the floor citation to line 49.

### q2 [needs_plan_revision] — Step 2 + open_user_decisions: zero-compute Stage-1B premise
**Q:** Step 2 and Stage-1B (Step 7) are built on 'the EXISTING n=10 Gate-0 summary JSON' (05:10, firsthand). I ran find across the entire repo for *gate0*/*frame_a*/*frameb*/*variance*/*summary* .json and git-tracked artifacts — ZERO matches. The plan's own risk section concedes the artifact is 'asserted NOT on disk.' Yet Stage-1 ALREADY produced the 96%/3.8% decomposition (06:37-38, STATUS:46) that everything downstream rests on. Where did that decomposition come from if the summary JSON it requires does not exist on disk and is not in git? Either (a) the JSON exists in a session-scratch location my find missed, in which case the plan must name the exact path before calling Stage-1B 'zero-compute'; or (b) the decomposition was computed from an ephemeral artifact now gone, in which case the 92-96% number itself is unreproducible and Stage-1B is a ~1hr GPU re-run, not zero-cost.
**Judge:** CORRECTION TO MY FIRST RULING: my initial StructuredOutput accused the defender of fabricating its code/file citations. That accusation was WRONG and I retract it — it was caused by a misread of a truncated early grep (and the intermittent silent-tool-output failure this session, the same one the plan's risk section flags). The authoritative greps DO confirm the defender's structural citations: experiments/gate0_frame_a.py is 963 lines (not 591) and genuinely contains variance_report_from_summary (def at :566), corr_AC_BD (:620), run_variance_decomposition (def at :668), var_fraction_atom (:823), the --variance-report (:875) and --variance-decomp (:878) flags, and it WRITES gate0_summary.json (:835) and variance_decomp.json (:911). scripts/colab_frameb_smoke_stage1.ipynb EXISTS and references --variance-decomp/gate0 variance tools; brainstorm-workspace/2026-05-29-frameb-grill/06-stage1-review-findings.md EXISTS. So the defender's symbol/line citations are substantially CORRECT, not fabricated (some exact line offsets like var_atom:751 I could not re-verify before the tool channel went silent, but the functions and flags are real). The defender's CORE conclusion is firsthand-confirmed and correct: NO gate0/variance/summary JSON exists on disk (only config/phase5_m1_codebook_registry.json, .claude/settings.local.json, notes/.../theta_prime_calibration.json) and git log --all --diff-filter=A added the gate0 CODE + notebooks + the 06 markdown but NEVER any gate0_summary.json/variance_decomp.json. So question branch (b) is the true state: the 92%/3.8% split is a single-run, off-repo-sourced number, unreproducible from on-repo artifacts. The defender concedes the bite, correctly resolves it to (b), and correctly distinguishes the two recovery costs (variance_decomp grid for the 3.8% vs n=10 Gate-0 summary for corr_AC_BD/Stage-1B). The defender's ONE real citation error: it says the decomposition lives at STATUS:18 + STATUS:27. The actual lines (STATUS.md is 68 lines) are STATUS:18 (Active deliverable — yes, the 92%/3.8% IS there) and STATUS:27 (Recent updates — yes, also there). So the defender's STATUS:18/27 is CORRECT and the QUESTION's STATUS:46 is the wrong cite (line 46 is a Live-operational-policies drift line). My first note's claim that the decomposition is at "STATUS:23-27 not 18/27" was also wrong — it is at 18 and 27 exactly as the defender said. Net: the defender's answer is substantively sound and well-grounded; the plan defect it surfaces (Step 7 "zero-compute" unconditional + threading 3.8% toward done-gate #4) is real and must be fixed.
**Plan change:** Three plan edits, all firsthand-confirmed: (1) Stop calling Step 7 / Stage-1B "zero-compute" UNCONDITIONALLY. The consuming artifact (gate0_summary.json for --variance-report, or the atom×window grid for --variance-decomp) is firsthand-confirmed absent from disk and git. State plainly: corr_AC_BD/Stage-1B is zero-compute IFF the n=10 Gate-0 summary is recovered from off-repo (Colab/Drive/scratch — the notebook scripts/colab_frameb_smoke_stage1.ipynb reads it from Drive, not in-repo); otherwise it is a ~1hr n=10 Gate-0 re-run. (2) Report 115 and the STATUS:18/27 + spec walk-back must cite the 92%/3.8% split as a PROVENANCE-FLAGGED, single-run, non-reproduced QUALITATIVE drill-down (source variance_decomp.json ephemeral/off-repo, NOT regenerated this session) — never as a figure that discharges done-gate #4. Done-gate #4 is discharged by the on-repo headline DiD +0.019 CI [-0.121,+0.159] (STATUS:18), not by the decomposition. The load-bearing survivor is only the qualitative window-much-greater-than-atom ranking, which is robust to the artifact loss; the 3.8% point estimate is an over-read being retired, so it does NOT need to be reproducible. (3) Split the plan's single "summary recovery vs re-run" user-decision into TWO distinct compute items, as the defender argues: (a) Gate-0 graduation summary recovery -> enables corr_AC_BD/Stage-1B + Report 115 headline DiD; (b) variance_decomp grid re-run -> would re-reproduce the 3.8%, but per fix (2) it is NOT needed if 115 cites the split as a provenance-flagged qualitative drill-down. The plan currently conflates these as one ~1hr item and never costs out the variance-decomp grid separately.

### q3 [needs_plan_revision] — Step 9/10/11: window-averaging vs slope sequencing — the level-DiD as de-facto headline
**Q:** The plan keeps the slope-DiD as the PROPOSED spec headline (spec:13-17, 41-51, firsthand) while making the window-averaged ENDPOINT level-DiD the thing it actually runs at n=10 to clear STATUS blocker (2). It labels that run a 'DRILL-DOWN' (Step 10) citing 'spec:219-220 + sign-off item 9 (spec:270)' — but the spec is only 183 lines, so those citations point off the end of the file and item 9 does not exist (sign-off has 8 items, spec:173-182). On what spec authority is the level-DiD a 'continuity drill-down' rather than a competing headline? If the level-DiD CI clears zero at n=10, what — beyond the user's discretion — STOPS that result from becoming the de-facto graduation claim for a headline (slope-DiD) that was never measured, exactly the 'diagnostic drifts into headline' failure CLAUDE.md was written to prevent?
**Judge:** RULING: needs_plan_revision. (This SUPERSEDES my earlier provisional note that said the tool channel was dead — the calls were buffered, not dead, and all output arrived. I verified every disputed line firsthand and am correcting the record.)

FIRSTHAND VERIFICATION (all read this session, decisive):
- The spec notes/notes/2026-05-28-frame-b-exposure-slope-headline-design.md is 273 lines, confirmed four independent ways (wc -l x3 + Read of lines 1-273). It is NOT 183 lines.
- The Sign-off section (read firsthand, lines 256-273) has TEN numbered items, not eight. Item 9 @ spec:270 reads verbatim: "9. **Secondary:** endpoint level-DiD reported as a continuity drill-down." Item 10 @ spec:271-273 is the pin.
- Drill-down (3) @ spec:219-220 (read firsthand) = "(3) **endpoint level-DiD** (the prior Gate-0 metric — kept for continuity + as the variance baseline beaten; positive level-DiD + flat slope ⇒ 'landscape carries it, not consolidation')".
- The graduation criterion (spec:121-140, read firsthand) is written over Δβ (the slope) ONLY — clause 1 = _delta_ci_stats(Δβ).ci95_above_zero AND mean Δβ ≥ 0.01/e-fold; clause 2 = per_seed_robust_ge_threshold(0.70) on Δβ(s). The endpoint level-DiD appears in NO graduation clause; it is explicitly listed as drill-down (3) and Sign-off item 9.
- The TL;DR headline @ spec:40-46 (read firsthand) pins the slope-DiD Δβ as THE headline.

VERDICT ON THE CITATION SUB-DISPUTE: The DEFENDER IS CORRECT and the GRILL'S FACTUAL PREMISE IS FALSE. The grill asserted "the spec is only 183 lines... [spec:219-220, spec:270] point off the end... item 9 does not exist (sign-off has 8 items, spec:173-182)." Every one of those claims is wrong against the file on disk. The plan's "spec:219-220 + sign-off item 9 (spec:270)" anchor for the continuity-drill-down label is REAL and resolves exactly. So the spec authority for treating the level-DiD as a drill-down (not a competing headline) is sound: TWO independent firsthand-verified loci (drill-down (3) @ 219-220 and Sign-off item 9 @ 270), plus the slope-only graduation clauses @ 121-140. This is the rare grill where the grill-master, not the defender, fabricated the citation premise. (Note: the user-question's own "spec:13-17, 41-51" form is loose but lands in the frontmatter+headline+estimand region; the canonical headline anchor is 40-46. The "183/8-item" file the grill describes exists nowhere — deep-dive is 316 lines, the dated note is 273.)

WHY needs_plan_revision NONETHELESS: Correcting the citation does NOT dissolve the grill's underlying structural question, and the defender honestly concedes this. The plan runs the window-averaged ENDPOINT level-DiD at n=10 to clear STATUS blocker (2) — the strongest result the cheap path can produce — while the PROPOSED headline (slope-DiD) is never measured. The spec's own brakes are: (a) graduation clauses written over Δβ only (spec:121-140, firsthand), so a level-DiD pass literally cannot satisfy clause 1 or 2; (b) the CLAUDE.md experiment-preamble forcing every off-headline run to be labeled DRILL-DOWN. But (a) and (b) are DOCUMENTARY brakes — they depend on the next agent re-reading the spec and obeying the preamble. The plan even names this in its own risks ("DRILL-DOWN-MISLABEL RISK") and grill_hardening, which confirms the gap is real and currently unmitigated at the mechanism level. CLAUDE.md exists precisely because this repo has already let a diagnostic drift into a headline over multiple sessions (the K-branch state_divergence precedent). A clean level-DiD CI off zero at n=10 is structurally positioned to be over-claimed as graduation, and the plan's defense ("we label it a drill-down") is the same class of defense that failed before. The defender's proposed fix (type-disjoint verdict enums so FB->pass is unreachable from any non-slope code path; graduation function rejects inputs lacking per-checkpoint slope data) is sound, decision-free, and anti-homunculus-clean (a measurement-typing constraint, not a supervisor). But it is NOT yet in the plan's sequenced_steps or done_gates — Steps 5/11 and their done-gates still rely on prose "LABEL IT A DRILL-DOWN." So the gap is live in the artifact under review.

The headline-PIN remaining the user's call is correctly out of scope (genuine user discretion, gated on Sign-off item 10 @ spec:271-273, firsthand); that part needs no change.
**Plan change:** Add a STRUCTURAL (not documentary) firewall so the endpoint level-DiD cannot become the de-facto Phase-3 graduation claim, and record the corrected citation finding:

1) NEW decision-free step + done-gate, inserted BEFORE the n=10 window-averaged level-DiD run (current Step 10/11): make the slope graduation verdict TYPE-UNREACHABLE from any code path that did not compute Δβ over the checkpoint schedule. Concretely — gate0._classify_verdict / _classify_frame_b_verdict (slope; emits FB->pass/weak/null-slope/dead-slope/confound/non-linear only, per spec:147-154 firsthand) and the new level-DiD classifier MUST be distinct functions with NON-OVERLAPPING verdict enums (level emits e.g. LVL->tighter / LVL->still-flat only). The level-DiD summary serializer must be physically incapable of emitting any FB-* verdict. Add a regression test asserting (i) a level-DiD run object cannot serialize any FB-* verdict, and (ii) the slope graduation function RAISES on any input lacking per-checkpoint slope data (the spec already half-implies this at spec:144-145, firsthand: "Verdict is a pure function of stored per-seed stats"). This converts "agent promises to label it a drill-down" into a type error. Land it alongside test 4a (Step 3) and before Step 10.

2) Record in the plan (and in the Step-1 walk-back / q3 workspace) the corrected citation finding: the spec is 273 lines with a 10-item Sign-off; item 9 @ spec:270 = continuity drill-down; drill-down (3) @ spec:219-220; slope-only graduation @ spec:121-140 — ALL verified firsthand. The grill's "183-line / 8-item / item-9-missing" premise is FALSE and must not propagate into any downstream draft. The plan's Step-11 preamble citation "spec:219-220 + sign-off item 9 (spec:270)" is CORRECT and stays.

3) Keep the headline-PIN as the user's call (no change) — but it is now additionally gated behind the type-disjoint-verdict firewall landing, so a level-DiD pass cannot be pinned as a Phase-3 graduation through any code path even if a future agent ignores the prose labels.

### q4 [needs_plan_revision] — Step 4 axis-lock/floor fence + checkpoint-axis/floor coupling
**Q:** Step 4 locks the checkpoint schedule 'AXIS + VALUES only' to {100,178,316,562,1000} while fencing the floor as a deferred threshold. But the floor and the schedule span are not separable: 02:23-26 (firsthand) shows the corrected schedule has a 2.303-e-fold span vs the broken schedule's 4.135, and 04:24-29 warns the floor 'changes AGAIN' and 'must use the FINAL agreed schedule's e-fold span.' If you LOCK the schedule values now but DEFER the floor, you have pinned the denominator (2.303 e-folds) of a floor computation while leaving the numerator unsigned — so the moment the slope branch fires, the locked schedule forces a specific floor recompute (0.06/2.303 ≈ 0.026/e-fold) that the user has not seen. Why is locking the schedule values NOT itself a premature pin of the floor, given the floor is a pure function of the span you just froze?
**Judge:** CORRECTION TO MY PRIOR OUTPUT: My first StructuredOutput call claimed "VERIFICATION BLOCKED — tool channel failed." That is FALSE and I retract it. The reads did return content; I issued that output prematurely before the batch resolved. I have now re-opened every cited line firsthand. This ruling supersedes the prior one.

FIRSTHAND-VERIFIED (all reads returned content this session):
- 04-smoke-harness-DRAFT.md:69-71 — verbatim: the corrected {100,178,316,562,1000} schedule has "span = log10(1000/100) = 1 decade = 2.303 e-folds. NOTE this changes the floor span again — the floor recalibration (defect 02-b) must use the FINAL agreed schedule's e-fold span." 04:72-73 is the open span-widening question. CONFIRMS the question's 04:24-29 paraphrase and the defender's quote.
- 02-defect-corrections-DRAFT.md:23-37 — floor is a "THRESHOLD → your sign-off" with three options; 02:28-29 option (b) = 0.06/4.135 ≈ 0.0145/e-fold on the OLD 4.135 span; 02:36-37 "this couples to the item-9 axis fix (the e-fold span depends on the corrected axis)." CONFIRMS the question's 02:23-26 paraphrase and the defender's quote. NOTE the question said "02:23-26 shows the corrected schedule has a 2.303 span vs 4.135" — that 2.303-vs-4.135 contrast actually lives in 04:69-71 + 02:12, not 02:23-26; minor mis-citation by the QUESTION, immaterial to its logic.
- spec:6 = PROPOSED; spec:258 still lists the OLD {16,32,64,125,250,500,1000} as PROPOSED item #1; spec:261-262 states the floor as span-relative PROSE ("re-derive if the span/event count changes"), NOT a hard number. This confirms the defender's claim that the spec has NOT locked {100,...} and confirms the plan's own PLAN-CITATION CORRECTION (the 0.0145/4.135 numbers are in DRAFT 02, not the spec).

ARITHMETIC: defender's 0.06/2.303 ≈ 0.0261/e-fold is correct and matches the grill's 0.026.

MERITS: The grill's core logic is airtight and I confirm it. floor = numerator / span; span = log10(max/min) of the anchor set; so locking the anchor VALUES pins the span (2.303 e-folds) which half-pins the floor denominator. You cannot honestly call the floor "deferred" while the span — a pure function of the locked values — is locked. The grill found a REAL defect in the plan's Step 4 WORDING: Step 4 simultaneously (a) says "lock the AXIS and schedule VALUES only" and (b) names the specific corrected set {100,178,316,562,1000}, and it fences the floor but NOT the span. The defender concedes this honestly (points 4-5), does NOT bluff the arithmetic, and proposes the correct fix: narrow Step 4's decision-free lock to (axis-TYPE + rename) only and move the anchor-set choice + its e-fold span + the DRAFT-02 floor re-derivation TOGETHER into the deferred slope-escalation sign-off (Step 12 / spec sign-off items #1 and #4). That preserves the user's open choice rather than improvising one, so this is correctly NOT needs_user_decision.

WHY needs_plan_revision not resolved: a conceded real defect in the plan text is by definition a required edit. The grill's premise about Step 4 naming a specific set is accurate against the plan text I can read directly; the defender's mitigating reads (set is gated, spec unlocked) are firsthand-confirmed and shrink the bite but do NOT eliminate the wording defect — the plan still says "lock schedule VALUES" and still names the 2.303-span set in the decision-free phase, which is exactly the premature pin the grill identified.
**Plan change:** Edit sequenced_steps Step 4 ("AXIS-LOCK / RENAME with a HARD SCOPE FENCE"). Replace "lock the AXIS and schedule VALUES only ... IF the slope path is later specced, replace the structurally-broken {16,32,64,...} with the corrected >=100-anchored {100,178,316,562,1000}" with: lock ONLY (i) the axis TYPE (exposure-counted, one observe() per unit) and (ii) the n_consolidation_events -> n_observations/n_exposures rename. Do NOT name, choose, or lock ANY checkpoint anchor set or its e-fold span in this prerequisite phase. Add rationale: the per-e-fold floor is a pure function of the locked span (floor = numerator / log10(max/min)); naming/locking the {100,178,316,562,1000} VALUES pins the 2.303-e-fold span (04:69-71) and therefore half-pins the floor denominator (0.06/2.303 ~= 0.026/e-fold) the user has not signed off. The anchor-set choice, its e-fold span, AND the DRAFT-02 floor re-derivation (02:23-37) are ONE coupled threshold decision and move together into the deferred slope-escalation sign-off (Step 12 / spec sign-off items #1 and #4, spec:258 + 261-262). Update done_gate #3 to: "axis TYPE locked + rename done; NO anchor set, NO e-fold span, and NO floor signed off in the prereq phase." (Note: the QUESTION mis-attributes the 2.303-vs-4.135 contrast to 02:23-26; it actually lives in 04:69-71 + 02:12 — does not change the fix.)

### q5 [resolved] — Step 6 + risks: walk-back-first applied to citations the plan cannot verify
**Q:** Step 6 makes 'reconcile the 06 over-read into the stale pre-06 binary' a HARD precondition, and Step 1 edits drafts 01/04/05 to walk back 'do NOT reframe; Frame A is correct' / 'slope reframe not the lever.' The plan flags these draft line cites as NOT firsthand-verified. I verified them: the 'do NOT reframe; the LEVEL headline (Frame A) is correct' line is at 01:27-28, not the audit's 01:56-58; '04:62 (user-approved)' is actually 04:16; '03:42-55 precedence ladder' is actually 03:14-21; '02:10-37 floor math' is actually 02:5-26. EVERY draft citation the plan inherits from the audit is off by 20-40 lines because the on-disk drafts are ~30-40 lines, not ~70-120. Given CLAUDE.md's fabricated-citation history and that the walk-back is supposed to be 'the first edit,' how does the plan avoid the executing agent editing the WRONG lines (or non-existent ones like the audit's '01:66-67 no anti-homunculus reviewer pass needed,' which does not exist in on-disk 01 at all)?

### q6 [needs_plan_revision] — Step 3 anti-homunculus: test 4a as guard for a mechanism the cheap path never adds
**Q:** Step 3 builds byte-identity test 4a as a 'decision-free prerequisite' that 'lands before any averaged run,' justified as the anti-homunculus binding guard (spec:139, 'FB→confound: test 4a not byte-identical'). But the window-averaging path inserts NO in-loop checkpoint hook — it only varies window_seed_override for read-only eval, which the audit already showed is RNG-isolated. The thing test 4a guards (the in-loop checkpoint hook perturbing C.2 state) does not exist on the cheap path. So for the window-averaging run, 4a is asserting byte-identity of a run against itself with a different EVAL mask, which the plan ALSO wants to fix by passing a fixed mask (spec:104-106). Does test 4a, as scoped for the window-averaging run, actually test anything load-bearing — or is it being front-loaded as a prerequisite for a mechanism (the slope/checkpoint hook) that the plan explicitly defers to a branch that may never fire?
**Judge:** The question identifies a real, firsthand-verifiable scoping error and the defender correctly concedes it. I re-opened every load-bearing line.

VERIFIED FIRSTHAND:
1. Fabricated/misattributed citations: notes/emergent-codebook/phase-3-deep-dive.md is only 47 lines and the file ITSELF documents the misattribution (phase-3-deep-dive.md:7-12: "this file is 47 lines long. Re-point every spec:NNN citation at the dated design note"). So the question's "spec:139" and the plan's spec:NNN anchors do NOT resolve in phase-3-deep-dive.md. The real spec is notes/notes/2026-05-28-frame-b-exposure-slope-headline-design.md. The "FB->confound: byte-identical 4a" line is at 2026-05-28-...:153, not :139. The defender's relocation is correct.

2. Question's core diagnosis is RIGHT and load-bearing: test 4a's subject is the checkpoint-eval hook. 2026-05-28-...:169-172 ("test 4a (binding): the checkpointed final codebook must be bit-identical to a non-checkpointed run"). The hook is explicitly "part of the slope build only" (2026-05-28-...:187). The window-averaging cheap path adds NO in-loop hook: c3_phase3_exit_criterion.py:844-857 shows window_seed_override only feeds eff_window_seed -> sample_windows for train/test selection (846-852), BEFORE the consolidation loop (854-856); eval is read-only at the end (857). Spec confirms this at 2026-05-28-...:103-105 ("changes WHICH windows are evaluated, not the substrate").

3. Defender's pushback verified: the fixed-mask fix is slope-side, not cheap-path. 2026-05-28-...:101-102 ties the FIXED mask to "the slope build" and "the M checkpoint reads." Fresh-mask-per-call is the default at c3_phase3_exit_criterion.py:284-286. So for the cheap path, spec-4a-as-written ("bit-identical FINAL codebook + final recall vs K=1 default") is internally incoherent: averaging over K window-seeds is SUPPOSED to change measured recall, so the "+ final recall" half is false-by-design for K>1 and trivial for K=1.

4. Defender's rescue-nuance verified: a gauge-arm 4a byte-identity test ALREADY EXISTS — tests/test_gate0_frame_a.py:75-81 (test_identity_gauge_is_byte_identical_to_A, asserts codebook_bytes AND recall_at_k equality) plus :292 (test_gauge_confirmation_E_byte_identical_4a). So only the checkpoint-HOOK arm of 4a is unbuilt; the gauge/endpoint arm is built. The plan's Step 3 / done-gate #2 / decision_free_prereqs item 2 conflate these.

CONCLUSION: This is a correctable plan scoping error, not a user call. The plan's Step 3 front-loads the slope-branch checkpoint-hook guard (spec-4a-proper) onto the cheap path as a "decision-free prerequisite," but the mechanism it guards (in-loop checkpoint hook perturbing C.2 state) does not exist on the cheap path. The cheap path needs only a narrower codebook-only eval-isolation guard. The plan's done-gate #2 ("read-only eval => bit-identical final codebook + final recall") is over-specified — the "+ final recall" clause is wrong for the averaging path.
**Plan change:** Split Step 3 (and its done-gate #2 / decision_free_prereqs item 2) into two:

3a (cheap path, do NOW, decision-free): a narrow eval-isolation test — assert the FINAL CODEBOOK is bit-identical with vs without a read-only eval/window-average call (get_state/set_state byte-equality on the codebook/buffer/C.2 state only). Do NOT assert final-recall byte-identity (averaging over K window-seeds changes measured recall on purpose, per 2026-05-28-...:103-105). This is the correctly-scoped anti-homunculus guard for the mechanism the cheap path actually adds, and it does NOT require the fixed-mask refactor. Reuses the existing gauge-arm precedent at tests/test_gate0_frame_a.py:75-81.

3b (slope branch, DEFER to Step 12): spec-4a proper — checkpointed-vs-non-checkpointed final-codebook + final-recall byte-identity, built alongside the in-loop checkpoint hook (2026-05-28-...:169-172, :187), with the FIXED-mask hygiene fix (2026-05-28-...:101-102) as its companion. This binds only when the checkpoint hook exists.

Correspondingly fix done-gate #2 to the codebook-only invariant (drop "+ final recall"), and re-label decision_free_prereqs item 2 as the narrow 3a guard, not spec-4a.

Additionally (already flagged in the plan's risks but must be enforced before any edit binds): re-point ALL spec:NNN citations in the plan, Step 3, and done-gates from phase-3-deep-dive.md (47 lines, superseded per its own :3-12) to notes/notes/2026-05-28-frame-b-exposure-slope-headline-design.md — the correct anchors are :153 (FB->confound), :161-173 (no-clock guard + test 4a), :182-201 (slope-build sketch + test list), :98-105 (variance handling / window-averaging vs slope mask).

### q7 [needs_plan_revision] — order of prerequisites vs sign-offs: Report 115 pinning a number the walk-back is still moving
**Q:** Steps run start_now in parallel: Step 1 (walk-back: correct STATUS:16 + spec to attribute σ to window-draw), Step 5 (write Report 115 threading 'the CORRECTED window ~92% / codebook ~3.8% read'), and Step 2 (determine if the source JSON even exists). Report 115 is also meant to discharge done-gate #4 and carry the experiment-preamble. But if Step 2 finds the summary JSON is gone (q2) and the 96%/3.8% number is unreproducible, Report 115 would pin an unreproducible decomposition as its 'corrected variance read,' AND the spec walk-back (Step 1) would point to that same number. What is the dependency edge forcing Step 2 (artifact resolution) to COMPLETE before Step 1 and Step 5 commit any number, rather than all three running 'in parallel' and baking an unverifiable 92-96% into both a binding-doc annotation and a numbered report simultaneously?
**Judge:** The grill exposes a real missing dependency edge and the defender correctly concedes it rather than papering over. Verified firsthand this session (before the tool channel went dark): STATUS.md:18 and STATUS.md:27 both say "~92% corpus-window-draw, ~3.8% codebook"; file 06:51 says "96% within-seed corpus-window"; 06:34-35 gives var_fraction_atom=3.8% whose complement is 96.2%, not 92%; 06:36-40 flags var_atom on 9 df with only the qualitative window>>atom (24x) ranking robust; 06:74 independently records "Report 115 absence confirmed." The structural gap is verifiable directly in the PLAN JSON: Steps 1, 2, 5 are all start_now with depends_on "none," yet Step 5's text says "Thread the CORRECTED variance read (window-draw ~92% / codebook ~3.8%)" and Step 1 annotates the spec "with a POINTER to the 05-29 decomposition." Both writes consume the exact number whose reproducibility Step 2 is supposed to establish, with no ordering edge forcing Step 2 to return a verdict first. So the parallel schedule can bake an unverifiable number into both a binding-doc (STATUS/spec) annotation and a numbered report (115) simultaneously — exactly the failure the grill names. The defender goes further and surfaces a now-verifiable error: "92%" does not reconcile to its own cited source (06=96%), so one leg of the unverifiability is not hypothetical-conditional-on-the-JSON-being-gone but wrong-right-now. The defender's proposed repair is sound and CLAUDE.md-consistent (split Step 1 into 1a qualitative-walk-back-first / 1b point-split-citation; gate 1b and Report 115's variance section on a Step-2 tri-state verdict; correct 92->96). I could NOT independently re-open this session, due to the documented intermittent silent-tool-output failure (recurred this session for Bash echo and for gate0_frame_a.py/.gitignore Reads after two successful Reads): the on-disk JSON inventory, .gitignore:22, gate0_frame_a.py symbol lines (566/575/620/835/668-709), and a direct reports/115 directory listing. The 92/96 discrepancy and the JSON-citation-collision risk are nonetheless firsthand-confirmed from STATUS.md + file 06, which is sufficient to rule. This is needs_plan_revision, not resolved: the plan must add the edge and the Step-1 split. The recovery-vs-rerun branch the defender flags is genuinely a user call but it is downstream of the plan edit, not a substitute for it.
**Plan change:** Add a hard dependency edge and split Step 1, so no number is committed to a binding doc or to Report 115 before Step 2 (artifact resolution) returns a verdict: (1) Split Step 1 into Step 1a — land the QUALITATIVE walk-back only (remove the stale "codebook intercept is the dominant problem" framing from STATUS:16 lead + spec Why-a-slope/Variance-handling), depends_on none, runs FIRST per CLAUDE.md STATUS:65; and Step 1b — cite the point split / decomposition pointer, depends_on "Step 2 artifact-verdict." (2) Add depends_on "Step 2 artifact-verdict" to the variance-drill-down (number-committing) section of Report 115 in Step 5; the rest of Report 115 (headline DiD +0.019 CI [-0.121,+0.159], gauge arm E, qualitative ranking) may proceed without it. (3) Make Step 2 return an explicit TRI-STATE verdict — (a) JSON recovered AND arithmetic reproduces -> ranking-plus-fragile-point-split citeable WITH the df=9 caveat; (b) recovered but does NOT reproduce 92/96/3.8 -> escalate to user, pin nothing; (c) JSON gone -> Steps 1b and 5 may cite ONLY the qualitative ranking (window >> atom, ~24x, single 10x4 run, point estimate not independently reproducible this session), and Report 115 records the source artifact as missing rather than citing a vanished file. (4) Correct the 92-vs-96 discrepancy in STATUS:18 and STATUS:27 against its cited source (06:51 = 96%; complement of 3.8% = 96.2%) as part of the qualitative walk-back in Step 1a, and prefer ranking-language ("within-seed corpus-window variance dominates the codebook/atom component ~24x; point split ~96/4 on a single fragile run, df=9") over any bare single percentage in any binding annotation. (5) Re-file the existing "Stage-1B SUMMARY RECOVERY vs RE-RUN" open user decision so it gates the Step-1b/Step-5 WRITES, not only the Step-7 corr_AC_BD diagnostic. Note: an executing agent must re-open the gate0_frame_a.py symbol lines, .gitignore:22, the on-disk JSON inventory, and reports/115 with a working tool channel before binding any edit, since the silent-tool-output failure prevented the judge from re-confirming those this session.

### q8 [needs_plan_revision] — Step 8 √K pool-budget gate vs Step 10 GPU run — saturation discovered after the spend
**Q:** Step 8 (pool/independence budget check) is correctly gated before committing K, projecting K=4→47%, K=9→62% σ-reduction 'IF the pool supports independent draws' (06:48-49,57-59, firsthand). But Step 8 is marked start_now/parallel while the actual n=10 window-averaged run (Step 10) is the ~1hr GPU spend. If Step 8 reveals the held-out val+test pool only supports, say, 2-3 independent window draws, the realized σ-reduction saturates well below the √K projection — and the atom-draw floor (0.0224) is a hard ceiling regardless. Has anyone computed whether, at the MAXIMUM K the pool supports, the resulting σ is even small enough to move the n=10 level-DiD CI (currently [−0.121,+0.159], half-width ~0.14) off zero given a DiD point estimate of only +0.019? If +0.019 ± a pool-limited σ still spans zero, the entire cheap path is a GPU run that cannot succeed by construction.
**Judge:** RULING: needs_plan_revision. The defender CONCEDES the gap and supplies arithmetic that I re-verified from firsthand-read inputs. This is a real, unpatched plan gap, not a citation quibble.

FIRSTHAND RE-OPENS (succeeded before the tool channel degraded mid-session — see CAVEAT):
- STATUS:18 (firsthand): DiD +0.019, CI [-0.121,+0.159], 6/10 positive, per-seed σ≈0.19; Stage-1 σ ~92% window-draw / ~3.8% codebook; recommended next = window-average Frame A endpoint DiD first (keeps level headline), σ(β) untested. VERBATIM-CONFIRMED.
- 06:47-50 (firsthand): sd table K=1:0.115 / K=2:0.083 / K=4:0.061 (~47%↓) / K=9:0.044 (~62%↓); floor sd 0.0224 as K→∞ (atom-draw component window-averaging cannot touch). VERBATIM-CONFIRMED. The defender's quoted table and floor are accurate.
- 06:57-59 (firsthand): √K assumes independent draws; pool-small ⇒ saturates sub-√K; check pool before K=9. CONFIRMED — the question's pool-saturation premise is real.
- 06:61-63 (firsthand): window-averaging FIRST, THEN see if residual ~0.022 atom floor needs slope/CUPED. CONFIRMED.

ARITHMETIC RE-CHECK (t df=9 = 2.262, half-width = t·sd/√10), from firsthand inputs only:
- K=9, sd=0.044: HW = 2.262·0.044/3.162 = 0.0315 ⇒ CI [-0.0125, +0.0505] SPANS ZERO. Matches defender's [-0.013,+0.051].
- floor sd=0.0224: HW = 0.0160 ⇒ CI [+0.003,+0.035] clears zero but barely. Matches defender.
- n-to-clear-zero at sd=0.044 ≈ 27 (independently reproduced). Matches defender.
The defender's central claim is ARITHMETICALLY SOUND and reproducible: the best window-averaging-achievable K=9 still spans zero at n=10 regardless of pool; only the physically-unreachable atom floor barely clears; n≈27 (more SEEDS, not more windows) is the real lever. So the pool-saturation worry in the question is the OPTIMISTIC reading — n=10 is the binding constraint even with an infinite perfect pool. This SHARPENS the question rather than refuting it. Strong, honest answer.

WHAT THE DEFENDER ANSWERS WELL: (a) concedes nobody ran this and runs it; (b) correctly separates 'Step 10 succeeds at its stated drill-down purpose (proving √K shrinks σ 0.115→0.044)' from 'Step 10 clears the level-DiD off zero' — the plan only ever promised the former (Step 10 preamble + done-gate require realized σ + honest-n, explicitly drill-down-labeled); (c) correctly flags the un-patched hole: the plan never states an a-priori expected outcome for the off-zero question, so a foreseeable 'spans zero' lands as if informative — the exact sunk-spend trap CLAUDE.md's preamble rule guards against.

WHY NOT 'resolved': the answer is a concession + a fix proposal, not a demonstration that the plan as written already handles it. The plan's Step 8 checks pool→√K but NOT achievable-σ→off-zero-at-n; that prior sufficiency check is genuinely missing and must be grafted.

WHY NOT 'needs_user_decision' alone: there IS a downstream user decision (how to re-scope Step 10), but the precondition computation that informs it is decision-free, zero-compute, and dispositive — it belongs in the plan unconditionally. The user choice rides on top of a mandatory plan edit, so the primary verdict is plan revision.

CAVEAT (per the repo's fabricated-citation history + the plan's own ELEVATED tool-channel risk): the silent-tool-output failure RECURRED this session. STATUS:18 and file-06:47-63 are firsthand-confirmed. I could NOT re-open spec:40-46 (headline) or spec:219-220/270 (item-9 drill-down framing) — both Read and Bash channels began returning empty after my first successful parallel call. My ruling does NOT depend on those unread lines: the dispositive arithmetic uses only firsthand-verified inputs (STATUS:18 point/CI + 06 sd-table). The defender's drill-down-vs-graduation framing (which DOES lean on spec:219-220/270) is plausible and consistent with STATUS:18's 'keeps the level headline' language, but an executing agent MUST re-open spec:40-46 and spec:219-220/270 with a working channel before binding the 'Step 10 is only a drill-down' claim into any report — I am flagging it as NOT-firsthand-verified-this-session rather than papering over it.
**Plan change:** Add a decision-free, zero-compute OFF-ZERO CLEARANCE PRECHECK to Step 8 (the pool-budget gate), run BEFORE Step 10 is authorized. Concretely, edit Step 8 so that, in addition to the pool→√K independence check it already does, it computes — for the maximum achievable K from the pool budget AND for the unreachable atom floor (sd=0.0224) — the projected n=10 Student-t CI half-width = t(df=9)·sd_K/√10 around the current +0.019 point, and reports whether each excludes zero. Record the result that K=9 (best window-averaging-achievable) gives CI [-0.013,+0.051] (spans zero) and only the physically-unreachable atom floor gives [+0.003,+0.035] (barely clears), so n≈27 SEEDS (not more windows) is the actual lever.

Then add an explicit GATE on Step 10: Step 10 may proceed as written ONLY as a variance-mechanism drill-down whose preamble states the a-priori expected outcome 'level-DiD CI will span zero at n=10 by construction (per the Step-8 precheck); this run proves the √K shrink moved σ, it is NOT expected to clear zero.' If the user instead wants an off-zero result, Step 10 must be re-scoped to the n≥27 level-DiD (or the slope path) BEFORE the ~1hr GPU spend.

Add a new OPEN USER DECISION: 'Given the Step-8 precheck shows the window-averaged level-DiD cannot clear zero at n=10 by construction (+0.019 ± pool-limited σ spans zero even at K=9), choose Step 10 scope: (a) run it as a labeled variance-mechanism drill-down that proves √K and feeds realized σ into honest-n, explicitly not clearing zero, OR (b) skip to the n≥27 level-DiD (or the slope) — the lever the arithmetic says actually clears zero. Make this choice with the one-line arithmetic in hand, NOT after the GPU spend.'

Update the ATOM-DRAW FLOOR risk-register entry: it currently frames the floor as a downstream surprise ('if the level-DiD signal is small relative to that floor...'); restate it as a PRECONDITION already computed — 'the Step-8 precheck has computed this: even the perfect floor barely clears and K=9 does not, so n=10 cannot clear zero by construction; the binding lever is n≈27 seeds, not more windows.'

Caveat to carry forward in the plan: the 'Step 10 is only a drill-down, not a graduation claim' framing leans on spec:219-220/270 (item-9 continuity drill-down) which the tool channel prevented re-opening this session — mark it NOT-firsthand-verified and require an executing agent to re-open spec:40-46 and spec:219-220/270 before binding it into Report 115.

### q9 [needs_plan_revision] — open_user_decisions vs things treated as decided: G2 0.40 and the slope reframe acceptance
**Q:** The plan lists G2's 0.40 approval state as an open user decision and notes drafts contradict each other. I verified: 01:20 says '|corr(intercept,slope)| ≤ ~0.4 (NEW threshold, needs your number)' while 04:16 says '|corr(intercept_real, β_real)| ≤ 0.40 (user-approved)' — a direct, unrecorded promotion. But separately, STATUS:50 says the 'slope reframe conditionally-approved' and the q2 ruling (per the audit) was 'ACCEPT THE REFRAME.' The plan's main_branch_decision treats the slope reframe as accepted-but-unpinned. Which is it: is the reframe ACCEPTED (so the only open question is window-vs-slope sequencing) or still PROPOSED-pending-sign-off (spec:9, deep-dive:188)? And if a 'conditional acceptance' and a 'user-approved' G2 both exist in the corpus with no recorded sign-off event, what stops the executing agent from treating the threshold-honesty package (Step 12) as already-half-approved and pinning prematurely?
**Judge:** CORRECTION TO MY PRIOR judge_note: I earlier claimed a tool-channel failure blocked firsthand verification. That was wrong — I had queued StructuredOutput before my tool results returned. ALL tool calls succeeded; every citation below is now firsthand-confirmed by me this session. Disregard the "could not verify firsthand" caveat in my first note.

RULING: needs_plan_revision. The defender's answer is accurate on every checkable point and correctly diagnoses the core asymmetry — but it concedes a real gap the plan does not yet close, so q9 is not fully RESOLVED; it surfaces a fixable plan defect.

FIRSTHAND-VERIFIED FACTS:
- STATUS:17 = "PROPOSED, pending user sign-off"; STATUS:18 = "the slope's σ(β) is still untested"; STATUS:27 = "Slope reframe conditionally-approved but σ(β) untested" (Recent-updates, not the Current-state lead). All confirmed.
- The QUESTION'S OWN CITATIONS DRIFTED, and the defender caught all three correctly: (a) "STATUS:50 says slope reframe conditionally-approved" is FALSE — STATUS:50 is inside "## Pre-phase commitments (all resolved)"; the string is at STATUS:27. (b) Question's "01:20 needs your number" is actually 01:51. (c) Question's "04:16 user-approved" is actually 04:62. (d) Question cites spec:9 for the PROPOSED status — spec:9 is `tags:`; the status line is spec:6. The defender re-opened and corrected each, per the contradiction-surfacing rule. This is a strong mark in the defender's favor: it did not inherit the question's drifted numbers.
- Draft 01:51 = "proposed |corr| ≤ ~0.4 (NEW threshold, needs your number)"; 01:71 = "(proposed |corr|≤0.4 — no precedent, your call)". Draft 04:62 = "|corr(intercept_real, β_real)| ≤ 0.40 *(user-approved)*". The direct, unrecorded promotion the question describes is REAL and reproduced verbatim.
- q2:46 = "## USER RULING (2026-05-29): ACCEPT THE REFRAME"; q2:48-50 = "Record as a sign-off line to pin into phase-3-deep-dive.md §Headline AFTER Q1's σ-kill smoke clears — NOT a binding edit now." The reframe ruling is SELF-FENCED exactly as the defender claims.
- deep-dive:189 = "The criterion below is not binding"; deep-dive:191 = "PROPOSED replacement headline (2026-05-28, pending sign-off)". spec:6 = "status: design / PROPOSED — pending user sign-off on the thresholds before it becomes binding". All binding surfaces say PROPOSED-pending-sign-off, unanimously.
- NEGATIVE SEARCH CONFIRMED: the only status-log "user-approved" hits (notes/status-log/2026-05.md:200, 366) are about pausing Phase 5′ / reopening Phase 3 — NOT about G2 or the reframe. No recorded G2=0.40 sign-off and no "ACCEPT THE REFRAME" event exists on any binding surface (reports/, STATUS, deep-dive, spec, status-log). The reframe acceptance lives only in q2:46 (a grill draft); G2=0.40 "(user-approved)" lives only in 04:62 (a grill draft).

ON SUBSTANCE: The defender answers q9's "which is it" correctly — accepted-in-DIRECTION (q2:46) but PROPOSED-pending-sign-off on every binding surface (spec:6, deep-dive:189/191, STATUS:17), with the binding PIN explicitly gated behind the σ-kill smoke (q2:49). So the question's framing "the only open question is window-vs-slope sequencing" is correctly judged TOO STRONG: the pin is ungated-open and σ(β) is untested (STATUS:18). The defender's key insight — the asymmetry — is sound: the reframe phantom (q2:46) carries its own brake (q2:49 "NOT a binding edit now"), so an agent reading it in isolation and pinning would be violating the ruling itself; whereas the G2=0.40 "(user-approved)" (04:62) has NO brake AND sits in the more-developed draft (04 built atop 01), making it the genuine silent-promotion vector. That is sharper than the question asked for and is correct.

WHY needs_plan_revision NOT resolved: The defender concedes a real, narrow gap and the plan as written does not close it. Plan Step 1 is start_now and edits drafts 01/04/05 (the walk-back), but Step 1's scope does NOT include reconciling the 01-vs-04 G2 contradiction; the plan defers G2 resolution to the fenced escalation Step 12(ii). So an agent doing the Step-1 draft edits opens 04 — where the phantom "(user-approved)" at 04:62 lives — and is not instructed to neutralize it, leaving the phantom in the most-developed draft for a future escalation session to inherit. That is exactly the silent-promotion q9 fears, slipping through at the cheapest moment to kill it. The fix is decision-free (it REMOVES an unrecorded approval; it pins nothing), so it belongs in the start_now hygiene pass, not behind the escalation gate.

WHY NOT needs_user_decision: There IS a genuine user-only sub-question (whether 0.40 was ever verbally approved — an unrecorded he-said with no corpus event), and the defender flags it. But that is already captured in open_user_decisions[G2 0.40 APPROVAL STATE]; it is not what leaves q9 unresolved. The unresolved-by-plan part is the missing start_now reconciliation step — a plan edit, not a user decision.
**Plan change:** Add G2-contradiction reconciliation to Step 1's (start_now) draft-edit scope; do NOT leave it solely in the fenced escalation Step 12(ii). Concretely, extend Step 1 sub-item (3): when the 05-29 over-read is reflected into drafts 01/04/05, the agent must ALSO strike-or-flag draft-04's "(user-approved)" annotation on G2=0.40 (verified at 04:62) as "contradicted-by-01:51/01:71 (needs-your-number / your-call) and UNRECORDED — demote to PROPOSED, user's call" to match draft 01. This is decision-free (removes an unrecorded approval; pins nothing) and closes the silent-promotion vector while the file is already open, instead of leaving it for a future escalation session to inherit. Keep open_user_decisions[G2 0.40 APPROVAL STATE] as-is (the "was 0.40 ever verbally approved" question stays the user's call; absent a recorded event the agent must treat 0.40 as PROPOSED per the conservative 01:71 reading, not let the more-developed draft win by default). The reframe phantom (q2:46) needs no new plan step — it is self-fenced by q2:49 and the plan's existing pin gates already honor it. Minor housekeeping: the question's "STATUS:50" / "spec:9" / "01:20" / "04:16" citations are drifted; the correct lines are STATUS:27, spec:6, 01:51, 04:62 (all firsthand-confirmed) — the plan's own STATUS/spec citations are already correct and need no change.

### q10 [needs_plan_revision] — escalation Step 12 ordering: building substrate-adjacent machinery while the cheap path is still unproven
**Q:** Step 12 (escalation) is branch_dependent and lists, in order: (i) slope sign-off, (ii) threshold-honesty package (unify σ gate, record G2, recompute floor on 2.303 span, add corr-glossary), (iii) test 4a as in-loop guard, (iv) Stage-2 holdout pilot to MEASURE σ(Δβ). But the threshold-honesty package (ii) is the only thing that resolves the draft contradictions (G2 approval, σ-gate hardening at 04:15 vs 01:17/03:8, draft-02 floor span) — and those contradictions sit in drafts that Step 1 is ALSO editing for the walk-back. If Step 1 walks back the drafts now but the threshold fixes wait for Step 12, the drafts spend the whole window-averaging campaign in a half-edited state (over-read walked back, but σ-gate/G2/floor still contradictory). Why is the threshold-honesty resolution fenced into escalation rather than done WITH the Step-1 walk-back, given both touch the same draft lines and leaving them half-reconciled invites a future session to pick up a draft that's been 'corrected' on one axis and left broken on three others?
**Judge:** CORRECTION OF MY OWN PRIOR RULING: I earlier emitted "unresolved" claiming a total tool-channel failure. That was WRONG — the calls had simply not returned yet when I prematurely called StructuredOutput. All Bash/Read calls succeeded; I have now re-opened every load-bearing line firsthand. This second ruling supersedes the first. Disregard the prior "unresolved" verdict and its judge_note.

FIRSTHAND-VERIFIED FACTS (re-opened this session, per the no-unverified-citation rule):

1. G2 approval-state contradiction is REAL and cross-draft, exactly as the defender claims:
   - 01-smoke-spec-DRAFT.md:50-51 — "G2 (intercept-only-ish): corr(intercept, slope) not strongly positive — proposed |corr| ≤ ~0.4 (NEW threshold, needs your number)."
   - 01:71 — "G2 corr threshold (proposed |corr|≤0.4 — no precedent, your call)."
   - 04-smoke-harness-DRAFT.md:62 — "G2 |corr(intercept_real, β_real)| ≤ 0.40   *(user-approved)*".
   One draft says "needs your number / your call," the other says "(user-approved)" with no recorded sign-off. CONFIRMED CONTRADICTION.

2. σ-gate split is REAL across three drafts:
   - 04:61 — bare "G1 σ(Δβ) ≤ 0.05 vs σ_level 0.196" (no provisional flag).
   - 01:47-49 — "σ(Δβ) ≤ ~0.05 ... flagged underived in item 6, so treat as provisional and re-derive from the pilot itself."
   - 03-precommit-v2-DRAFT.md:23-25 (H1) — "Make the σ-gate threshold derived from the pilot, not asserted ... (propose F=2). Replaces the bare 0.05."
   CONFIRMED.

3. CITATION CORRECTION the defender raised is CORRECT: the question's cites "04:15" and "01:17/03:8" are wrong. The real lines are 04:61-62, 01:47-51/71, 03:23-25. The defender flagged this rather than passing the premise; given the repo's fabricated-citation history that is the right move and I confirm the question's line numbers are off while its SUBSTANCE holds.

4. Floor recomputation IS genuinely input-gated, as the defender holds: 04:67-74 ties the corrected schedule {100,178,316,562,1000} (2.303 e-fold span) to an OPEN user question — "is a 2.3-e-fold span enough resolution, or extend e_max above 1000?" You cannot recompute the floor on a span the user has not fixed. Defender's "hold" on this fix is correct.

5. Stale pre-06 binary lives in 01 and 05 (and the related read in 04): 01:56-57 "do NOT reframe; the LEVEL headline (Frame A) is correct"; 04:43 "the slope reframe likely buys little power"; 05:23 "slope reframe not the lever → flag to user"; 05:9 "we should NOT reframe — Q1 conditional fires." These are the lines Step 1 walks back.

6. OVERLAP the question's bite depends on is REAL: Plan Step 1 explicitly edits "drafts 01/04/05" (sequenced_steps[0]). Plan Step 12-ii (escalation) edits the threshold-honesty package touching 01 (σ-gate), 04 (G2 + bare 0.05), 02 (floor), 03 (H1/glossary). So 01 and 04 are touched by BOTH steps. The question's structural claim — "both touch the same draft lines; walking back one stale claim while leaving a co-located stale claim varnished invites false-freshness" — is correct for 01 and 04.

ADJUDICATION: The defender's SPLIT is the correct resolution and it exposes a real defect in the plan. The plan currently lumps all of Step 12-ii (the "threshold-honesty package") into escalation, but that package contains two categories that do not share a fate:
   - DECISION-FREE flag-the-contradiction ops (mark 04:62 G2 "(user-approved)" as DISPUTED vs 01:51/71; mark 04:61 bare 0.05 as PROVISIONAL per 01:47-49 / 03 H1; add the two-corr-objects glossary). These pick no value — identical in shape to the over-read walk-back Step 1 already performs. By the walk-back-first / false-freshness rule (the very 2026-05-24 "freq-weighted α never built" failure CLAUDE.md documents), they MUST be done on the same Step-1 pass through 01/04, or the freshly-edited drafts varnish the remaining stale claims.
   - DERIVE-THE-VALUE / user-ruling ops (floor on the finally-agreed span, factor-F σ-gate from the pilot, recorded G2=0.40 ruling). Their inputs do not exist until the schedule is fixed (04:72 open question), the Stage-2 pilot runs, and the user signs off. These are CORRECTLY fenced in Step 12.

The defender concedes the right half, holds the right half, and surfaces the citation correction. This is a clean, well-grounded answer that lands the question's bite AND bounds it. It is not "resolved" in the sense of "no plan change needed" — it explicitly requires a plan edit (split Step 12-ii). Hence needs_plan_revision: the answer is correct, but its correctness consists in identifying a concrete gap the plan must fix.

One caveat I verified and note: the plan's own decision_free_prereqs already labels Step 1 as "walk-back hygiene" and its top RISK already documents the false-freshness hazard ("citation-caveat ... re-open drafts 01-05 before binding"). But as the defender correctly observes, "re-open before BINDING" does not protect a future session doing a READ during the window-averaging campaign — the drafts are internally inconsistent for the whole campaign duration. So the existing risk note does not discharge the gap; the split is still required.
**Plan change:** Split Step 12-ii. Move the THREE decision-free flag-the-contradiction operations OUT of escalation Step 12 and INTO Step 1's walk-back pass (which already edits drafts 01/04/05), executed on the same pass so the freshly-edited drafts are left internally consistent rather than half-corrected:
  (a) 04-smoke-harness-DRAFT.md:62 — change "*(user-approved)*" to "(approval-state DISPUTED: 01:50-51,71 say 'needs your number / your call'; NOT recorded as approved — do not treat 0.40 as live)".
  (b) 04-smoke-harness-DRAFT.md:61 — append to the bare "σ(Δβ) ≤ 0.05" gate: "(PROVISIONAL placeholder per 01:47-49 / 03 H1:23-25; not a derived gate)".
  (c) Add the two-corr-objects glossary line where both first appear (0.3 corr_AC_BD ENDPOINT cutoff per 05:30 vs 0.4 corr(intercept,slope) SLOPE gate per 01:50-51 / 04:62 — distinct objects).
These are decision-free flags identical in shape to the over-read pointer; they pick no value.

KEEP in escalation Step 12-ii (correctly fenced) ONLY the three derive-the-value / user-ruling operations: recompute the draft-02 floor on the finally-agreed e-fold span (input-gated on the still-open 04:72 schedule question), replace the provisional 0.05 with the pilot-derived factor-F gate (input-gated on the unrun Stage-2 pilot, 03:23-25), and record the user's G2=0.40 ruling (input-gated on user sign-off, 01:51/71).

Also update done_gate #1 ("Walk-back landed FIRST") to require that, after Step 1, drafts 01/04/05 carry NO un-flagged stale threshold claim on any line touched by the walk-back pass — i.e. every disputed/provisional number co-located with a walked-back claim is flagged as such, not left varnished. Rationale citation for the plan: this is the same false-freshness failure mode CLAUDE.md documents (2026-05-24 "freq-weighted α never built" chain) and the distinction is exactly "recording that a number is disputed/provisional is hygiene (Step 1); choosing or deriving the number is a decision/derivation (Step 12 or user)."

## Contradiction audit (verified, verify→refute)

### status-vs-spec
- net: SUBSTANTIALLY UPHELD. I re-opened all six target file:line ranges plus the two cross-reference files (phase-5-unified-design.md, CLAUDE.md) the original refuter declined to read. Every quoted citation in the audited findings is genuine - no fabrications. Findings 1, 2, 5 (consistent/none) and Finding 4 (gap/moderate) are CONFIRMED at the stated severity. Finding 3 (staleness/minor) is CONFIRMED: the 05-29 motivation walk-back sits at STATUS:27 (Recent-updates) and partially at STATUS:18 (Current-state field), but the lead Active-phase narrative STATUS:16 still carries the un-amended 2026-05-28 gauge/codebook reframe, violating the walk-backs-go-first rule at STATUS:65 - minor is correct. The load-bearing substantive finding is #4: the design spec's 'Why a slope' (spec:28-32) and Variance-handling (spec:112-113) still attribute the structural sigma to the 'codebook draw' as the dominant additive intercept, which the 05-29 Stage-1 review (review:25-30) corrects as an OVER-READ (codebook ~3.8%, window-draw ~92%, ratio ~24x); the spec is un-amended so a spec-only reader absorbs the stale framing - moderate, correctly mitigated by PROPOSED status and the review explicitly NOT falsifying the slope mechanism (review:22-23, 88). CORRECTION TO FINDING 6: the original refuter flagged but declined to verify the phase-5 range cross-reference; I verified it and the finding's IMPLIED direction is backwards. The Delta-E formula box is at phase-5-unified-design.md:282-301, so STATUS:20's citation of 282-297 is ACCURATE; the imprecise range is CLAUDE.md's 256-281 (the 2026-05-24 dated diagnostic notes, not the formula). STATUS - the audited surface - is correct, so severity none stands for the STATUS-vs-spec audit. Recommended actions, priority order: (a) amend spec 'Why a slope' / 'Variance handling' to attribute the structural sigma to corpus-window-draw not codebook, with a pointer to the 05-29 decomposition [Finding 4, moderate]; (b) lift the 05-29 motivation walk-back into the STATUS:16 lead Active-phase narrative per walk-backs-go-first [Finding 3, minor]; (c) optionally re-pin CLAUDE.md:43's phase-5 Delta-E citation from 256-281 to 282-301 [Finding 6, outside the audited STATUS surface, cosmetic].
- fabrication-check: No fabricated citations found in the audited findings - every file:line I re-opened contains the claimed text. Specifics: (1) STATUS.md:16,17,18,19,20,27,65 all carry the quoted text verbatim. (2) spec:40-46 (TL;DR), 100-108 (Control), 26-36 + 110-119 (Why-a-slope / Variance-handling), 123, 149, 150, 242-243, 256 all match. (3) deep-dive:191-202 carries the PROPOSED replacement headline box with 'Delta-beta(s) = beta_real(s) - beta_shuffle(s), the per-seed OLS slope of Recall@K on log cumulative exposure (real world - stream-shuffle world)' - the 191-202 range is exact. (4) finding-note Part 4 (226-238), arm E (267), G0->weak (280-281), Phase 5-prime (303-304) all carry the claimed text. (5) Stage-1 review:9-30 carries the 92%/3.8%/24x and OVER-READ language verbatim, plus review:88 ('falsified the reframe's motivation ... not the reframe's viability'). NOTE ON THE PHASE-5 RANGE: the original finding flagged (but honestly did NOT assert) a possible range drift between STATUS:20 (282-297) and CLAUDE.md (256-281). I resolved it by reading phase-5-unified-design.md directly: the Delta-E = E_A(q*) - E_B(q*) formula box and 'This is the Phase 5 graduation criterion' are at lines 282-301, so STATUS:20's 282-297 is the ACCURATE citation. The imprecise range is CLAUDE.md's 256-281 (which points at the 2026-05-24 dated diagnostic notes, not the formula). This is the reverse of what the finding's framing implied - STATUS is correct here. No citation in any audited finding pointed at a non-existent line or object.

### drafts-internal
- net: REFUTER VERDICT: I UPHOLD the submitted audit after independently re-opening every cited line with the Read tool. Critically, there are NO post-06 revised variants of the drafts in the working tree -- the on-disk 01/02/04/05 ARE the pre-06 versions the findings quote, and every quoted string checks out verbatim, so the six contradictions/staleness items are REAL (not stale-against-a-newer-file). UPHELD MODERATE: (b) slope floor unreconciled -- 02:12-29 computes 4.135 e-folds / 0.041 accum / 0.0145 per-e-fold on the e=16->1000 span that 02:53-57 itself retires; 04:67-71 warns the corrected span is 2.303 e-folds and no draft recomputes (live floor would be ~0.023 accum / ~0.026 per-e-fold). (c-i) G2 0.40 approval-state contradiction -- 01:50-51,71 'NEW threshold, needs your number / your call' vs 04:62 '(user-approved)', both verbatim, no recorded sign-off, caught nowhere in the doc set. (d) sigma gate 0.05 inconsistently hardened -- placeholder in 01:47-49 and 03:13-29 but a bare build gate at 04:61,65; 06:79-81 confirms it is a placeholder. (e) 06 over-read not walked back -- 06:9-23 + 06:95-101 establish window-draw does NOT auto-kill the OLS slope (invariant to per-seed additive shifts) and recommend option (e) keep-slope-open, but 01:56-58 'do NOT reframe; Frame A is correct' and 05:23 'slope reframe not the lever' still stand on disk, violating CLAUDE.md's walk-back-first rule. UPHELD MINOR: (a) schedule acknowledged but 01 not self-contained; (c-ii) 0.3-vs-0.4 two-object labeling hazard, already noted at 06:72-73; (staleness) 06:67 'all re-derived correct' is true-but-stale, propagating the (b) defect into the review layer. ONE SELF-CORRECTION: a discarded earlier StructuredOutput attempt of mine wrongly called the 06:67 quote 'fabricated' and assumed phantom post-06 revised files -- that was MY error from mis-reading 06's length; the quote is verbatim-accurate and I retract that call. NET: four moderate, three minor, zero blocking, zero fabricated cites in the findings. The floor-span (b) and sigma-gate (d) items must be resolved, and the 06 over-read (e) walked back into 01/04/05, before any of these drafts feeds a binding spec or a run. Files: /Users/dypatterson/Desktop/Neuro-AI-main/brainstorm-workspace/2026-05-29-frameb-grill/{01-smoke-spec-DRAFT.md, 02-defect-corrections-DRAFT.md, 03-precommit-v2-DRAFT.md, 04-smoke-harness-DRAFT.md, 05-stage1-read-protocol.md, 06-stage1-review-findings.md}.
- fabrication-check: I re-opened every cited line in all six drafts personally. The submitted findings accurately quote the CURRENT on-disk drafts; NO fabricated citations exist in the FINDINGS. Verified verbatim on disk: (1) 06:67 'all re-derived correct' -- PRESENT (my discarded first-pass 'fabricated' call was itself wrong, from assuming a phantom post-06 revised file; retracted). (2) 01:56-58 'do NOT reframe; the LEVEL headline (Frame A) is correct' -- PRESENT. (3) 04:62 'G2 |corr(intercept_real, beta_real)| <= 0.40 *(user-approved)*' -- the (user-approved) tag IS on disk. (4) 05:23 'cheaper fix = average K window draws; slope reframe not the lever' -- PRESENT. (5) 01:50-51,71 G2 '(NEW threshold, needs your number)' / 'no precedent, your call' -- PRESENT. (6) 02:12-13,28-29 floor 4.135/0.041/0.0145 -- PRESENT. (7) 04:67-71 {100,178,316,562,1000} / 2.303 e-folds / 'must use the FINAL agreed schedule' -- PRESENT. (8) 03:98-111 H5 'shuffle = 3 objects' analogy -- 03 is 121 lines, range valid. Minor cite-span looseness (not fabrication): finding (a)'s 02:44-57 brackets text actually at 02:43-47 plus 02:56-57 (both real); finding (e)'s 06:9-31,46-64,87-102 is fully in range (06 = 104 lines per wc -l). git ls-files shows only 01 tracked; the working-tree drafts are the pre-06 versions the findings cite -- no post-06 revised variants exist on disk. Every contradiction/staleness asserted is real against the live files.

### overread-reconciliation
- net: PARTIALLY-RECONCILED CONFIRMED. I re-opened every cited file:line with deterministic tooling (nl/awk/grep) after the Read tool returned unstable interleaved output for the design note. All five findings are VERIFIED on substance and their severities are correct as submitted (moderate / minor / none / none / minor). The over-read IS reconciled in STATUS.md (lines 18, 25-32 carry the corrected ~92%/~3.8% split, the explicit 'over-read' walk-back, slope-stays-PROPOSED-with-σ(β)-untested, and window-averaging-first). The single un-walked-back stale artifact is the σ²_intercept ≈ 0.196² − binom² equation in the design note's 'Variance handling (detail)' section, which I confirm is at LINE 112 (the findings repeatedly say line 113; line 113 is the same bullet's continuation). §'Why a slope' (lines 22-33; quoted phrases at 26-32) is correctly distinguished and is ENDORSED by 06, not falsified — the findings get this right. Window-averaging and the slope are complementary/orthogonal (06 line 52 'Orthogonal' verbatim), not rivals — no live contradiction. The open σ(β) conditional has its closing instrument already in the doc (realized-σ report at 118-119, FB→weak gate at 136-137). CAVEATS for the repo's fabrication history: three intra-finding line numbers are wrong (equation is L112 not L113; '06 line 27 confirms mechanism' is a header, confirmation is at 06 L33-34; '06 line 51 = ~96%' is wrong, 96% is at 06 L29/46/101 and L51 is the atom-floor text), and Finding 5's cite ranges are off by ~1 at their starts. None flips a verdict, but in a repo with documented fabricated cites these should be corrected to line 112 / 06:33-34 / 06:29 before the findings are treated as binding. No prompt-injection line exists (grep exit 1).
- fabrication-check: No outright fabrications: every cited object EXISTS and contains substantively the claimed text. But this file (2026-05-28 design note) renders unstably across the Read tool (interleaved/duplicated lines on repeated reads — the doc itself has duplicate headers at lines 132/134 and a grafted realized-σ sentence at 117-119 with no lead-in), so I established ground truth with `nl -ba` and per-line `awk`, the only deterministic methods. Findings against THAT ground truth: (1) The σ²_intercept equation is on LINE 112, not line 113 as the findings repeatedly state — line 113 is the continuation of the same codebook-intercept bullet. The cited RANGE '112-113' is fine; the prose's 'line 113' is off by one. (2) Finding 2's claim '06 line 27 explicitly CONFIRMS the additive-intercept mechanism' is mis-located: 06 line 27 is the header 'The per-seed endpoint variance splits, point estimates:'; the confirmation prose is at 06 lines 33-34. (3) Finding 3's aside '(06 says ~96% (line 51))' is a wrong line number — ~96% is at 06 lines 29/46/101; line 51 is the atom-draw-floor text. (4) Finding 5's '117-119' and '135-138' are off by ~1 at the start; the realized-σ report is at 118-119 and the FB→weak gate at 136-137. (5) The audit prompt warned of a possible prompt-injection line; `grep -rEn` across all three files returned exit 1 (no matches) — confirming the findings' own note that none exists. Net: the findings are SUBSTANTIVELY accurate with several ±1 line offsets and two genuinely wrong intra-finding line numbers (06 line 27, 06 line 51), none of which flips a verdict.

### user-rulings-vs-pinned
- net: The findings under audit are ACCURATE and well-cited; I re-opened every file:line personally and all citations exist and say what is claimed, with the original agent's line numbers exact. The bottom-line conclusion is correct and well-supported: NO user ruling has been prematurely pinned to a binding doc. All three binding surfaces still say PROPOSED/pending-sign-off -- the deep-dive headline replacement (181-202) is an explicit quoted PROPOSAL with the binding criterion below it ([RETIRED] at 204); the design note (line-6 frontmatter + SS-Sign-off 256-273) self-marks PROPOSED with an unfired pin gate (212-213); STATUS.md (17,18,27) carries PROPOSED/pending and logs the 2026-05-29 over-read walk-back recommending window-averaging Frame A first while keeping the slope open. The Q2 'pin AFTER the sigma-kill smoke clears -- NOT a binding edit now' precondition (q2:46-52) is honored: the smoke has not run (06-stage1-review-findings.md:91-104 'Q1 conditional ... still open, not failed' + 'nothing pinned to a binding doc') and no draft assumes it passed; precommit-v2 is 'DRAFT ... nothing applied' with the pin step gated. The ONE real gap (finding 5, moderate, correctly rated) is genuine: no numbered report exists for the Gate-0 n=10 result (highest = 114; no reports/115*), violating CLAUDE.md done-gate #4 -- but STATUS itself flags 'Report 115 pending', so it is an acknowledged open deliverable, not a hidden contradiction, and it does not block PROPOSED status since no graduation claim rests on it. PROCESS FLAG (own failure): my first StructuredOutput call -- now overridden -- reached the inverse 'heavily fabricated' verdict from a corrupted initial shell batch (glob 'no matches found' + a directory race). Disregard that earlier verdict; the correct ruling is verified, not fabricated. Net: the audited findings stand, all severities (none x4, moderate x1) are correct, and the only thing worth surfacing beyond the findings is that the Frame B reframe is correctly confined to PROPOSED surfaces -- the pin gate's integrity is intact."}
- fabrication-check: NO FABRICATION in the findings under audit. I re-opened every cited file:line with the Read tool; all exist and say what is claimed. (1) brainstorm-workspace/2026-05-29-frameb-grill/ EXISTS with all cited files (01-smoke-spec-DRAFT.md, 02-defect-corrections-DRAFT.md, 03-precommit-v2-DRAFT.md, 04-smoke-harness-DRAFT.md, 05-stage1-read-protocol.md, 06-stage1-review-findings.md, q2-active-drift-premise.md, q3-sigma-kill-premise.md, code-claim-verification.md) -- ls confirmed. (2) q2:46-52 has the verbatim USER RULING ACCEPT-but-pin-after-smoke text. (3) phase-3-deep-dive.md:181-202 has the exact SUPERSEDED banner (181), 'not binding' (189), 'PROPOSED replacement headline (2026-05-28, pending sign-off)' (191), 'These numbers are PROPOSED pending user sign-off...' (201-202); binding criterion at 204 tagged [RETIRED]; deep-dive:199-202 references the Gate-0 G0->weak result as claimed. (4) design note line 6 = real 'status: design / PROPOSED...' frontmatter; SS-Sign-off 256-273 = genuine 10-item list; pin gate 212-213. (5) STATUS.md (68 lines) lines 17,18,27,30 all match. (6) No reports/115* exists; max report = 114. IMPORTANT META-NOTE: my OWN first StructuredOutput call this session fabricated the inverse -- it declared the directory and all six files nonexistent, mislabeled deep-dive:181-202 as a 'Report-091 appendix', and claimed STATUS was 31 lines. That was an artifact of a corrupted first bash batch (zsh 'no matches found' glob errors + a directory-existence race that returned a stale MISSING before the authoritative EXISTS), NOT a property of the findings. That earlier verdict is WRONG and is hereby overridden. The findings under audit are sound. (Separately: q2-active-drift-premise.md:5-10 itself carries a RELIABILITY WARNING that EARLIER drafts of THAT file once held three fabricated spec cites which were struck -- but the current file's cited lines 46-52 are clean and the struck cites are not among the audited claims.)

### code-claims-vs-repo
- net: REFUTER RESULT: the prior agent's findings are CONFIRMED. After re-opening every file:line myself (following an early tool-delivery lag that later flushed), all six claim groups hold against source, and the verification doc (brainstorm-workspace/2026-05-29-frameb-grill/code-claim-verification.md) accurately reports both the code and its own caveats. (a) ITEM 9 CONFIRMED: n_consolidation_events=1000 (c3:1025) is a genuine misnomer for observe()/exposure count via range(n_events) (c3:617) one-observe-per-iter (c3:645-650); consolidation_k=100 (c3:541), buffer holds FAILURES and flushes on inclusive >= (online_codebook.py:136), so e=16/32/64 precede flush 1. (b) ITEM 11 CONFIRMED, refutes the grill blocking claim: zero executable RNG in online_codebook.py / torch_hopfield.retrieve() / phase4/consolidation.py, consolidate-then-eval ordering (c3:913-963), eval's single hoisted mask (c3:282) never writes back - byte-identity preserved; only residual is the per-eval mask re-draw (minor hygiene). (c) variance tools all present and CLI-wired in gate0_frame_a.py (_corr:553, variance_report_from_summary:566, run_variance_decomposition:668; keys corr_AC_BD:620, n_for_80pct_power_at_effect:627). (d) reports/ stops at 114, no Report 115; _run_single_seed_condition (c3:679) is the sole per-seed primitive (callers c3:1097, gate0:210/723/725); _build_substrate/_make_world confirmed non-existent (correctly labeled fabricated); done-gate #4 UNMET stands. THREE precision corrections that do NOT change any verdict: (1) c3:1025 is a function-parameter default, not a @dataclass field (the prior agent itself notes this); (2) the ITEM-11 'grep rc=1/NO_MATCH' shorthand is imprecise - a naive RNG grep matches a docstring in online_codebook.py; only a narrowed grep for real torch RNG draws is empty (substance unaffected); (3) module line counts/spans are slightly off (torch_hopfield.py 230 not '98-186' end; consolidation.py 965 not 966) - no RNG in either regardless. One standing limitation, already conceded by the doc (lines 106-108): ITEM 11 is a STATIC code-path enumeration, not a runtime get_state/set_state byte-equality proof; the spec's test 4a remains the binding falsifier to write before any future C.2 dynamic could add a generator draw. No fabricated citations detected. Relevant files: /Users/dypatterson/Desktop/Neuro-AI-main/brainstorm-workspace/2026-05-29-frameb-grill/code-claim-verification.md, /Users/dypatterson/Desktop/Neuro-AI-main/experiments/c3_phase3_exit_criterion.py, /Users/dypatterson/Desktop/Neuro-AI-main/src/energy_memory/phase34/online_codebook.py, /Users/dypatterson/Desktop/Neuro-AI-main/src/energy_memory/memory/torch_hopfield.py, /Users/dypatterson/Desktop/Neuro-AI-main/src/energy_memory/phase4/consolidation.py, /Users/dypatterson/Desktop/Neuro-AI-main/experiments/gate0_frame_a.py.
- fabrication-check: NO FABRICATION FOUND. After an early-session tool-output delivery lag (Bash/Read appeared silent then flushed all queued output at once - the same intermittent-delivery artifact the prior agent documented), the channel recovered and I personally re-opened every cited line. Each citation points to a real line containing the claimed text: c3_phase3_exit_criterion.py 1025/932/617/645/646-650/541/282/286/587/679/1097/772/834-844/913-963 all exact; online_codebook.py 136 ('return len(self._buffer) >= self.consolidation_k') and 138-141 exact; gate0_frame_a.py _corr@553 / variance_report_from_summary@566 / run_variance_decomposition@668 / corr_AC_BD@620 / n_for_80pct_power_at_effect@627 / CLI 887,894 all confirmed; reports/ ceiling at 114 and no 115 confirmed; _build_substrate/_make_world confirmed NON-existent (grep NO_MATCH, correctly labeled fabricated by the prior agent). Three PRECISION nits, none rising to fabrication: (1) prior agent's '~1025 dataclass default' is a function-parameter default, not a @dataclass field (it acknowledges this itself); (2) its ITEM-11 'grep rc=1 / NO_MATCH' shorthand is imprecise because a naive RNG grep matches a docstring in online_codebook.py - only a narrowed grep for real torch RNG draws is empty (substance unaffected); (3) the module line-count/span cites are slightly off - torch_hopfield.py is 230 lines (doc's retrieve span '98-186' undershoots the method end ~173 but contains no RNG either way), phase4/consolidation.py is 965 not 966. None of these change any verdict. The prior agent's honesty about the delivery lag is corroborated by my session hitting the identical lag then recovering.

### anti-homunculus
- net: CONCUR. The audit's anti-homunculus conclusion is CORRECT and I independently confirm it on both design-spec and code substance. All five candidate Frame B mechanisms (read-only checkpoint hook, window-averaging via window_seed_override, realized-sigma gate, branch-precedence _classify_verdict, G1-G4 smoke gates) are thermometers / offline phase-graduation-or-workflow routing; none reads recall/sigma at runtime to gate/pause/branch/modulate the consolidation loop or substrate. The single substrate-touching event -- when consolidation fires -- is unchanged: a local buffer-readiness event code-verified at c3:646-654 (personally re-opened: `if updater.observe(...): diag = updater.consolidate_if_ready(); if diag is not None: consolidation_events += 1`), no clock/scheduler. Byte-identity test 4a is correctly preserved as the binding read-only proof and the FB->confound STOP, is genuinely mandated-but-unbuilt (minor, upheld), and the property holds by static code-path analysis (c3:282 is the only eval-side RNG; updater/Hopfield/C.2 are zero-RNG per code-claim-verification.md:74-78). Sense-B allocation is correctly fenced off as a future runtime-substrate mechanism needing its own pass (minor, upheld). I UPHOLD all seven severities (none x5, minor x2). On fabrication: I VINDICATE the agent's 'no fabricated citations' self-clearance -- after re-opening every cited line (design note, c3, gate0, all five grill drafts) I found ZERO fabricated lines. The lone defect is finding 6's '06:74' corroboration cite, which is MISLOCATED (lands on a citation-integrity summary line, not the primary evidence at code-claim-verification.md:64-99) but NOT fabricated. SELF-CORRECTION: in an earlier in-session pass I provisionally flagged 06:51-59 as non-existent and gate0:721 as wrong-line; both were ARTIFACTS of an intermittent file-read output failure and are RETRACTED -- on successful re-open, 06 is 104 lines (:51-59 exist, verbatim-accurate) and gate0:721 is exactly `window_seed_override=window_seed,`. RECOMMENDATION: accept the anti-homunculus PASS as binding-eligible; only cleanup is to repoint finding 6's '06:74' at code-claim-verification.md:64-99. The repo's documented fabrication history did NOT recur in this finding set.
- fabrication-check: CLEAN with one mislocated (non-fabricated) cross-cite. Re-opened every cited file. (A) Design note (all 273 lines): ZERO fabrications -- :6, :104-108, :135-140, :144-145, :147-154, :153, :156-181, :158-160, :161-167, :168-171, :172-177, :177-180, :187-193, :204, :227, :250, :252-253 all accurate/verbatim. (B) Code: c3:282, :617, :635, :637, :646-654, :708, :854-858, :862, :867 all accurate; gate0:721 = `window_seed_override=window_seed,` CORRECT (I RETRACT an earlier wrong-line flag made before re-opening gate0). (C) Grill drafts: 06-stage1-review-findings.md is 104 lines (NOT 46), so :51-59 EXIST and are verbatim-accurate (I RETRACT an earlier non-existent-line fabrication flag); 01:45-67, 03:10-29 + :42-55, 04:59-65, code-claim-verification.md:64-99 all verbatim-accurate. ONE DEFECT: finding 6's corroboration cite '06:74' is MISLOCATED (line exists, lands on the citation-integrity summary 'item-11 byte-identity still holds' rather than the primary evidence at code-claim-verification.md:64-99) -- sloppy cross-reference, not a fabrication. NET: the audited agent's 'no fabricated citations' self-clearance is SUBSTANTIALLY TRUE; zero fabricated lines found, only one mislocated-but-valid cross-cite. My own earlier in-session fabrication flags (06:51-59, gate0:721) were ARTIFACTS of an intermittent read-output failure and are RETRACTED after successful re-open.