# Project STATUS

The bookmark. Read this first every session before doing anything. If something
in this file is wrong or stale, fix this file *first*, then do the work.

Keep it small: a fixed-shape **Current state** block (overwritten each session)
plus a rolling **Recent updates** window (≤10 entries / ≤7 days). Long-form
narrative, walk-back chains, and per-report detail live in the numbered reports
and the monthly archive at [notes/status-log/](notes/status-log/) — never inline
here. See [§Maintaining this file](#maintaining-this-file).

## Current state

*(2026-05-29. Five fixed fields — overwrite, do not append. Detail → report + archive.)*

- **Active phase:** **Phase 3 — continual learning / "Growing Codebook" (Frame B).** Reframed 2026-05-28 after the C.3 shuffled-token control was found **gauge-vacuous** (it permutes which i.i.d. atom each token-id wears → `E[Δ]=0` by construction), which makes the Path C / Path α / Γ1 null chain uninterpretable as evidence about corpus-specific learning. Anchor: [2026-05-28-phase3-frame-b-continual-learning-and-gauge-control-finding.md](notes/notes/2026-05-28-phase3-frame-b-continual-learning-and-gauge-control-finding.md).
- **Headline metric:** Frame B endpoint **level-DiD** is **CLOSED 2026-05-30** — not economically rescuable (Report 117; three rescue levers closed 116/117/118). **Replaced (user-approved 2026-05-30)** by the new Phase-3 **consolidation-write** sub-program headline: held-out **value-codebook `top_index_hits` Selectivity-Δ** = recoverability(true-role cue) − recoverability(role-shuffled cue), **two-floor Wilson rule** (absolute true-role arm CI lower > 1/N **and** Δ CI > 0; a Δ>0 from a sub-chance shuffled arm is rejected). Spec: [phase-3-consolidation-write-design.md](notes/emergent-codebook/phase-3-consolidation-write-design.md) (§Headline / §Required controls). Slope-DiD stays **dropped** (`corr_AC_BD=+0.10`, [Report 116](reports/116_frameb_corr_ac_bd_slope_drop.md)); old C.3 `ΔE`-vs-shuffled-token control stays **retired** (gauge-vacuous).
- **Active deliverable:** **Consolidation-write sub-program — surgical mechanism (heteroassoc write + L2 cue-space decorrelator) GRADUATED, G-D PASS, and INTEGRATED into the real consolidation path.** Verdict **SURGICAL-in-place, NOT rebuild** (the substrate holds real associations; the write — not the binding algebra — was the wall). Chain: [052](reports/052_graduation_d4096/report.md) failed → [053](reports/053_autopsy_killer_variable/report.md) autopsy (killer=D-scale) → [054](reports/054_decorrelator_fix/report.md) one-line L2-renorm fix → **[055](reports/055_graduation_certificate/report.md) GRADUATION** (D=4096 WikiText, tracks the info ceiling, multi-seed, re-verified) → **[056](reports/056_gd_selectivity_panel/report.md) G-D PASS: the write is a ROLE-selective associative MEMORY** (in-sample role-selective recall passes on topic-corpus + repo_sample + D=4096 WikiText 3/3, all controls; held-out split reveals **memory-not-learner** — generalizes on low-rank structure, real-text held-out ≈ chance; **memorization = the target** per contextual-completion-NOT-prediction, user-approved 2026-05-31; entropy/margin drill-down: a miss is sharp-wrong not flat) → **[057](reports/057_phase4_hetero_integration/report.md) INTEGRATED** into `OnlineCodebookUpdater` (`phase34/online_codebook.py`): `observe(cue=)` + `consolidate_hetero()` + `recall_hetero()`, **dense H**, batch-offline, **default-off flag → byte-identical** (117 insertions/0 deletions; 102 tests green), AH-reviewer PASS, key=raw cue (bypasses scene-MHN). **MESH-scaffold scaling DEFERRED** (open user decision; noted in spec + config docstring + memory). The decorrelator is the **sole active ingredient** (raw write == floor; batch ZCA whitening, distinct from atom-space `repulsion_force`); online FEP form too slow. **Wiring CONFIRMED ([Report 058](reports/058_e2e_integration_wiring/report.md), 2026-05-31):** the integrated public API (`observe(cue=)`→`consolidate_hetero()`→`recall_hetero()`) reproduces the standalone 055/056 mechanism **BIT-IDENTICALLY** (24/24 cells `torch.equal` on H + basin indices; synthetic obs2 role-Δ=0.592=published; random-cb→chance; adversarial verify PASS/high, 3 falsification probes); Phase-4 checklist **F3** added. **D=4096 WikiText-2 GPU confirmation DONE** (Colab): bit-identical, role-Δ 0.32/0.73/0.91 = published 055/056 **exactly** (A−report=+0.000), true_rate at the info ceiling. **Scaling RESOLVED ([Report 120](reports/120_mesh_scaling_decision/report.md), 2026-05-31):** ship dense H; **DEFER MESH** (wrong tool) — graduated H is intrinsically low-rank (eff-rank = L=8 toy / ~126 real text ≪ D; SVD-truncated H byte-identical recall at r=D/16, 4–130× smaller), so the cost fallback is **factored low-rank H, NOT MESH** (whose N≫D catastrophic-forgetting-cliff property is unneeded at N≤D). Pre-existing Phase-5′ `min_branch` aggregator still FAILS anti-homunculus (de-arbitrate before any 5′ reopen).
- **Blockers:** (1) Retrieval-time interventions over the existing FHRR substrate are **closed at smoke scale** (D1/D3/E1/M1 null; Reports 062–066). Training-time **M2 path remains open**. **Range-lane correction (2026-05-30, supersedes "lane closed"):** [Report 111](reports/111_phase5_prime_range_replay_downstream_viability.md) closed only the **storage-side `range_postsettle`** proxy (novelty-without-retrieval); the **Dorrell nonneg-AE *training*-objective** (Report 068:120-128, "That work is deferred") is **OPEN** — fence-clear as a Stage-1 candidate under Selectivity-Δ (the bundled ΔE re-run stays Phase-5′-fenced). Mirrors the freq-weighted-α walk-back. (2) No path yet has an **n≥10 result with a control** — no graduation-style claim is licensed.
- **Paused:** **Phase 5′.** Pre-pause headline spec, binding once Phase 5 reopens: `ΔE = E_content-prior − E_role-prior`, magnitude floor `5.5e-3`, CI strictly > 0 ([phase-5-unified-design.md:282-297](notes/emergent-codebook/phase-5-unified-design.md)). The 2026-05-26 audit §9 flags a saturating `raw_scene_energy_v0` readout and an arbitration-shaped `min_branch` aggregator that must be fixed before any reopen ([audit-phase5-2026-05-26.md](audit-phase5-2026-05-26.md)).

## Recent updates

Newest first. Rolling window — ≤10 entries / ≤7 days, each ≤5 lines. Older
entries live **only** in [notes/status-log/2026-05.md](notes/status-log/2026-05.md).

- **2026-05-31** — **Two open questions CLOSED (ground-before-build).** **Scene-ID generalization lever → NO-GO** (4-agent grounding): scene-ID is pure stored-item retrieval (no held-out dimension), structurally the SAME memory as [Report 119] one stage upstream (only a per-scene identity key moves it, Report 075; PAM held-out scene-key=0.000; Report 056 leaves no transferable structure) → **3rd confirmation generalization here is a memory by design**; the in-sample scene-ID *memorization* lever is already built+validated (Reports 075–081, n=10). **MESH scaling RESOLVED → defer MESH** ([Report 120](reports/120_mesh_scaling_decision/report.md)): graduated H is intrinsically low-rank (eff-rank = L=8 toy / ~126 real text ≪ D), SVD-truncated H gives **byte-identical recall at r=D/16** (4–130× smaller) → cost fallback = **factored low-rank H, NOT the unvalidated MESH scaffold** (whose N≫D cliff is unneeded at N≤D). [archive](notes/status-log/2026-05.md)
- **2026-05-31** — **Generalization-axis (SECONDARY/exploratory): grounded → BUILT → RESOLVED = a THIRD MEMORY (no generalization; value-codebook-shaping is a dead end).** Q: can a consolidation write lay down *generalizable* structure beyond memorization? **Dorrell rectangular-support (2410.06232) does NOT port** (theorem needs a linear/invertible-source read; FHRR bind is non-invertible trig → 3 grill fatals → DESIGN-ONLY). **Within-scene predictive/JEPA PORTS** (FHRR conjugate = free exact inverse, the Dorrell-killer absent), axiom-legal, AH+fence-clean, COMPOSES with H; hardened 0 fatals (`WS-InfoNCE`, codebook-primary g_φ=identity). Citations repaired → cluster rests on PAM **alone = itself memory-not-learner** → zero paper-backed generalization evidence; a held-out PASS was the only admissible source. **BUILT Stage-1 + RAN ([Report 119](reports/119_ws_infonce_codebook_shaping/report.md), `phase4/ws_infonce.py`, 10/10 tests, exp58): held-out composition-margin Δ(C′)−Δ(rawC) ≤ 0 at EVERY cue richness (0/6 cells, 0/3 seeds), significantly NEGATIVE at the rich-cue end, robust across (τ,epochs).** In-sample floor guard PASSES (licenses the read); F-KILL **memory-not-learner CONFIRMED** (held-out ≈ chance, decays with frac_seen). Drill-down: the InfoNCE collapses the value codebook (d_eff 341→~165) → wrecks held-out cleanup separability; real text has no generalizable low-rank structure to write (Report 056). F-REPRO pre-discharged by [Report 067](reports/067_mqar_multirole_bundle_first.md). **DON'T-SCALE-A-NULL honored: no WikiText.** Stage-2 NOT licensed (no Stage-1 basin). Memorization (055–058) unaffected. [jepa](notes/emergent-codebook/phase-3-within-scene-predictive-jepa-design.md) · [archive](notes/status-log/2026-05.md)
- **2026-05-31** — **Integration WIRING confirmed BIT-IDENTICAL ([Report 058](reports/058_e2e_integration_wiring/report.md)).** The thin end-to-end check drove identical masked-encoding data through the integrated `OnlineCodebookUpdater` public API (`observe(cue=)`→`consolidate_hetero()`→`recall_hetero()`) vs the standalone 055/056 harness → **24/24 cells `torch.equal` on H + basin indices** (`all_basin_indices_bit_identical=True`), synthetic obs2 role-Δ=**0.592**=published, random-cb readout-leak control→chance, AH/routing invariants preserved. Adversarial 4-agent verify **PASS / high / no genuine defect** (3 injected wrapper bugs all break the match → equivalence non-vacuous). repo_sample ±0.024 anchor drift = the *live* corpus grew (n 993→1040), **not** wiring (A==B is bit-exact intra-run). **D=4096 WikiText-2 GPU run (Colab) CONFIRMS it:** bit-identical, role-Δ 0.32/0.73/0.91 = published 055/056 **exactly** (A−report=+0.000; WikiText is a fixed corpus → zero drift), true_rate at the ceiling. Phase-4 checklist **F3** done; MESH dense-H scaling still open. [archive](notes/status-log/2026-05.md)
- **2026-05-31** — **D=4096 graduation failed → autopsy REVERSES the attribution (do NOT pivot).** [Report 052](reports/052_graduation_d4096/report.md): floor-gated, obs=1 = tie (0.098 vs 0.096); the notebook's "PASS" **RETRACTED** (gated vs store-as-is, itself sub-floor). **[Report 053](reports/053_autopsy_killer_variable/report.md) corrects the cause:** killer is **D/scale, NOT corpus** — the mechanism hits the **0.51 info ceiling at D=512** and collapses to ~0.10 at D=4096 on both corpora (key-correlation constant) → a **fixable decorrelator-at-high-D bug**; **mechanism-limited not task-limited** (obs=1 info ceiling 0.42). Two Colab notebooks (graduation + autopsy), verdicts adversarially verified. **FIXED ([Report 054](reports/054_decorrelator_fix/report.md)):** a one-line decorrelator **renorm bug** (element-wise vs **L2** — element-wise filled the rank-deficient null space with noise, ~93% at D=4096). D=4096 obs=1 rescued **floor 0.10 → ceiling 0.43** (WikiText). **GRADUATION CONFIRMED ([Report 055](reports/055_graduation_certificate/report.md)):** the multi-seed Colab certificate (3 seeds, N≈2174/cell, **independently re-verified from raw Drive JSONs**) shows write+decorr **tracking the information ceiling across cue richness** (obs 1/2/3/5 = 0.42/0.76/0.93/0.997 ≈ Bayes-opt), 8–15× store-as-is, **disjoint-CI floor-clear at every cell**; the element-wise-bug ablation ties the floor → the L2 fix is the whole signal. **G-D DISCHARGED same day ([Report 056](reports/056_gd_selectivity_panel/report.md)): the write is a ROLE-selective associative MEMORY** — in-sample (memorization, the contextual-completion target) it recalls role→target bindings role-selectively (two-floor + write-marginal PASS, topic-corpus + repo_sample, all controls; content-bag collapses, structure-ablation 7×). Adversarial verification (held-out split, 4 agents/3 ran code) revealed it is a **memory not a learner**: role-selectivity generalizes on low-rank structure (topic toy Δ0.42, bag→0) but real-text held-out ≈ chance → memorization is the target (CLAUDE.md contextual-completion); G-D judged in-sample (user-approved). **D=4096 WikiText Colab CONFIRMED (3/3 PASS, re-verified from raw JSONs): element-wise ablation separates hard (Fork 3 closes), held-out ≈ chance (memory-not-learner).** **INTEGRATED into the real consolidation path ([Report 057](reports/057_phase4_hetero_integration/report.md)):** `OnlineCodebookUpdater.{observe(cue=),consolidate_hetero,recall_hetero}`, dense H, batch-offline, default-off flag → byte-identical (117 ins/0 del, 102 tests green), AH-reviewer PASS; MESH deferred. entropy/margin drill-down done (sharp-wrong not flat). [archive](notes/status-log/2026-05.md)
- **2026-05-30** — **Mechanism built + AH-reviewed** (heteroassoc write + L2 cue-space decorrelator, `phase4/{hetero_write,decorrelator}.py`, 11 tests; batch ZCA, decorrelation is the sole active ingredient). Superseded by the 052→058 graduation/integration chain above. [Reports 049–051](reports/051_decorrelator_production_build/report.md) · [archive](notes/status-log/2026-05.md)
- **2026-05-30** — **Strategic rethink → research-grounded plan** (2 verified workflows, 84 agents): root cause localized to the consolidation **WRITE**; **surgical-in-place** chosen (user-approved); Stage-1 write-then-read gate built+smoked (`experiments/48_stage1_writeread_gate.py`, readout `phase3/basin_readout.py`; G-A frozen-refit shows NO readout defect → surgical not readout-fix). Superseded by the 052→058 chain above. [plan](brainstorm-workspace/2026-05-30-research-grounded-plan/02-forward-plan-FINAL.md) · [archive](notes/status-log/2026-05.md)
- **2026-05-30** — **Landscape-size diagnostic DONE → landscape lever CLOSED.** Parallel fan-out (30 single-cell CUDA workers; anchor σ_A(64)=0.157 reproduced exactly). The frozen floor σ_C drops with L (0.069→0.023) but σ_A does **not** (0.157→0.139) → consolidation re-injects variance **independent of landscape size**; L=512 hit the β=10 capacity wall (lift +0.050→−0.006). Verdict **CAPACITY-WALL/irreducible**. **3 levers closed this session** (slope/level-DiD/landscape); strategic rethink pending. [Report 118](reports/118_frameb_landscape_sweep_variance_irreducible.md) · [archive](notes/status-log/2026-05.md)
- **2026-05-30** — **Level-DiD shown NOT economically rescuable; the n≈40 plan is killed.** 9-agent workflow (verified, reproduced from the recovered JSON): window-averaging via `window_seed_override` is **K× re-consolidation, not cheap eval**; total cells to clear zero is **monotonic in K** → min at K=1 = **n≈424 ≈ 42× n10** (n=40 → t=0.61, clears nothing). Report 115 §3's `K=9⇒σ=0.044`/`pool saturates` numbers were **wrong** (corrected). Root cause: **consolidation injects the variance** (σ_A=0.157 vs σ_C=0.069; DiD differencing *adds* variance). Decision pending: cheap landscape-sweep diagnostic vs mechanism reconsideration. [Report 117](reports/117_frameb_leveldid_feasibility_consolidation_variance.md) · [archive](notes/status-log/2026-05.md)
- **2026-05-30** — **Frame B branch RESOLVED: slope DROPPED.** Recovered the missing Gate-0 n=10 summary from Google Drive (artifact dependency discharged; now on-repo `reports/gate0_2026-05-28/`). `gate0_frame_a.py --variance-report` → **`corr_AC_BD=+0.10`** < pre-committed 0.3 cutoff (σ(A−C)=0.149, σ(B−D)=0.143, ~uncorrelated → the *outer* real−shuffle kill cancels ~nothing; honest-n≈750 at raw σ). Pre-commit `corr_AC_BD<0.3` fires → **drop slope, stay on level-DiD** (falsifies the outer kill; the inner-slope σ(β) kill stays **untested/Q1-open** — the drop honors the pre-commit, not a σ(β) measurement); `G0->weak`→escalate-n → **n≈27 level-DiD** is next. Drive verdict was stale `G0->dead`; current code reclassifies **`G0->weak`** (bug-fix confirmed). [Report 116](reports/116_frameb_corr_ac_bd_slope_drop.md) · [archive](notes/status-log/2026-05.md)
- *Older entries (2026-05-29 ← 2026-05-23): see [notes/status-log/2026-05.md](notes/status-log/2026-05.md).*

---

## Live operational policies

- **HAM regime split** (from report 022): β=30 + summed scores for retrieval; β=10 + HAM-arithmetic for replay diagnostics.
- **Online error-driven codebook updates: BANNED.** Use Hebbian for runtime, error-driven only in batch offline passes.
- **Drift sources sanctioned by design** (phase-4-unified-design.md:296-309): periodic batch retrains, online Hebbian reinforcement, or simulated synthetic perturbation.

---

## Pre-phase commitments (all resolved)

Specified as required before claiming a phase done. All three are now closed —
grep-confirmed 2026-05-29 after they had drifted as "still open" in this file.

- ~~Consolidation-geometry regime classifier (d̄, d_eff per atom)~~ **RESOLVED 2026-05-20** — built ([scripts/consolidation_geometry_diagnostic.py](scripts/consolidation_geometry_diagnostic.py)), lifted to a reusable module with tests ([regime_diagnostic.py](src/energy_memory/phase3/regime_diagnostic.py), [test](tests/test_regime_diagnostic.py)), and run twice (post-hoc over 10 snapshots in [Report 044](reports/044_consolidation_geometry_diagnostic.md); C.3 exit-criterion over live-consolidation atoms). The spec's per-atom `d̄`/`d_eff` are computed as specified; only the data referent shifted (substrate state ← live context-bag history).
- ~~Empirical θ′(β) calibration spike~~ **RESOLVED 2026-05-26** (Path C C.1.4) — E1 protocol run on the FHRR substrate ([calibrate_theta_prime.py](experiments/calibrate_theta_prime.py), [test](tests/test_theta_prime_calibration.py)) at β∈{0.01…100}; empirical θ′(β=10)≈0.99 confirms θ′≈1/β is off by 2–3 orders of magnitude at low β. Artifact: [theta_prime_calibration.json](notes/emergent-codebook/theta_prime_calibration.json).
- ~~Frequency-weighted Benna-Fusi α~~ **RESOLVED 2026-05-17** — built, tested, run at n=10×40 on Colab; production-scale null. See [Report 040](reports/040_freq_weighted_alpha_sweep.md) and [phase-5-unified-design.md:97-107](notes/emergent-codebook/phase-5-unified-design.md).

---

## Maintaining this file

- **Budget (enforced):** keep this file **< 250 lines / < 20 KB**. A pre-commit hook ([scripts/check_status_size.sh](scripts/check_status_size.sh)) fails the commit if it goes over. If an edit would exceed budget, trim the Recent-updates window **before** adding.
- **Current state** is a fixed 5-field block — **overwritten** each session, never appended. No narrative, no per-report chain; that goes to the report + archive.
- **Recent updates** is a rolling window: **≤10 entries / ≤7 days**, each **≤5 lines** (summary + report link + archive link). Adding an 11th entry means cutting the oldest — and the oldest may only be cut once its section exists in [notes/status-log/](notes/status-log/).
- **Walk-backs go first.** If a session walks back something currently in this file, that walk-back is the **first** edit of the session — and it goes in *Current state*, not stacked into *Recent updates*.
- Update at the **end of every working session**.
- Promote a "blocker" → "done" only when a report has **multi-seed CI evidence**.
- Monthly archive files are append-only (newest section at top within a file).
