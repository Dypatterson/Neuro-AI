# Session Handoff — 2026-05-26

Branch: `codex/phase5-prime-bundle-first-scene-memory`
Tip: `445eecd` (MPS as default device + MPS-native participation_ratio)
All work committed and pushed.

## TL;DR for next session

1. **Read STATUS.md banner + audit + Path C precommit first.** The session built and tested the Path C diagnostic-as-actuator framework end-to-end, then ran two C.3 graduation smokes that both produced nulls.
2. **Phase 3 does not graduate at the synthetic-vocab=200 operating point.** The Path α smoke (with `alpha_anti` enabled and a proper shuffled-token control) shows consolidation produces structural change *independent of corpus-specific token assignment*.
3. **Path β (WikiText-2 smoke) is the unfinished experiment.** Two attempts in this session: one CPU run killed at 45 min after we noticed MPS was off; an MPS retry was launched but interrupted before completion. The driver and pipeline are ready to retry.
4. **Falsification ladder:** synthetic null + alpha_anti null → WikiText-2 (next) → if still null, Path γ (Phase 3 mechanism redesign per `literature-and-principles.md`).
5. **Phase 5′ remains paused.**

## What landed in this session (commits, newest first)

| SHA | Title |
|---|---|
| `445eecd` | MPS as default device + MPS-native participation_ratio |
| `44c70b1` | Path α smoke: stronger null than C.3 baseline |
| `c833c2d` | C.3 second smoke: substantive NULL on Phase 3 exit criterion |
| `7151c41` | Land Path C C.3 scaffolding + smoke (partial) |
| `f0ec008` | Land Path C C.2.5 drift-replay-tension + C.2.4 A4 flake fix |
| `c405f7a` | Land Path C C.2.4 metastability replay-priority verification |
| `83a2000` | Land Path C C.2.3 cap-coverage error gradient |
| `53545c4` | Land Path C C.2.2 splitting-tension dynamic |
| `f14940f` | Land Path C C.2.1 NC1 anti-collapse dynamic |
| `b0f7cff` | Land Path C C.1 passive diagnostic stack |
| `328a1f3` | Add D=4096 FHRR substrate fidelity tests |
| `8a39770` | Reopen Phase 3 as Path C diagnostic backfill |

13 commits ahead of the previous session's tip (`f028ebf`).

## State of Path C

```
Path C = Phase 3 diagnostic-stack backfill (user-approved objective change 2026-05-26)

  C.1 (passive instrumentation)              ✓  CLOSED
       C.1.1 basin (NC1 + NC2 + pairwise fallback)         9/9 tests
       C.1.2 bimodality (Hartigan dip + GMM-BIC fallback)  10/10
       C.1.3 regime classifier (lifted from script)        7/7
       C.1.4 θ′(β) calibration spike                       3/3
       C.1.5 metastability EMA (thin wrapper)              5/5

  C.2 (diagnostic-as-actuator)               ✓  CLOSED
       C.2.1 NC1 anti-collapse force                       11/11
       C.2.2 splitting-tension modulation                  14/14
       C.2.3 cap-coverage error gradient                   14/14
       C.2.4 metastability replay-priority                 11/11
       C.2.5 drift replay-tension                          19/19

  C.3 (Phase 3 exit-criterion re-run)        ✗  NULL (two smokes, both null)
       Smoke 1 (C.3 second): consolidated std vs random ctrl  → null
       Smoke 2 (Path α):     consolidated std vs shuffled-token-ctrl → stronger null
       Smoke 3 (Path β):     WikiText-2 corpus → unfinished (interrupted)

  C.3 status: scaffolding + driver + dual smoke reports all committed.
              n=10 graduation never reached because n=3 smokes produced
              clean nulls that would only tighten under n=10.
```

Full discovery regression: **522/522 tests pass** at the current tip.

## Active blocker

**The two C.3 smokes (synthetic vocab=200 + Path α with proper control) showed consolidation produces structural change but no corpus-specific learning.** Standard condition (consolidation on real corpus) and shuffled-token control (consolidation on randomly-permuted vocab) produce statistically-indistinguishable Recall@K. Pull/push (Hebbian) IS firing; C.2.x dynamics ARE firing; `alpha_anti` IS firing. None produces measurable preference for the real corpus.

The audit's §4.3 hypothesis ("regime stratification + calibration reveals hidden signal Report 017 missed") is FALSIFIED at this configuration.

## Next session: pick up Path β (WikiText-2)

The Path β smoke was the natural next step but got interrupted before completion (twice). The driver is wired and tests pass. To re-run:

```bash
PYTORCH_ENABLE_MPS_FALLBACK=1 PYTHONPATH=src .venv/bin/python experiments/c3_phase3_exit_criterion.py \
    --seeds 0,1,2 \
    --corpus-source wikitext \
    --vocab-cap 1000 \
    --standard-mode consolidated \
    --control-mode shuffled-token \
    --theta-prime-mode both \
    --alpha-anti 0.01 \
    --repulsion-step-size 0.05 \
    --n-test-windows 500 \
    --output-dir reports/c3_smoke_wikitext_mps_<date>/
```

Notes:
- `PYTORCH_ENABLE_MPS_FALLBACK=1` is required because `torch.linalg.eigvalsh` (used in `C.2.2 _spatial_bimodality_signal` for the λ_2/λ_1 ratio) is not implemented on MPS. The env var falls eigvalsh back to CPU per-call; the rest stays on MPS. Tracking: github.com/pytorch/pytorch/issues/141287.
- `--device` now auto-detects MPS by default; no need to pass it explicitly.
- Estimated wall-clock on MPS: ~10-15 min (the prior CPU run was killed at 45 min before completion).
- If the smoke completes and shows the standard condition CI-disjoint above the shuffled-token control in any stratum or pooled — that's the first real Phase 3 signal. Escalate to n=10 graduation.
- If the smoke produces null again — that's a third null in a row. Pivot to Path γ.

### Expected outcomes and what to do

**Outcome A (positive — WikiText differentiates):** standard's Recall@K beats shuffled-token-control's by ≥ 0.02 in at least one stratum or pooled, CI-disjoint. → escalate to n=10 graduation run. If n=10 holds, Phase 3 graduates and Phase 5′ reopens.

**Outcome B (null — WikiText doesn't differentiate):** consolidation produces structure independent of corpus regardless of operating point. Path γ becomes the only honest move:
- Re-read `notes/emergent-codebook/literature-and-principles.md` for the three candidate mechanism families: Eugenio's Self-Organizing Language (hierarchical Hopfield chains), Hyperseed / SOM-VSA emergent codebooks, Taniguchi's Collective Predictive Coding.
- The current Phase 3 design (C.2.x diagnostic-as-actuator on a static FHRR substrate with pull/push) is not the mechanism. Either rework the Hebbian update to be more aggressive (e.g., higher `lr_pull`, more events), or commit to a structurally different design.

**Outcome C (intermediate — small signal but not graduation):** Δ in the 0.005-0.015 range, marginally CI-disjoint at n=10. This would suggest the mechanism is *almost* there but configuration-bound. Consider sweeping `n_consolidation_events`, `lr_pull`, and C.2.x strengths before declaring graduation or null.

## Things to check at session start

Per CLAUDE.md session-start protocol, the next session should:

1. Re-read `STATUS.md` — the top banner reflects Path α null.
2. Re-read `notes/emergent-codebook/phase-5-prime-checklist.md` §"Headline metric" and §"Required controls" lines (cited verbatim line refs).
3. Re-read `notes/notes/2026-05-26-path-c-phase3-diagnostic-backfill-precommit.md` Implementation Findings section (which records all five C.2.x landings + both C.3 nulls).
4. Grep `reports/` for any prior runs that might inform: `grep -rln 'corpus-source' reports/c3_smoke_*` (only the prior smokes are committed; no Path β output yet).

## Risks / known issues

1. **MPS `eigvalsh` fallback.** Current workaround is `PYTORCH_ENABLE_MPS_FALLBACK=1`. If the next session wants a fully-native MPS path, the C.2.2 `_spatial_bimodality_signal` could be reformulated to avoid `eigvalsh` (e.g., power iteration for top-2 eigenvalues), but this requires an anti-homunculus reviewer revisit because it changes the dynamic's numerical character. For now the fallback is fine.

2. **The C.3 driver's markdown output's "regime classifier agreement diagnostic" section** is stale text from before the calibration grid was extended. It claims "at β=10 the calibration JSON does not cover the operating point" but it does now (since commit c833c2d). The actual numerical results are correct (calibrated and default modes ARE producing different regime distributions). Just the commentary text is misleading. Worth fixing if next session iterates on the driver.

3. **Two methodological errors recorded** (in the precommit's Implementation Findings):
   - Error 1: degenerate stratified comparison when control's stratum has 0 trials. Future C.3 reports should NOT call out CI-disjointness in a stratum where one arm has 0 trials.
   - Error 2: "shuffled control" conflation — the proper Phase 3 shuffled-token control is shuffled-token-with-consolidation (now wired in Path α). The old `random` control mode is preserved for backward compatibility but should not be the default for graduation.

4. **Two pre-existing flakes fixed during this session** (both: unseeded global torch RNG order-dependency under full discovery):
   - C.2.4 A4 in `test_metastability_replay_priority.py:252` — `torch.manual_seed(3)` added.
   - C.2.5 A9 in `test_drift_replay_tension.py:680` — `torch.manual_seed(43)` added.
   If other tests show similar flaking, the fix pattern is the same.

## Files most worth re-reading

- `STATUS.md` (top banner reflects Path α null)
- `audit-phase5-2026-05-26.md` §9 verification deltas + the original 3 highest-leverage moves (TL;DR section)
- `notes/notes/2026-05-26-path-c-phase3-diagnostic-backfill-precommit.md` Implementation Findings (full history of C.1, C.2, C.3 landings + smoke nulls + recommended next paths)
- `notes/notes/2026-05-26-c25-drift-replay-tension-precommit.md` Implementation Findings (the C.2.5 closure + the joint-factor entropy WE1 finding)
- `experiments/c3_phase3_exit_criterion.py` (the driver to re-run for Path β)
- `notes/emergent-codebook/theta_prime_calibration.json` (extended grid β ∈ {0.01, 0.1, 1.0, 3, 10, 30, 100})
- `notes/emergent-codebook/literature-and-principles.md` (if pivoting to Path γ)

## Anti-homunculus discipline reminders for next session

The Path C precommit's H1–H23 bind:
- C.2 mechanisms must take right-hand-column form per 2026-05-09:140-148.
- Construction constants (`λ_ac`, `μ_T`, `τ_T`, `λ_cc`, `θ_cc`, `τ_cc`, `κ_meta`, `μ_rep`, `μ_obs`, `κ_drift`, `μ_drift`, `α_anti`) are fixed substrate constants; NEVER adaptive on observation.
- Actuators read substrate state, NOT diagnostic dataclasses (H6).
- No clamp on tension scalars (`Ψ`, `T_k`, etc.) — H23. Tune defaults instead.
- If Path γ proposes a new mechanism, the same 4-step cycle applies: precommit → reviewer PASS → implement → convergence-equivalence test.

The `min_branch` aggregator at `scripts/phase5_prime_strict_discriminator_viability.py:96-98` and the `q_bundle`/`q_greedy` dispatch at `scripts/phase5_frozen_snapshot_audit.py:656,915-916` remain flagged for the **post-Path-C Phase 5 reopen**. Out of scope until Path C closes.

## Session statistics

- 13 commits.
- 522 tests at tip (up from 2 — `test_fhrr.py` — at session start).
- 7 new modules in `src/energy_memory/phase3/`.
- 5 modifications to `src/energy_memory/phase4/` and `src/energy_memory/phase34/` (C.2.x wiring).
- 8 new test files.
- 1 new experiment driver + helper module + calibration artifact.
- 7 anti-homunculus-reviewer PASSes recorded (2 review-only + 5 C.2.x mechanism reviews).
- 5 C.2.x precommit design notes.
- 1 audit document (`audit-phase5-2026-05-26.md`).
- 1 Path C master precommit.

## What was NOT done (deliberately out of scope)

- **Phase 5 work.** All Phase 5 refactor candidates (`min_branch`, combiner dispatch, headline reform) remain out of scope per the Path C precommit. They reopen only when C.3 closes.
- **C.1 → consolidation integration.** None of the C.1 modules are wired into `ConsolidationDiagnostics` or the consolidation event dict. Each is a callable that takes substrate state and returns a diagnostics dataclass. The reviewer's preference was to keep them standalone until C.2 lands — that's done now, but I left integration as follow-up rather than expanding scope.
- **Phase 4 fixes from the audit.** Insertion-path novelty erasure (P9), replay scheduler as fixed-cadence (P9-adjacent), and the `tag_overlap`/`suppression_decay`/`recovery` defaults remain unaddressed. Reopen after Path C closes.

---

**Hand-off complete.** All work is committed and pushed at `445eecd`. The session's central finding — Path C built a principled diagnostic-as-actuator framework that produces measurable structural change but no corpus-specific learning at the Phase 2 operating point — is documented in the precommit, STATUS banners, and commit messages. Next session can pick up cleanly at the Path β WikiText smoke or pivot to Path γ at the user's choice.
